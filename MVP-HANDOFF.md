# MVP review and continuation handoff

## Steel lifecycle / OOM fix — latest

Docker memory exhaustion was confirmed: ~217 MiB available of 7.65 GiB, swap exhausted, OOMKilled true for all three recent containers, three cgroup oom_kill events in the paid-run container and a logged Page crashed error. Per-run containers were accumulating. User explicitly authorized stopping all Steel environments and automatic teardown at run end/shutdown. **13 running Steel containers stopped**, no profiles, container filesystems, workbooks or evidence deleted; `logs/steel-lifecycle-emergency-stop.json` records the one-time scope. No paid retry or Cvent action.

`stopJobBrowser()` now validates creation receipt, name, image and job label, stops by immutable container ID, verifies Running=false/Pid=0 and writes `receipts/steel-cleanup.json`. Central native Stop/settlement/failure/shutdown cleanup invokes it even if native cleanup fails. Stop waits for pending provisioning before teardown; failed provisioning cleans up directly too. No new prompt, replay, profile reset or uncertainty clearing. Cleanup errors remain durable STOPPED failures. Viewer displays Browser stopped after verified cleanup. **235 offline tests pass**, `logs/steel-lifecycle-tests.log`; source baseline `logs/steel-lifecycle-baseline-tu2v0a8e/`. Graceful app shutdown is covered; hard SIGKILL/host/daemon failure still requires manual ownership-verified reconciliation. Historical instructions to leave containers running are superseded. Full live RR acceptance remains unproven.

Activated by controlled app-only restart **45724 → 58367**, original credential/model environment reused only in memory. Receipt `logs/steel-lifecycle-activation-osemb0th/receipt.json`: **2,067 non-profile data files unchanged**, zero running Steel containers, USER ownership and stopped-viewer response verified. No new browser, paid prompt or Cvent mutation. Current runtime's already-stopped container was verified through the new helper to create its cleanup receipt; no historical job/report/cost rewrites.

## DONE policy activated — prior

On the user's explicit restart request, verified no active jobs, live recorded native Pi PIDs or operation locks, with USER browser ownership. Gracefully restarted **only the app, PID 71626 → 45724**, on `http://127.0.0.1:8788/`. Original credential/model launch environment was passed unchanged in memory, never printed or persisted. No browser/profile restart, reset, login/handoff, Cvent/API action, new job or paid prompt.

Receipt: `logs/rr-done-activation-631hnvkl/receipt.json`. **2,005 non-profile protected data files hash-identical**, including originals, historical jobs/prompts/results, runtime/ownership, saved workbook versions and spending. Global recorded Pi cost remains $39.386908 (not the event-only total). `/api/runtime` verifies login-first + USER ownership; no active jobs, served JS matches tested source, new server lock owns the expected PID. Reused 211-test passing evidence; no duplicate test run needed for an app-only restart. The DONE/INCOMPLETE/STOPPED handling and prior exact-match/separate-creation adjustments are now loaded. Refresh the dashboard. A new paid run still requires a fresh upload/Start/human Return; historical runs are not resumed or reclassified on disk.

## RR completion means DONE, not review — implementation before activation

User confirmed the supplied EmeraldX roadmap's individual-run completion scope: website (theme/branding, header/footer/all six body-widget types), registration (admission items, pricing, paths, optional items, vouchers, advanced rules), plus connected dependencies. Implemented this definition in the **362-word canonical task**. All RR-required configuration must be saved and verified in Cvent, still Draft. Exact matches count; originals remain immutable and differing/missing requirements call for separate compliant creations. Reading, a report, or normal session exit is not completion. Continue independent work; missing/blocked/unverified requirements remain INCOMPLETE. No review phase.

The existing final-report.json now carries only a small explicit execution result: eventId, status DONE/INCOMPLETE, completion booleans website/registration/dependencies/draft, blockers/untested arrays. `reportedCompletion()` in rr-connection reads this bounded job-local file after native cleanup. DONE requires the bound event, all four strict true values, empty exception arrays and no retained uncertainty/locks. Missing/malformed/oversized/linked/wrong-event/contradictory data yields INCOMPLETE. Stop/cleanup/spending/API/tool uncertainty still override success as STOPPED; historical FINISHED/REVIEW_REQUIRED display INCOMPLETE without rewriting records. Job/state/result/public-event/UI paths agree. No ledger audit, reviewer, new process, automatic prompt/retry or semantic-certification claim. Pi is still responsible for actual saved-result verification; booleans cannot independently prove its truth.

**211 offline tests pass**, `logs/rr-done-tests.log`: successful DONE end-to-end, normal exit without report INCOMPLETE, every missing/false scope flag, blockers/untested/uncertain writes, wrong event, malformed/linked/oversized report, cleanup failure overriding even a complete report, unchanged ownership/one prompt, and historical UI mapping. Repository artifact-path, whitespace and Node syntax checks pass. Baseline `logs/rr-done-baseline-jzr2z4nd/` preserves the previous dirty source.

No paid run, live Cvent/API/browser operation, restart, commit or push. **Server activation is still pending a controlled app-only restart.** The coding shell has no Cvent credential-file configuration; do not replace the live server's working launch environment blindly. Fresh approvals capture the current task; no historical prompt/run/cost/evidence is rewritten. Last settled run/headroom remain as recorded below. Roadmap Azure/M365/email/approval/triage/CI engagement deliverables are not implicitly added to this per-RR scope; latest user instruction explicitly removes automatic review outcomes.

## Exact RR execution, no mandatory review — prior source-only update

User explicitly changed preservation semantics: **reuse only exact RR matches; any RR-specified letter/number/format/relationship difference requires a separate RR-compliant object, leaving originals untouched.** Removed the prohibition on replacement variants from the canonical 322-word task. Native Pi still chooses its plan and tools and is told to execute until complete or no permitted executable work remains—not deliver a review request. Never invent missing values or suffixes, overwrite originals, rename the event, delete or publish. Cvent singleton/uniqueness constraints can still be real blockers; other scope, ownership, human-login, uncertainty and spending boundaries remain.

Natural `agent_settled` now ends as FINISHED, not REVIEW_REQUIRED. Stop, failed cleanup, malformed state, retained outstanding tools/API uncertainty or unreconciled spending remain STOPPED. Result/state/public activity/UI agree; the native final text is preserved and no completion validator or automatic reprompt is introduced. FINISHED is a session lifecycle label, not semantic certification. Historical job records are unchanged; the UI maps their legacy labels without rewriting evidence. Security-incident attestations are separate and unchanged.

API changes: differing supplied fields return `creation-required`/CREATION_REQUIRED plus an explicit same-identity authoring limitation, never satisfied or silently reused. Exact volume names (including case/internal spaces) are distinct; removed the equivalent-rule/different-name ban. Original records, complete catalogs, new-ID-only link/finalization, uncertainty and no-replay guards remain. The adapter still cannot create two objects under the same identity; documented Ego creation may handle that gap only where Cvent supports a separate exact RR object. Discount-code collisions remain conservative, with literal spelling differences explicitly reported. Do not misrepresent this as universal duplicate-object support or verified platform uniqueness semantics.

**206 offline tests pass**, `logs/exact-rr-execution-tests.log`: exact-name variant POST + original preservation + rerun no-op, differing fields, CLI receipts/identity keys, native terminal states/cleanup, legacy UI labels and all prior safety tests. Baseline: `logs/exact-rr-execution-baseline-6gbp59ot/`. No paid run, live Cvent/API/browser operation, server restart, commit or push. Connection changes need controlled app-only restart before the next run; fresh approvals capture the new task, old captured scopes remain immutable. Source/UI tests are not full live acceptance.

The last live run remains `280f6e34-1a96-4316-9825-da087e812e26`: 6m51s, $3.199532 run / $38.286896 event, about $11.71 model headroom. Zero API write intents, nine successful Ego executions, no native tool errors; report says zero mutations and substantial untested work. It ran the prior immutable-existing policy. Native PID gone, USER ownership. Sanitized monitoring evidence: `logs/live-monitor-280f6e34-summary.json`. Do not resume it or reset cumulative spend.

## Native task simplification — prior update

User corrected the approach: lean on native Pi/RPC and a plain RR execution request, not more required schemas/audits. Replaced the 547-word checklist/task with a **296-word prompt**: uploaded workbook + authorized event, Ego/assigned local Steel, preserve existing items/name, only complete permitted missing creations, prohibited-action/security/uncertainty boundaries, saved verification and honest source-referenced results. Pi chooses its plan and workbook-reading approach. Removed the seven-section checklist, mandatory evidence sequence, requirements.json contract and audit-before-finish instruction. Compact evidence (including the number-format fix) remains optional; its audit command is explicitly a manual review tool, not an execution requirement.

Removed the automatic settlement audit hook, its standalone wrapper and two wrapper tests. `app/rr-connection.mjs` is **byte-for-byte identical to the pre-audit source** saved in `logs/simple-fixes-baseline-3ab1irs3/`. Native RPC transport/system prompt/tools/compaction/retries remain unchanged; settlement waits for `agent_settled`, then returns the result with REVIEW_REQUIRED and existing Stop/USER ownership cleanup. No validator, synthetic follow-up or new model turn controls completion. Upload/Start/human login/Return, API preservation, target, uncertainty and cumulative spending gates are unchanged.

**203 offline tests pass**, `logs/native-task-simplification-tests.log`. Updated regressions check one concise task, no required audit/schema, and no settlement on intermediate agent_end/compaction events; natural settlement succeeds without a ledger and sends only one prompt. The count drops by two because the removed wrapper's two tests were removed, not because safety tests were skipped. Repository artifact-path and whitespace checks pass. Baseline copies: `logs/native-task-simplification-baseline-e4j46um8/`. Installed Pi README, full RPC protocol and RPC example reviewed; no SDK migration or Pi internals changed.

No app/browser restart, live browser/API operation, paid execution, workbook execution, commit or push. User's example Downloads path/event was not adopted or executed. Fresh approvals capture the live prompt; historical SOWs/jobs/evidence/costs and dirty vendor remain intact. No completion-time or full-acceptance claim. Prior automatic-audit activation advice below is superseded; the audit integration was source-only and is now removed.

## Simple system-wide evidence / completion fixes — prior source-only update

Implemented the user's requested minimal fixes, not a new execution framework. `rr-evidence` now groups/previews number formats, keeping 10% and $0.10 distinct. Added `audit` with a small source/event-bound `requirements.json` contract documented in `RR-EVIDENCE.md`: all-sheet accounting, bounded source ranges, dispositions, required saved-state/absence/intent receipt pointers and blocker fields. Full immutable audit remains on disk; stdout is paginated. UNTESTED/UNCERTAIN and missing evidence remain INCOMPLETE. Structural accounting never proves semantic coverage, receipt truth, authorization or saved state. Native Pi keeps autonomy; no automatic reprompt/continuation or writes.

`app/completion-audit.mjs` and `rr-connection.mjs` run the same local check once on natural settlement, with a 10-second timeout/fail-closed UNAVAILABLE summary. Stop/ownership cleanup and REVIEW_REQUIRED remain; no acceptance or paid retry is inferred. **Server integration is source-only and needs a controlled app-only restart; none was performed.** CLI changes load immediately; the **547-word** canonical prompt is captured only for fresh approvals. Historical captured prompts/jobs were not rewritten.

Added `npm run check:installation` (local executable/dependency/configuration presence only, no Pi/Docker/browser/Cvent requests), `requirements.txt` pinning openpyxl 3.1.5, configurable absolute `CVENT_AGENT_ROOT` / `CVENT_UI_AUTOMATION_ROOT` (existing defaults retained), and minimal local-only setup guidance in README. External clients/vendor patch and fresh-install/live acceptance remain prerequisites, not solved by a green doctor. Current coding-shell check is BLOCKED solely on missing Cvent credential source; it did not inspect the running app environment or read secret contents. Do not infer the existing server lost its credentials.

**205 offline tests pass**, `logs/simple-fixes-final-tests.log`. Two earlier test attempts caught the prompt's existing <550-word limit; prompt shortened without weakening that assertion. Repository artifact-path and whitespace checks pass. Copied-evidence replay `logs/simple-fixes-replay-3szg484o/receipt.json` retains 655 mappings (649 existing/six missing), 19 format-aware patterns/655 rows, and identifies missing old-ledger coverage as INCOMPLETE; original job unchanged. Original dirty baseline copies: `logs/simple-fixes-baseline-3ab1irs3/`. No new paid execution, API request, Cvent mutation, browser operation, restart, commit or push. Existing dirty vendor and user changes preserved. Next: inspect source changes, authorize controlled activation if desired, then resolve actual RR inputs and acceptance prerequisites before any paid run. 90-minute full acceptance, deployment signoff and prior spending limits remain unproven/unchanged by this implementation.

## Settled CED261 run and compact evidence — latest

User reaffirmed **no existing-setting updates**, requested log/Ego review and implementation to support full execution. Job `e746d74b-df9f-4b2d-a4ea-6927b62d6ba1` ran 19:51:07–20:01:19 UTC on 2026-09-20, settled `REVIEW_REQUIRED`, **$6.065026** session / **$35.087364** cumulative event. Native PID 88379 is gone. No API write intents; report says no Cvent mutations. Existing differences and incomplete requirements prevented creation; broad untested areas remain, so this is not acceptance. One recovered workbook `EmptyCell.row` error, not the prior browser crash. All 33 RR-related question-choice reads passed in about 130 seconds with no model messages during that tool wait. Large document/workbook/catalog outputs grew retained context; tiny later actions recorded roughly $0.17 each. Bulk would not solve this run: 649 discount rows matched existing codes, six were missing, with one later classified not applicable and five blocked for incomplete fields/eligibility.

Explicit project Ego attempt `taskSpace(1246080070).tabs()` was blocked: **Use the app Return to Agent gate before resuming this TaskSpace**. Browser is USER-owned after settlement. No takeover/ownership rewrite, direct-CDP fallback, reload/reset, new browser/job or paid prompt. Saved Ego snapshots support diagnosis, not a fresh live inspection. A valid human handoff is still required; do not resume this settled job.

Added `bin/rr-evidence` / `app/rr-evidence.py`: generic all-sheet source-addressed extraction, bounded cell/text views, repeated-row term grouping, local scoped receipt identity views and full-row code/name comparisons with compact exception output. Original hash checks, explicit pagination/truncation, full content-addressed evidence, formula/comment/link retention, malformed/foreign/failed receipt rejection, symlink/bounds checks. No network, hardcoded workbook schema, defaults, mutations or satisfaction inference. A compact match is not live absence/purpose proof. `RR-EVIDENCE.md` documents commands/limits. Prompt is **524 words**, directs compact evidence use and requires a source-referenced ledger plus specific blockers for untested work. No app-side financial changes or automatic deadline. This is not a complete shell sandbox or a guarantee of full coverage.

Offline copied-evidence replay reproduced all **655 row/ID mappings (649 existing, six missing)** in **1,392 output characters**, retaining **21 sheets / 18,230 cells**; ~1.2 seconds for index+comparison. `logs/compact-evidence-replay-djxzb_tg/replay-receipt.json`. Discount terms C:P collapse 655 rows into 19 distinct patterns without losing source membership. **195 offline tests pass** in `logs/compact-evidence-final-tests.log`; the first full run failed one obsolete prompt-text assertion, corrected to check the new bounded-evidence instruction. Source-only artifact-path and whitespace checks pass. No restart needed: helper CLI loads current source, live SOW getter affects only fresh approvals. Historical scopes, job records, originals, costs and uncertainty remain intact. Changes local/uncommitted. Remaining event AI headroom is about **$14.91**, not a new $60. Do not consume another paid run just for tuning; full live/90-minute acceptance requires a suitable fully specified permitted creation and independent saved-result review.

## Create-only API expansion — prior update

User authorized implementing all identified API writes fitting the existing SOW, not live execution. Reviewed RR-relevant public OpenAPI write families; the remaining clearly permitted family was **volume-discount creation**. Added `configureVolumeDiscount` using the existing discount transaction lifecycle: complete identity/purpose-collision checks, immutable existing rules, explicit four-type threshold schema, optional initial AdmissionItem/QuantityItem links, inactive creation, durable intent per step, exact new-ID-only finalization, independent readback and shared no-replay uncertainty. Separate job-local `api-volume-discounts.json` retains verified identities. Legacy updates, standalone links, feature switches/launch, attendees and account-global operations remain blocked. The API has no atomic uniqueness/ETag guarantee; live financial behavior is not certified.

Added `listQuestionChoices` (prove question membership through the complete approved-event catalog first) and `listEventFeatures` (status only), both with bounded scoped pagination. Updated the compact prompt map to **474 words**, leaving seven sections, autonomous ordering and app-side spending gates intact. `CVENT-API.md` supplies on-demand schemas; `CVENT-API-COVERAGE.md` records the write-family audit. Removed stale forced sequencing from the API guide.

**188 offline tests pass**, `logs/api-expansion-full-tests.log`; syntax, whitespace and repository artifact-path checks pass. Includes mocked production CLI creation/link/finalization, retained identities, preservation, missing RR references, Stop/takeover, foreign uncertainty, stale readback and cross-operation replay blocking; question choices reject wrong/missing membership and pagination escapes. No live API request, Cvent mutation, paid prompt, new RR, browser/profile operation or app restart. App lock/PID **71626** reverified. Fresh CLI invocations load the adapter directly; the live SOW getter captures the updated prompt at the next fresh approval, so no restart is required for these additions. Historical captured SOWs are not rewritten. Browser crash isolation, saved-state review and full RR acceptance remain unresolved. Changes are local/uncommitted; no push.

## Concise routing without model budget coaching — prior update

User wants concise, targeted runs without a dollar ceiling distracting the model. `app/runner-prompt.md` is now **473 words**, including seven sections and a compact API-read/API-write/browser map. Reads list actual supported catalogs; the only production API write is `configureDiscount` for confirmed-missing supported final-total/item-scoped codes. Existing-code updates, volume discounts and unsupported eligibility remain gaps. Relevant API documentation is consulted on demand, not a mandatory full audit read. A regression test checks the map against local CLI capabilities without credentials/network access. Corrected obsolete forced-write-sequencing prose in `CVENT-API-COVERAGE.md`.

Removed dollar targets/budget coaching from the canonical task and all four financial fields from `executionPrompt()`'s job envelope; stopped directing Pi to read job.json initially or produce spending reports. **App allowance/reserve, cumulative accounting and fail-closed spending gates remain unchanged.** This is not authorization for unlimited spend. Historical ledgers/receipts still contain cost data and remain accessible locally; this change removes proactive prompt injection, not all possible model visibility of billing. Native tools, scope/preservation, Stop/uncertainty and UI/upload behavior remain intact. Legacy intake is not enabled in production.

**177 offline tests pass** (`logs/concise-routing-full-tests.log`), including actual login-first and retained legacy execution prompts without financial fields, unchanged server limits and budget Stop tests. App-only restart **17768 → 71626**, port 8788, receipt `logs/concise-routing-activation-2026-09-20T19-09-02-758Z/receipt.json`: **288 protected files unchanged**, USER ownership, unchanged global cost **$30.122350**. No browser/profile reset, Cvent mutation, new RR job or paid prompt. Earlier captured SOWs remain intact; next fresh approval uses new instructions. Browser-crash trigger and full RR acceptance remain unproven; no ~$60 completion guarantee. Source changes remain local/uncommitted.

## Page-crash isolation and section prompt — prior update

Read-only investigation found **Steel CDPService/Puppeteer `Page crashed!` at 2026-09-20T04:15:38.906Z**, with `pageId` matching the assigned target of stopped job `ca3dfacc-a81a-47c0-8b8c-53152c1d32db`. This followed the 04:15:38.243 tool invocation clicking the observed Settings ref 30100 and requesting a snapshot; the bridge's `Accessibility.enable` timeout appeared at 04:15:58.835, followed by a URL evaluation timeout and unsuccessful reload. The first failure was a Chromium page crash, not merely Pi indecision or an ambiguous selector. **Which request inside click/snapshot triggered it, and why Chromium crashed, remain unisolated.** Do not attribute the crash conclusively to Ego, Cvent, the bridge or OOM.

Evidence: private `logs/hang-isolation/diagnosis.json`, matching Steel log window and sanitized tool timeline. Container has no recorded restart and `OOMKilled=false`, configured 2 GiB shared memory; current memory ~687 MiB. These do not establish crash-time renderer resources or exclude renderer-only OOM. Docker retained-event query found no OOM entries, not proof no OOM occurred. No crash dumps/profile contents inspected. Browser remains USER-owned; no guard bypass, page/CDP probe, reload, save replay, new container, browser reset or paid run was used for diagnosis.

Shortened `app/runner-prompt.md` from 651 to **526 words** and added seven explicit RR-driven configuration sections: event details; website/branding/widgets; registration types; admission/optional items; pricing/fees/discounts; registration paths/questions; vouchers/advanced rules. These are a coverage checklist, not a forced sequence or authorization to overwrite existing objects. All values/dependencies remain workbook-derived; report verified, preserved differences, blocked, untested and not-applicable per section. Native prompt composition, app guards and UI are unchanged. Added explicit stop for a crashed page, not another recovery recipe. **176 offline tests pass**, `logs/section-prompt-full-tests.log`; exact live crash mechanism and full acceptance remain unproven. The live policy getter reads this file for the next fresh approval; no app/browser restart or paid prompt is needed to apply this prompt-only update. Prior captured SOWs/history stay immutable. `logs/section-prompt-receipt.json` verifies **288 protected files unchanged**, USER ownership, unchanged **$30.122350** global cost, server PID **17768** (not restarted), zero paid prompts/Cvent mutations/browser operations. Source changes remain local/uncommitted.

## Prompt simplification — prior activation

User requested native Pi autonomy with the drag-and-drop UI flow preserved. Implemented one canonical `app/runner-prompt.md` task/SOW, captured via `RUN_POLICY.approvedSow` and sent once with the verified job envelope by `executionPrompt()`. Removed appended runtime policy and overlapping workflow paragraph/JSON scope; native Pi tools/system prompt/compaction and pinned Ego skill remain. Development context discovery still disabled. No prescribed route-plan schema, forced write ordering or one-retry quota. Existing-item preservation, approved target/scope, human-only authentication, API-first routing, ownership/Stop, uncertainty, budget and saved verification remain explicit. App guards remain separate, not a complete shell/browser-object sandbox.

Ordinary selector/stale-ref errors are recoverable and no longer count toward global Stop. Three recognized renderer/CDP/unresponsive failures within ten minutes still stop; unconfirmed execution immediately stops with durable uncertainty. No UI changes, new paid execution, browser/profile reset or Cvent mutation in this slice. Underlying browser hang and full RR acceptance remain unresolved; no automatic retry. **175 offline tests pass**, `logs/prompt-simplification-full-tests.log`. Idle app-only activation **27024 → 17768**, port 8788; receipt `logs/prompt-simplification-activation-2026-09-20T16-53-46-667Z/receipt.json` confirms **288 non-profile files unchanged**, USER ownership, unchanged global cost **$30.122350**. No new native Pi session/job or paid prompt. Reverify PID before future maintenance. See latest `RPC-CONNECTION.md` for details. All source changes remain local/uncommitted.

## Browser failure guard — historical (selector quota superseded above)

User approved stopping/detecting the inefficient run and implementing fixes. Stopped `ca3dfacc-a81a-47c0-8b8c-53152c1d32db` safely: **$5.594820** run / **$29.022338** event cumulative; no native Stop failures or unreconciled spending. Its browser eventually became unresponsive after selector failures; a large dependency ledger repeated the same blocker over 1,000 times. No full acceptance/final report; do not assume no browser changes because API receipts were reads. Saved-state/browser reliability review remains necessary.

Implemented `app/browser-failure-guard.mjs` and native event integration: Stop after **3 recognized failed browser tool calls in 10 minutes**, not reset by successful snapshots. Recognized unconfirmed page execution stops immediately with durable unresolved tool identity, blocking later event execution until reconciliation. Payload-free fixed metadata only; not a semantic-progress detector, arbitrary-shell sandbox or general hang repair. Updated instructions for observed selectors, bounded retry, python3 and grouped blockers without discarding actual requirements. No auto-retry or successor.

**173 offline tests pass** (`logs/browser-failure-full-tests.log`). Recorded-error replay would trip at 04:15:58.835Z (`logs/browser-failure-diagnosis.json`). App-only restart **11071 → 27024** on **8788**, receipt `logs/browser-failure-activation-20260920T042415Z/receipt.json`: **288 non-profile data files unchanged**, USER ownership, unchanged post-Stop global cost **$30.122350**. Native Pi gone; no new build/paid prompt/browser operation/Cvent mutation during implementation. Prior stopped records not rewritten; current browser hang has not been live-repaired. Fresh authorized acceptance only after review, never resume/replay. Changes remain local/uncommitted; inspect Git before staging. Snapshots below are historical.

## Upload preservation correction

User reported upload clearing all input and appearing unsaved. Actual latest uploaded workbook was present with matching checksum and a working 20-sheet preview. Fixed automatic upload to retain the entered target and prior workbook/edits until replacement save succeeds; show persistent `Saved: filename`, retain selection on failure. Only explicit CLEAR / NEW RR blanks the form. Completed Stop cannot be undone. **165 offline tests pass** (`logs/upload-preservation-full-tests.log`); real rendered-browser tests against an isolated temporary server/synthetic workbook covered upload, reload, failure and explicit Clear. Test browser/server closed; assigned Cvent browser untouched. Static assets are served immediately, no app restart; PID remains **11071**, USER ownership, no active jobs. Receipt: `logs/upload-preservation-receipt.json`. No paid prompt/build/Cvent mutation or change to the user's saved workbook. These are local uncommitted changes; earlier upload-clearing semantics below are superseded.

## Subsequent Clear / New RR activation

User requested a `/new`-like button beneath the Excel upload and automatic fresh context on another Excel selection. Implemented **CLEAR / NEW RR** and guarded `/api/new-rr`: Stop/release first, no new Pi prompt/process, then clear UI selection/form/preview. Active runs/unsaved edits require confirmation; cleanup uncertainty blocks successful clearing; existing history, originals, receipts, profiles, security attestations and cumulative spending remain. Old transcripts are not imported into the next empty session. Reload alone still restores the selection; Clear removes it. See current `RPC-CONNECTION.md` and `README.md`.

**164 offline tests pass** (`logs/new-rr-full-tests.log`). Verified idle app-only restart **6009 → 11071** on **8788**; `logs/new-rr-activation-20260920T034958Z/receipt.json`: **253 non-profile data files unchanged**, USER ownership, **$24.527530** global recorded cost unchanged, new button/JS served, empty idle reset returns success with AI not started. No browser/profile reset, build, paid prompt or live Cvent mutation. Actual active-run live acceptance remains unproven; user still supplies approved RR/target and explicitly starts. Changes remain local/uncommitted atop the private checkpoint; inspect Git. Do not execute the prohibited mock. Snapshots below are historical.

## Subsequent Stop/status activation

At the user's request, implemented bounded Stop/shared-ownership and status fixes before their manually started monitored test. **151 offline tests pass** (`logs/stop-status-full-tests.log`). Job Stop now revokes matching shared ownership; cancelled handoffs cannot regrant control; Stop remains available during pending handoff; stale setup messages clear; bounded payload-free timestamped activity and honest review-required labels replace misleading status. Full live lifecycle/RR acceptance remains unproven.

App-only restart **66266 → 6009**, port **8788**, activated these fixes and the prior privacy guards. `logs/stop-status-activation-20260920T033755Z/receipt.json` verifies **253 non-profile data files unchanged**, USER ownership, unchanged global recorded Pi cost **$24.527530**, updated UI served and raw progress HTTP denial. No browser/profile restart or inspection, paid prompt, Cvent mutation or new build. No new run was authorized or launched by maintenance; user will supply the RR/target in the UI and explicitly start. These changes are local; inspect Git for commit/push status. Historical snapshots below are not current activation state.

## Subsequent source-checkpoint update

The user subsequently approved creating a private GitHub repository to preserve the work: https://github.com/bpickett2019/cvent-ego-runner. The source checkpoint includes the bounded privacy slice (140 offline tests), complete runtime-tree Git exclusions, payload-free SSE and blocked raw-progress downloads. Private runtime artifacts remain local. Those source changes were initially not activated; the subsequent verified app-only restart above activated them. The earlier no-remote/no-commit statements below describe the historical handoff, not the later checkpoint; use Git/GitHub to verify current commit and push status. Deadline, users/concurrency, deployment scope and live acceptance approval remain unconfirmed. See `README.md` and the newest `RPC-CONNECTION.md` section.

## User request / next-session mandate

Review the entire project and recent changes, identify real MVP release blockers, then continue finalizing it for people to use and eventually push it to a repository. Time is limited. Do not confuse passing local tests, API reads, or a running dashboard with a production-ready end-to-end builder. Freeze cosmetic work; prioritize safety, accurate capability/status reporting, required coverage, reproducible deployment and real acceptance.

The previous assistant recommended a supervised single-operator pilot before unattended/team deployment. **The user has not yet confirmed that reduced release scope, the deadline, number of users, deployment host, Git remote or repository visibility.** Clarify these after presenting a short evidence-based critical path. Do not silently call a local pilot a multi-user production release.

Work in `/Users/bailey/cvent-ego-runner`. Read applicable AGENTS instructions. No workers or paid acceptance runs are in flight as of this handoff. Keep work bounded; do not autonomously consume an indefinite backlog or launch nested agents. Review before modifying, preserve the current baseline, and do not push private artifacts or credentials.

## Verified handoff snapshot

- App: `http://127.0.0.1:8788/`, PID **66266**, recorded in `data/rr-connection.lock`. Recheck; PIDs are historical observations, not authorization to kill arbitrary processes.
- No active jobs and no `*operation.lock` under `data` at handoff.
- Assigned browser ownership **USER**, runtime ID/job `88758a4b-46bf-4bf8-a80a-b422ddbf0b22`.
- Steel session `d81f7788-5fe1-4890-aac4-87ecf534fb82`, target `66C3818336AF4742191C21B550981A22`. Reverify identities rather than adopting another tab.
- A security-acknowledgment field is now present in the runtime. Older documents saying the incident is necessarily still unresolved are stale; examine the explicit acknowledgment evidence before making any claim. Never fabricate/clear incident attestation. A fresh browser does not revoke an old session.
- Last run `88758a4b-46bf-4bf8-a80a-b422ddbf0b22`, requested target **(C+D) Medtrade Testing Clone 2**, stopped on explicit user instruction: `STOPPED_REQUIRES_REVIEW`, **$5.682544** run cost, **$23.427518** target-attributed cumulative cost, no Stop failures or unreconciled spending.
- Global recorded spending also includes another event, so global and target totals differ. Do not erase or reset either.
- Git branch `main`, **no remote configured**. Status reported 19 untracked entries and 2 other changes. Much of the project predates this handoff as untracked work. No destructive Git cleanup or blanket `git add -A`; inspect source-only changes and artifact exclusions.
- Latest full suite: **138 passing**, `logs/api-item-writes-full-tests.log`.
- Latest activation: `logs/api-item-writes-activation-20260920T015938Z/receipt.json`; 135 preservation hashes, zero implementation paid prompts or live Cvent mutations.
- Restart history for transparency: 55122 → 65715 loaded the update, but verification found ownership still AGENT after Stop. With no active processes/jobs, explicit Take Control restored USER; preserved restart 65715 → 66266 then passed. Neither browser/profile was restarted or logged out. Review Stop/ownership semantics for MVP; do not hide this operational distinction.

## Intended product and fixed authorization

Upload RR + explicitly name existing target → START BUILD → genuinely clean local Steel browser → human login → RETURN TO AGENT → deterministic safety/identity/budget checks → one fresh native Pi executes the permitted RR.

- **One native Pi**, original tools and pinned Ego skill; no second intake agent, replacement framework, saved-transcript restoration or replay browser automation.
- Upload-bound `requestedEventName` is authoritative, even when the workbook has a different event name. Never rename or switch the target.
- **Create-only preservation remains in force.** Reuse existing/default/prior-run items unchanged. Confirm absence with a complete scoped lookup before creating. Report existing differences as unresolved, not satisfied. Never duplicate/replace/disable to evade preservation. Follow-on initial configuration of an object newly created inside the same guarded API command is narrowly allowed; later calls cannot edit that existing object.
- This scope cannot promise complete RR compliance when existing settings differ. Changing the policy requires explicit user authorization, not an API-routing fix.
- Skip ordinary ambiguous/missing values and dependent work, record omissions, continue independent requirements. Pause for authorization, security, identity, ownership, uncertainty, budget or human login. Never invent financial/application values.
- No deletion/archival, publishing, communications, attendee access, session/speaker configuration, other events, account-global or merchant/financial-account setup, or integration identifier changes.
- Authentication and session-revocation attestation are human-only. Never inspect cookies/storage, hidden/password/token inputs, page source or raw credential-bearing network content.
- Local Steel only. Do not touch unrelated browser services or use Steel Cloud.
- Preserve originals, workbook versions, profiles, reports, costs and uncertain-write evidence. Stopped runs require a fresh upload/native session; no automatic resume/replay.
- Budget: $60 cumulative allowance with $10 external reserve, effectively $50 AI allowance including prior target-attributed spending. Not a promise that the whole RR completes below $60.
- Coding/test authorization is **not** authorization for a paid RR acceptance run or live Cvent mutations. Obtain bounded explicit approval for those and any permanent new test items.

## Read these first

1. `RPC-CONNECTION.md` — newest sections first; old sections describe superseded behavior.
2. `CLEAN-BUILD-PLAN.md` — implemented login-first lifecycle and initial activation evidence.
3. `CVENT-API.md` and `CVENT-API-COVERAGE.md` — current writes, limitations, official routes and historical audits.
4. `E2E-ACCEPTANCE.md` — acceptance framework, **some rows/statuses are stale** (see corrections below).
5. `BROWSER-SESSION.md` and `.pi/skills/ego-browser/references/steel-bridge.md` — distinguish historical shared profile from current per-build provisioning.
6. `package.json`, deployment/configuration files, `.gitignore`, `app/`, `ego-bridge/`, `public/`, and tests. Inspect actual working-tree/untracked changes; documentation is not a substitute for code review.

Do not dump native transcripts or broad JSON datasets. Historical traces include exposed authentication material. Use narrow, redacted evidence and metadata; never put raw tool output/private reasoning in a user activity feed.

## Implemented baseline

### Login-first clean browser

- `app/clean-browser.mjs`: owner-only empty per-job profile, uniquely named Docker container, pinned existing local Steel image, dynamic loopback API/CDP ports, bounded non-model readiness and identity checks. No old-cookie copying, old-profile fallback or destruction.
- Image pinned to `sha256:21cf2a5785aa9478d0f7933c04bce96ca79f3d7a93d9824ea184800d29d3cd02`.
- `app/rr-connection.mjs`: production upload stores only; Start provisions/waits with **zero Pi launch/RPC/prompt**. Verified Return starts one empty native session and one execution prompt. Rechecks assigned browser after preparation, handles duplicate Return/Stop/provision races and blocks legacy prelaunch execution paths. Legacy fixtures still exercise old lifecycle paths; distinguish them from production wiring.
- `app/server.mjs`: setup, API/browser identity, incident acknowledgment, history/budget gates, assigned runtime viewer proxy.
- `ego-bridge/host.mjs`: BOOTSTRAPPING only navigates the assigned fresh blank page to the neutral Cvent entry URL. It does not type/login or authorize configuration.
- Real isolated provisioning + Ego-bootstrap probe passed without AI/authentication actions or touching the assigned browser: `logs/clean-browser-bootstrap-probe-03b59f62-09b5-47d4-837a-cc879de249ff/receipt.json`. Owned probe containers were stopped, not deleted; profiles/evidence retained.
- Old per-build containers/profiles are retained; retirement/resource limits are operator-managed, not a finished production lifecycle.

### API-first reads AND writes — latest change

The stopped run did perform eight successful API reads before browser work. It did not prove API-write coverage. Its `route-plan.json` explicitly blocked item-scoped discounts on a missing association integration. This was a real adapter gap, not proof that APIs were read-only.

Files: `app/cvent-api.mjs`, `app/cvent-api-cli.mjs`, `app/runner-prompt.md`, `test/item-discounts.test.mjs`, `test/discount-cli.test.mjs`, `test/runner.test.mjs`.

- `configureDiscount` already created missing final-total codes and preserved existing ones.
- It now accepts `agendaItems: [{id, type: "AdmissionItem" | "QuantityItem"}]` for **initial new-code configuration**. Resolves 1–100 unique item UUIDs against complete event-scoped catalogs; no session/attendee/registration-type-ID substitution.
- Creates inactive/final-total temporarily, verifies the new ID, links each explicitly requested item via documented PUT, polls authoritative complete association reads, then finalizes RR values with item application enabled and independently verifies all saved state.
- Every mutation has durable intent; the whole command retains one uncertainty marker until completion. The CLI permits follow-on PUTs only for the same-command verified new ID, rechecks Stop/ownership/target and marker ownership, and never exposes a standalone existing-code link/update grant.
- Existing codes/associations, including prior-call creations, remain immutable. Differences are `PRESERVED_DIFFERENCE`, not compliance. Failed/partial creations remain uncertain and must not be replayed, deleted or repaired automatically.
- Added `listQuantityItems` and `listDonationItems`; donation linking is not supported.
- Capability output now explains write support/limits. Legacy `updateEvent`, `updateEventBasics`, `updateRegistrationType`, `updateEventCustomFieldAnswers` remain blocked in production. Historical low-level helpers are not grants.
- Prompt requires separate per-requirement read/write routes and eligible independent API writes before unrelated browser exploration, with specific read-only UI dependencies allowed. This is a routing instruction, **not proof every future model run follows it**.
- **Volume discount writes remain unintegrated.** Arbitrary eligibility semantics are not implemented by item associations. Question-choice/feature-state read gaps and browser workflows still require review against the chosen MVP RR.
- 138 offline tests pass, including production CLI multi-step writes, preservation, scoped lookup, Stop/takeover, foreign uncertainty, stale readback and no replay. **No live item-discount creation acceptance has occurred.** Review official semantics, partial-failure handling and guard boundaries before claiming production certainty.

### UI/workbook/security baseline

- Forge-style RR editor and dashboard: `public/{index.html,app.js,styles.css}`.
- Workbook preview/edit/versioning: `app/workbooks.mjs`, `app/workbook-editor.py`; immutable originals, checksum provenance, formula read-only, archive/XML safety, stale-save and active-run write guards. No paid prompt merely to preview/edit.
- One ownership toggle: AGENT → TAKE CONTROL (Stop/handoff); USER → RETURN TO AGENT (only a live setup-waiting run). Not pause/resume of stopped jobs.
- `app/session-security.mjs`: durable explicit incident acknowledgment with read-only authenticated assigned-page verification; multiline DOM login detection regression fixed. Review broad password-string denial for false positives without weakening actual login/MFA/expiry checks.
- Production Pi uses `--no-context-files --append-system-prompt app/runtime-policy.md`, pinned Ego skill, low thinking. Preserve native execution, not a replacement agent architecture.

## Known MVP risks / unfinished work

1. **No accepted full RR.** API read success and mocked API writes are not saved-result evidence for all SOW areas.
2. **Preservation vs “full configuration.”** Existing mismatches are intentionally not changed. Define honest MVP completion/exception semantics with the user.
3. **Browser authoring and object preservation unproven.** Identity/ownership/uncertainty guards are not a complete object-level create-only sandbox. Direct shell and retained low-level adapters also exist. Review least privilege and practical supervised safeguards.
4. **Deployment is local, not validated team isolation.** No completed authentication/access-control/multi-user isolation/security review. User 2/3 were deliberately unavailable rather than falsely isolated. Decide local supervised pilot vs concurrent users before deployment design.
5. **Portability/package dependencies.** Existing API clients are imported from `~/cvent-agent` and `~/cvent-ui-automation`; credentials file and runtime paths are local. Pi provider/auth, Docker availability, pinned image, viewer/WebSocket routing, startup/recovery and secrets provisioning need reproducible installation/deployment checks.
6. **Status/activity accuracy.** Last run's stage label stayed “Inventory RR” after API and browser work; an old security message remained after successful handoff. Earlier monitoring based on those labels was corrected using actual receipts. Fix misleading status before users rely on it. Rich timestamped actions/tools/results/skips/blockers with scrollback was requested but not implemented. Use safe structured summaries, not raw transcripts or private reasoning.
7. **Costs/performance unproven.** Incremental all-sheet inventory/source ledger is instructed, not yet demonstrated to meet full-RR cost/latency targets. Prior spending reduces remaining budget. Do not restart paid attempts merely to test UI fixes.
8. **Stop/crash/uncertain-write recovery.** Review partial multi-step creation, stale locks, orphaned processes, ownership flags, no replay, disconnects and delayed readback. Do not silently clear a lock or uncertainty marker.
9. **Browser retirement/resources.** Clean profiles are real, but retained old containers can accumulate resource consumption. Preserve evidence/profiles; agree on a safe lifecycle rather than blanket cleanup.
10. **Stale documentation.** E2E-ACCEPTANCE still describes some old paid-intake ordering, unconnected catalogs/associations and unresolved incident status. Update it from current verified evidence; do not simply tick all gates.

## Recommended next steps (bounded, not an endless backlog)

1. Read-only audit of code, tests, security boundaries, actual receipts, Git changes and deployment assumptions. Reconfirm no new active job before touching shared state. Produce a short P0/P1 blocker list distinguishing proven, mocked and untested behavior.
2. Confirm deadline, intended users/concurrency, hosting, accepted create-only scope and target repository/visibility. Recommend supervised single-operator MVP if time is short, but obtain agreement.
3. Select the smallest release-blocking implementation slice and its tests. Avoid cosmetic redesigns or integrating unrelated APIs. Keep one writer and review resulting changes.
4. Once code is ready, request authorization for ONE bounded live acceptance using an approved RR and explicit unpublished target with useful permanent missing items. Obtain approval for spend and creations; never use the prohibited mock below. Prove saved state, preservation, no duplicates, Stop/uncertainty behavior, cost accounting and honest final exceptions.
5. Stage source/docs/tests only after checking ignores, secrets and sensitive fixtures/history. No remote exists yet: ask for the intended repository and visibility before configuring/pushing. Do not push data, workbooks, profiles, logs, credential files, native sessions or customer traces. Do not rewrite history or clean untracked work destructively.
6. Final handoff/runbook: startup/login/use/Stop, known limitations, supported scope, recovery and human signoff. Passing local tests alone is not go-live approval.

## Protected resources

- Never execute, modify or delete `/Users/bailey/Downloads/Medtrade_Testing_Clone_2_MOCK_ONLY_New_RR.xlsx`. It explicitly prohibits execution. SHA256 `bb381b810379e26b3accf27d93234197f4550c5bacbe4fff0dfc0384128b98e7`.
- Preserve original shared container `cvent-ego-runner-steel`, old profiles, all build artifacts and the reference screenshot `/Users/bailey/Downloads/screencapture-127-0-0-1-28877-2026-09-19-17_02_54.png`.
- The separate Steel skill runtime at port 4310 is not this app's runtime. Do not use or stop it to repair Cvent.
- Historical security-sensitive job `0cd488a1-0209-461d-a556-69328bfe0334` contains exposed authentication material in native traces. Never dump or publish it. Use sanitized incident evidence only.

This is a durable handoff for the user's `/new`; it is not a claim that the next session has already taken control or that any release/push/paid acceptance has occurred.
