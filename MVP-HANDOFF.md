# MVP handoff

## GitHub checkpoint and staging cutover blockers — CURRENT (2026-09-21)

User chose **Anthropic Sonnet 5**, will provide the team Key Vault secret **in the morning**, and explicitly authorized **GitHub push plus publication replacing the existing staging root dashboard**. Verify the exact provider model ID/availability when credentials arrive; do not invent an ID, claim availability from a display name or copy personal OAuth. Paid builds are authorized as a future capability, not an automatic trial; no key exists on the new hosts yet. Existing three local previews stay available/AI-disabled.

**Staging cutover is NOT performed.** Read-only inspection of the existing Azure app's actual process environment and control database found **six durable `login_required` jobs**, zero worker/event leases (including unexpired leases), and no direct children of the main app process. These are waiting jobs, not evidence of six paid workers running. Do not discard/rewrite their records, stop resources or clear uncertain outcomes automatically. The current app's restricted-access policy is enabled but **expired at 2026-09-21T04:14:27Z**; inspection occurred at 11:37 UTC. Existing Caddy Basic authentication alone does not replace this application-level expiry. Do not bypass/remove the expiry or silently turn temporary access into indefinite access. Ask approval to settle the waiting jobs with all files/evidence retained and renew restricted staging access (proposed 24 hours), then perform the protected root cutover.

Sanitized evidence: `logs/azure-staging-ui/{idle-auth-policy,job-lease-preflight}.log`. No secret values read, public login bypass, IAM/network/proxy/DNS/service change, cloud activation or paid session. Source checkpoint/push authorized to private `bpickett2019/cvent-ego-runner` main; Git receipt belongs under `logs/azure-staging-ui/`, not tracked runtime data. **347 offline test evidence reused** (runtime unchanged since the passing UI test); **21 deployment tests pass** again. Preserve intentional vendor patch; no jobs/profiles/workbooks/costs/credentials in Git. Earlier preflight/cutover details follow.

## Existing staging domain integration — prior (2026-09-21)

User now requests deploying the three-workspace setup into Azure using `https://staging.app-chartsdarts-dashboard.com/`. **Read-only discovery completed; no source deployment, activation, proxy/DNS/network/auth change or service restart performed.** The domain resolves to existing `cvent-pi-dev-vm` (`57.154.50.217`), not the three new dedicated VMs. Existing Caddy serves HTTPS, with one HTTP Basic account and additional `/pi`/proxy routing; current upstream is `127.0.0.1:8877`, service `cvent-one-shot.service`, user `cvent`, working directory `/opt/cvent-one-shot/current`. Do not print/copy its auth hash, environment or credentials. Sanitized Azure Run Command evidence: `logs/azure-staging-ui/existing-routing.json.log`; initial broader preflight output was truncated, so use the bounded routing summary. Browser navigation encountered `ERR_INVALID_AUTH_CREDENTIALS`; do not bypass the web login or infer rendered UI acceptance. The existing web login needs human completion for final browser verification.

**Cutover authorized:** user explicitly chose **replace the existing dashboard at `/`**, then additionally requested **enable paid AI**. Preserve current HTTPS/authentication; no public unauthenticated app/viewer/CDP ports. No cutover or paid activation has happened yet. Asked which team-funded provider/model and, if present, Key Vault/secret name to use (never paste secret values). This is authorization to enable paid execution after safe configuration, not to automatically start a paid trial or copy personal Pi OAuth. Do not retain the earlier paid-disabled scope as the final target; previews remain disabled until credentials/controls are ready. Existing single shared staging login is not per-user authorization. The local loopback-only workspace config is not a public-domain gateway/auth implementation. The three dedicated VMs remain staged/disabled; safely routing them through the existing protected hostname still needs implementation/verification. Paid activation is now requested but blocked on team credential/provider/model selection plus event-collision/shared-quota safeguards. Existing vault metadata confirms `kvcventstg729` exists (access-policy mode); no secret values retrieved. Do not grant/escalate IAM to obtain access.

Read-only existing-app shape: release `9f487c18395cfdd1352c5863b403e777c2b89c21`, Python app with `/api/status`, `/api/jobs`, `/api/admin/jobs`, `/healthz` and Key Vault launcher source. Main PID had no direct children; this alone is NOT verified idle-job evidence. Inspect sanitized durable job status before root cutover. Evidence `logs/azure-staging-ui/existing-app-shape.log`. No gateway/routing code or deployment mutation yet. Existing app uses an AMD64 D8as VM; keep exact ARM64 Steel on the three dedicated ARM VMs rather than swapping pins.

Local previews remain RUNNING, USER-owned and $0 at ports **50825/50828/50831**, supervisor **31501**, handed to user in desktop Ego space **53**. Original PID **4846** dashboard/8788 remains idle, browser stopped, accounting **$103.476558**. Leave all these alone. No commit/push or paid build. The user deliberately stopped the last RR; do not diagnose/replay it as a new failure. Prior local interaction evidence and exact resource identities follow below.

## Clickable three-user local workspaces — prior (2026-09-21)

**User correction:** the last RR was intentionally stopped by the user. Do not treat its terminal INCOMPLETE report/readback note as a new failure to diagnose or as authorization to retry. Retain original records/costs unchanged. Current request is to **click around User 1, User 2 and User 3 workspaces**, not just receive automated test results.

**Three interactive workspaces are RUNNING and handed to the user:** User 1 `http://127.0.0.1:50825`, User 2 `http://127.0.0.1:50828`, User 3 `http://127.0.0.1:50831`. Each has its own app/data/HOME/upload/job/Steel browser/profile/session/ports. Sidebar USER 1/2/3 buttons now navigate between these actual local instances; labels reflect the selected workspace and its active job controls load on a fresh tab. Global desktop Ego space **53**, page p1, is handed to the user on User 1's browser viewer. It is dashboard-only; project Steel browsers remain USER-owned. No automated login, Return or Cvent authoring. Placeholder lifecycle workbooks are not build RRs. **Model execution disabled**, credentials not copied; no paid sessions/cost.

Supervisor **PID 31501**, command `node scripts/validate-three-local.mjs --interactive`, evidence `logs/local-three-KamoWi/receipt.json`; dispatcher `logs/local-workspace-ui/interactive-dispatch.json`, UI handoff `logs/local-workspace-ui/handoff.json`. Leave these instances/browser profiles alone while the user tests. Do not launch duplicates, reuse old stopped test profiles, or stop the original dashboard PID 4846/8788. For a separately requested shutdown, verify supervisor identity/evidence before signaling its exact PID; graceful shutdown stops its own app/browser jobs and preserves profiles/evidence. Stopping a build in one UI does not shut down the other workspaces. These previews share one OS user/dependencies and are not authenticated multi-tenant deployments.

New `public/workspace-config.js` strictly validates opt-in, distinct loopback-only workspace links/current origin; `public/app.js`/`styles.css` enable labelled switching (including mobile) only when the local launcher generates `public/local-workspaces.json` in private source copies. The usual single-user dashboard has no such config and keeps its original navigation. These are UI changes, not a backend execution model change/restart. Source-only deployment bundling includes the new JS automatically; Azure was not refreshed/activated.

**347 offline tests PASS**; fresh real lifecycle run/cleanup PASS `logs/local-three-I4wex5/receipt.json`; interactive browser click-through **1 → 2 → 3 → 1** verified, rendered Cvent login viewers/screenshots for User 1 and 3 plus User 2 canvas/viewer observed. Evidence `logs/local-workspace-ui/`. No claim of authenticated Cvent editing/full RR/Draft completion, shared quota/event exclusion or hostile-user security. Original dashboard idle/accounting unchanged at launch. Azure unchanged, apps still disabled and VMs still incur hosting charges. No commit/push. Next: let the user exercise these live previews and report concrete UI issues; do not automatically diagnose/retry their intentionally stopped RR or consume another paid run.

## Local three-instance validation — prior (2026-09-21)

User requested stopping the current run and validating locally, including three users, before further Azure work. Read-only inspection found job `4a0f4127-9741-4f88-82c8-82b59163cd2d` already **INCOMPLETE/SETTLED**, actual native cost **$23.176562**, reconciled, browser STOPPED and owned Pi process gone. Final report records a saved readback mismatch: Internal registration path had ATTGUE associated but SPONCOMP still unassociated. No retry, mutation, uncertainty clearance or historical reconciliation performed. Current aggregate charge **$103.476558**; $60 is only a target, neither a demonstrated full-build price nor an enforced cap.

**Real three-instance local lifecycle PASS**, evidence `logs/local-three-JtGQC6/receipt.json`. Added opt-in `scripts/validate-three-local.mjs`: three source-only app copies, separate HOME/data/uploads/jobs, no credentials or global Pi state, shared installed dependencies, three real fresh Steel containers concurrently. Verified distinct session/target/profile/loopback ports/Ego ledgers; foreign job reads/Stop rejected; foreign Origin rejected; viewer HTML proxy routes present. Stopping user 1 left users 2/3 running and waiting for human login. All three test apps/containers stopped and profiles/evidence retained; **zero Pi sessions/model cost**. Original PID 4846 dashboard/runtime/jobs/accounting/lock unchanged. No Return-to-Agent or Cvent authoring.

**337 offline tests pass**, including new three-controller mocked native-session/Stop/accounting isolation test in `test/login-first.test.mjs`; full log alongside receipt. Runtime source unchanged; no original app restart, paid trial, credentials, Git commit/push or Azure action. Existing three Azure apps remain staged/disabled; VMs continue prior charges. Earlier source refresh remains valid for runtime source.

**Not all functionality accepted:** local copies share one OS identity and dependency directories, so this is not hostile-user/access isolation. Human Cvent login, interactive viewer stream/input, three actual concurrent native builds, event-collision exclusion/shared quotas, full saved relationships and unpublished Draft RR completion remain unproven. Do not call this three-user production readiness or replay stopped jobs. Superseded next step: the user clarified this run was deliberately stopped and wants clickable workspaces instead of failure diagnosis. A fresh live RR trial still needs explicit authorization/target and human login. Continue local proof before further Azure rollout. See `E2E-ACCEPTANCE.md` and `docs/NEXT-SESSION-HANDOFF.md`.

## Three-user exact-source staging — prior (2026-09-21)

User interrupted monitoring and requested **“this exact setup so it can run 3 users.”** Three existing VMs `cvent-ego-1/2/3` in `rg-chartdarts-stg` were reverified unused/disabled, then refreshed to the same current native-Pi launcher, **318-word task**, standing SOW and Ego/Steel/API source. **Source staged and verified, NOT activated or team-ready.** The local run `4a0f4127-9741-4f88-82c8-82b59163cd2d` (CGJWS 2026 workbook, selected Medtrade clone) was still executing at preflight; it was not interrupted, steered, restarted or otherwise modified. Latest preceding monitoring sample was $16.892072; this is not a final cost/result. Local source/runtime/vendor/credentials and other Azure services remain untouched by deployment.

New `deploy/azure/refresh-stage.py` supports only unused/disabled/unconfigured staged VMs with unchanged dependencies. Reviewed SHA archive, path/hash/dependency checks, two pre-mutation idle checks, protected old-source backups, exclusive intent, post-source/dependency/data verification; no installer replay, activation, automatic retry or rollback. Added missing `bin/rr-evidence` to source packaging. **21 deployment tests pass**; all three VMs passed **18 dependency checks**, exact source hashes, unchanged data/vendor/image, disabled service/no credentials/jobs/containers, and read-only native-pi policy/SOW plus workbook-helper checks. Existing 334 runtime / 74 focused evidence reused; no live three-session/RR acceptance claimed.

Snapshot SHA256 **c4085c0457ab458bc8f324f09139cafd6c996682ffe84159afe05e269bc8b302**, 59 archive entries, **15 changed source files per VM**. Receipts: `logs/azure-native-pi/`; remote backups/intents: `/var/lib/cvent-ego-source-refresh/<sha>/`. Never replay `logs/azure-native-pi/refresh-approved-three.py`, old bootstrap/stage scripts, or clear intents. Initial read-only SSH attempt lacked the administrator key; matched the existing staged public key and explicitly selected its private-key path without reading/copying it. Strict pinned host verification retained; network rules unchanged (admin SSH only).

**Next bounded work:** restricted per-user tunnel access; enforce disjoint immutable event assignments or central leases including local/other executors; shared Cvent/model concurrency/quota policy. Three users' identities/public keys/source networks and event assignments are not yet specified. Team-funded model credential/provider/model remain deferred until the user supplies them at the end; do not copy personal OAuth or ask prematurely. Then securely configure credentials, explicitly activate and prove three-session isolation/Stop/cleanup and saved Draft results. No new VMs, IAM/network expansion, credentials transferred, paid prompts, browsers, Git commit/push or source activation in this leaf. VMs continue accruing the prior estimated $0.42/hour compute plus extras. See `deploy/azure/README.md`.

## Native-Pi simplification — prior local activation (2026-09-21)

User explicitly directed: **“Let pi be pi.”** Implemented a separate working-tree simplification after private `main` checkpoint **34ee7cc**, then **activated by a separately authorized idle restart: 97787 → 4846** on port 8788. Live `/api/runtime` reports `executionPolicyId: native-pi`; USER ownership, browser stopped, no active jobs. All **1,066 checked non-profile data files**, accounting **$80.299996**, environment and selected-event identity preserved; served assets match. Receipt: `logs/native-pi-activation-ddjmhzzs/receipt.json`. Do not replay `logs/activate-native-pi.py`, its `.started` marker, or any previous restart script. Restart permission consumed. No paid run, live Cvent action, Azure change, commit/push or historical reconciliation. Full RR execution remains unproven.

- `app/native-task.md` is the new **318-word task**; `app/standing-sow.md` is the standing event-build scope distilled from the supplied SOW plus later delivery changes. New launcher captures them separately as each job's `runner-prompt.md` / `approved-sow.md`; the sole native prompt references the scope file and workbook. No full historical delivery roadmap, old tasks or extra prompt stage is injected.
- Removed forced API-first routing, Site Designer-last/one-object editor sequence, model time target and preferred Save-helper prescription from active task/guides. Pi chooses plan, tool order and ordinary recovery; normal Ego Page actions and waits remain available. Ego/Steel is still the browser route, supported API tools remain available. The upstream skill, native Pi system/tools/RPC launch, bridge/API enforcement, cost/Stop/uncertainty handling are unchanged.
- New completion report uses `completion: {requirements:true,draft:true}` with eventId, DONE/INCOMPLETE and blockers/untested arrays: no website/registration category checklist. Saved verification and truthful completion remain required. Legacy four-flag reports retain their interpretation; uncertainty, locks, malformed/mismatched reports and cleanup failures still cannot become DONE.
- `app/runner-prompt.md` is deliberately unchanged from 34ee7cc. Retaining it avoided partially activating new instructions through the old module's dynamic getter before restart. The now-active launcher reads **native-task.md**, not this frozen legacy task.
- After activation, the user clarified **skip prohibited/blocked requirements and note them at the end, rather than stopping all work**. The task now explicitly leaves those requirements untouched, skips their dependents and continues independent work; a confirmed pre-dispatch denial is local, not write uncertainty. Actual operator Stop, loss of login/control/identity, crash/disconnect, exposed secrets and possibly executed/uncertain writes still stop. No guard code changed: ordinary prohibited-action denials already do not trip the controller. **74 focused tests pass**, `logs/native-pi-simplification/local-blockers.log`. This prompt-only clarification is loaded by the existing getter for the next fresh job, without another restart; the immutable activation receipt predates this 318-word revision.

**334 full offline tests pass**; legacy prompt-string tests were consolidated into the new task/scope contracts (the lower count is not skipped tests). Final focused path/capture checks and logs live in `logs/native-pi-simplification/`; whitespace passes. Full end-to-end RR acceptance remains unproven. This is a workflow simplification, **not a repair of the prior Save identity failure** or added iframe/asset capability; missing RR inputs remain real blockers. Next separately authorize a fresh paid RR trial/human login; the simplified backend is already active. Do not replay stopped/uncertain operations or alter Azure.

## Pre-simplification Git checkpoint — prior (2026-09-21)

User explicitly requested a Git commit/push of the current implementation before testing a simpler single-Pi workflow. The standing event-build scope is the user's `/Users/bailey/Downloads/Finalized - EmeraldX_Timeline_Roadmap_v3.docx`, subject to the later agreed delivery exclusions below. Each RR supplies requirements, not scope overrides. Retain Ego/Steel; one native Pi chooses the plan and executes/verifies all applicable in-SOW requirements. No deletion or publishing, no extra planner/reviewer, deterministic importer or broad redesign. Simplification is **not implemented in this checkpoint**; the 448-word runtime prompt and live source are unchanged.

The explicitly authorized converted-workbook trial `e4d4b2bb-3489-4cb9-ac8d-aed000c1fb02` is now **INCOMPLETE/SETTLED**, native cost **$7.956288**, aggregate tracked spending **$80.299996**. Exact upload SHA-256 `d22ef277d1c93a9d89ca1b81d2f041fdad8d731330e341e42867d2acc277c31d`; 14 sheets, no OOXML media/drawing parts, not the previous original BDNY source. Human Return successfully launched one fresh native session after the dashboard's active job was explicitly selected. A newly opened dashboard without a selected run showed a disabled Return button; selecting the active job enabled it. No launcher fix yet.

Native final report records **no verified creations or saved modifications**. EMPP's checkbox edit reached a `saveOnce` failure: `Save observation lost assigned event/page`, before any confirmed Save dispatch/completion. Persistence and final Draft remain unverified. No current-job browser/API uncertainty marker was found; the agent stopped and settled normally as INCOMPLETE, not a demonstrated controller-marker stop. The exact identity-check failure still needs bounded diagnosis; do not infer drift or repair by removing event isolation. Preserve the edited run/evidence; no replay or historical reconciliation authorized by this checkpoint. Source also has unconfirmed conversion defaults, missing pricing/dates/assets, unresolved code/path mappings and missing FT42 specifications; simplification cannot invent them. Private final reports remain under this job's `reports/` and are not committed.

Read-only checkpoint: USER ownership, assigned browser stopped, no active jobs or unreconciled spending; original app restart permission remains consumed. Azure remains untouched/disabled with older source. **344 offline runtime tests and 12 deployment tests pass** again at checkpoint, with logs in private `logs/git-checkpoint/`; shell syntax and whitespace pass. These are not full RR acceptance. Git destination verified as private `bpickett2019/cvent-ego-runner`; source-only checkpoint excludes runtime trees, workbooks, SOW binary, credentials and private receipts. Vendor pin remains unchanged; its intentional working-tree modification exactly matches the already tracked session-ledger patch.

**Next:** after checkpoint push, separately make the smallest simplification grounded in the actual Save preflight failure and launcher friction; make the standing SOW scope available without importing obsolete delivery requirements or adding prompt layers. Preserve all historical evidence, no-delete/no-publish, selected-event isolation, human login, Stop and no uncertain replay. Recheck runtime before edits; do not restart or auto-run another paid workbook as part of the checkpoint. See `docs/NEXT-SESSION-HANDOFF.md` for remaining context.

## Narrow Save/controller repair — prior activation (2026-09-21)

User approved the single-Pi recommendation and narrow repair, then explicitly authorized the idle local app restart and requested a `/new` handoff. **Implemented, offline-tested and locally activated; live RR completion remains unproven.** Restart verified **44417 → 81487** at `http://127.0.0.1:8788`, USER ownership and browser stopped. Latest job `42437509-fce2-4b0a-8ea9-4bfd97da0e76` remains INCOMPLETE/SETTLED; no active/unsettled jobs or owned native process at activation. Exact launch environment preserved in memory only. Accounting unchanged (charged **$72.343708**, no reset), all **1,003 checked non-profile data files** unchanged, selected target/browser identity and served assets match. Receipt `logs/save-transaction-activation-ym1exljz/receipt.json`; one-time script `logs/activate-save-transaction-fix.py` and exclusive `logs/save-transaction-activation.started` **must not be replayed/cleared**. No paid prompt, browser launch, Cvent action, historical-marker change or Azure change. Source hashes bind the restarted app to this repair. Recheck current state before any subsequent action; this restart permission is consumed.

- `ego-bridge/save-observer.mjs` / `cli.mjs`: new project-global `saveOnce(page, saveRef, options)` prevalidates options, waits for a grounded visible Save locator, rejects pending/prior/conflicting outcome states, rechecks ownership/identity, and clicks **once**. Dispatch exceptions remain uncertain with no retry. Existing `observeSave` remains available immediately after an already-clicked Save, never as permission to click again.
- Observation retries only its fixed read-only DOM evaluation for destroyed/missing execution context or not-ready document, within the original bounded deadline. Control is checked before and after reads. Control loss, crashes/disconnects, operation timeouts/possible late effects, selector/unknown errors remain terminal; control checks themselves are not retried. Initial post-Save runtime-read failures now persist uncertainty too. Known transitions are a narrowly supported possibility, **not proof of the original incident's exception**.
- Uncertainty retains phase, attempts/successful polls, transient count/last diagnostic, last observation and fixed cause category/type/fingerprint, without raw exception messages. Exclusive private markers remain immutable. No refresh/navigation, Save replay or historical reconciliation.
- `app/browser-failure-guard.mjs` / `rr-connection.mjs`: current-job marker presence is authoritative regardless of shell `isError`, checked at tool boundaries, the two-second monitor, asynchronous settlement and final cleanup. Marker inspection does not parse contents/follow symlinks; inspection errors fail closed. Successful quoted stdout alone is not an authority. Stops retain uncertainty, reconcile native costs and clean up the assigned browser without retry/DONE.
- Linked guidance documents the helper and independent exact-value/relationship/default verification obligations. **The same Pi still chooses its plan and performs independent supported readback; the helper does not automatically verify saved API state or schedule a 10-second persistence read.** Completion/rejection remain `verified:false`. One native Pi per build, no added planner/reviewer/model session; prompt unchanged at **448 words**.

**82 focused and 344 full offline tests pass**, `logs/save-transaction-fix/{focused-final,full-final}.log`; syntax and `git diff --check` pass. Coverage includes transient DOM/context errors, persistent transitions, deadline overrun, control loss, late-effect/timeout errors, sanitized diagnostics, preflight/no-click, one-dispatch behavior, initial runtime failure, and successful mixed-shell events plus monitor/settlement/cleanup races. The first full run had one test expectation typo, corrected; its log is retained. These are mocked DOM/Page and controller fixtures, **not a real Steel/CDP navigation test, live Cvent Save acceptance or full RR acceptance**.

Preserved dirty work, historical markers/evidence/accounting and the selected event; no commit/push or paid build. Azure stays disabled with the older snapshot; no credential/deployment changes. Source/asset/pricing ambiguities and readback/default/relationship acceptance remain unresolved. **Next: use `docs/NEXT-SESSION-HANDOFF.md` for the requested `/new` session. Keep the app a thin upload/selected-event launcher for one native Pi, not a deterministic importer or multi-agent pipeline. Read-only preflight first; separately authorize bounded live validation/fresh RR execution. Do not repeat activation, auto-retry old runs or promise end-to-end success from offline evidence.**

## Latest RR stop investigation — prior diagnosis (2026-09-21)

User requested log diagnosis and a long-term fix for the completed/remaining list. **Investigation only, no production fix implemented.** See `docs/RUN-42437509-STOP-REVIEW.md`; offline diagnostic receipt `logs/run-42437509-review/offline-reproduction.json`. No live browser/Cvent requests, mutation/replay/marker clearance, runtime change/restart, paid session or Azure change.

Run `42437509-fce2-4b0a-8ea9-4bfd97da0e76` ran **19m15.360s**, recorded native cost **$12.549894**, settled INCOMPLETE. At **08:25:03.629Z**, after one FULL admission fee Save, `observeSave` recorded **zero polls, 33 ms, null observation** and raised `BrowserSaveUncertainError`; this was not expiration of its 60-second budget. `save-observer.mjs` catches all underlying errors without retaining cause/category/phase, so the exact original low-level exception is **unproven**. Document/navigation transition is a hypothesis, not a confirmed root cause. Offline simulations prove multiple distinct exceptions collapse to the same marker. Earlier description saves had successful 4–5-poll observations.

The same shell command then successfully ran supported read-only `listFees`, masking the browser nonzero exit: actual progress event **12209 has isError=false**, and `classifyBrowserFailure()` returns null. No browserFailureGuard was recorded. Agent stopped configuring and marker guards blocked writes; local reporting continued, then normal settlement returned INCOMPLETE. Immediate controller stop was not demonstrated by this run. Fix must consume authoritative current-run uncertainty/structured results, not depend solely on shell exit or scan arbitrary quoted stdout.

Readback observed new fee **24d8a88e-ca41-41d4-96de-0d121e293114**, USD530 / early255 and330 / ATT; complete fee diff **one added, zero removed, zero modified existing**. Do not duplicate/replay. Defaults/refund fields/remaining relationships and final Draft were not fully accepted. Historical marker remains; human reconciliation required. Cleanup receipt STOPPED, no stop failures/unreconciled spending, recorded native PID no longer exists. Most remaining sections were not reached, including Site Designer (correctly last), rather than each independently failing.

Additional source gaps: independently inspected original XLSX ZIP contains **13 PNGs/two drawing parts**, despite earlier zero-image library observation. Reported old/new pricing, shared label/mapping differences, invalid `#FF8F2`, discount input gaps and platform defaults need source-aware resolution, not invented defaults. Durable plan: prevalidated single-save transaction; sanitized diagnostic causes; narrowly bounded read-only handling of known document transitions with stable ownership/identity; full independent saved readback before declaring success/uncertainty; controller marker-based stop; OOXML asset/input preflight; real navigation/mixed-shell/identity-loss regressions followed by explicitly approved live full-RR acceptance. Keep one Pi and concise prompt.

**Azure remains staged but disabled as below, with the same observer/guard source.** Do not activate it as a fix for this failure. Team model credential still deferred until user calls it out. Current review does not authorize implementation, new runs, source activation or historical uncertainty clearance.

## Azure infrastructure staged — prior deployment checkpoint (2026-09-21)

User chose **a team-funded model API credential**, then explicitly said they will provide it at the end and to **continue**. Do not ask for it again prematurely, copy personal OAuth state, or launch paid builds. **Three Azure VMs are now provisioned and staged, not activated/team-ready.** Existing Azure Cvent services and local runtime/source/vendor/data remain untouched. No commit/push.

Deployment `cvent-ego-pilot-20260921` succeeded in **`rg-chartdarts-stg`**: `cvent-ego-1`, `cvent-ego-2`, `cvent-ego-3`, each ARM64 `Standard_D4ps_v6`, separate disks/NICs, dedicated `ego-vnet` and `ego-ssh-only` NSG. Current Azure identity has staging-resource-group permissions, but none at subscription scope; new-resource-group existence check returned Forbidden. Used the authorized staging scope for separately named NEW resources, no access escalation or existing-resource replacement. Existing `cvent-pi-dev-vm` and Caddy were not modified. Three new VMs are running: public compute list price **$0.42/hour total**, excluding disks/IPs/network/model usage.

Administrator SSH host keys were recovered through authenticated Azure Run Command, then strictly pinned for SSH/SCP. Verified NSG permits only SSH from the administrator's `47.151.24.159/32`, denying all other inbound including VNet peers. No dashboard/CDP/Docker API ports exposed. Per-user tunnel-only access is **not yet configured**. Private connection inventory/known hosts: `logs/azure-deploy/{hosts.json,known_hosts}`; do not publish or confuse administrator access with team onboarding.

New `deploy/azure/bootstrap.sh` and `stage.sh` ran once per new VM: Node **24.13.0** checksum verified, native Pi **0.85.1**, Python venv/openpyxl **3.1.5**, Docker, exact ARM Steel image, external API-client sources/tsx **4.23.12**, app dependencies and source snapshot. Ego commit verified against exported snapshot, reviewed patch applied and vendor built. Source installed at `/home/egorunner/cvent-ego-runner`, fresh private jobs/workbooks/current state; no historical profiles/workbooks/jobs/accounting or credentials copied. `cvent-ego.service` is **disabled/inactive**, `Restart=no`, owned by `egorunner`, requiring root-managed `/etc/cvent-ego/activation-approved` and `/etc/cvent-ego/runner.env`; neither exists. Do not create those gates until controls/configuration are complete.

**12 deployment tests pass**; shell syntax/whitespace pass. `verify-stage.py` confirmed **18 dependency checks per VM**, all source hashes, exact Steel image, vendor patch, no containers/jobs, no credentials/activation, and only SSH plus expected loopback DNS/containerd listeners. Model selection and Cvent credential configuration are intentionally blocked. Runtime 324-test evidence reused because runtime code unchanged; no claim of live RR/three-session acceptance. Receipts: `logs/azure-deploy/receipt.json`, `cvent-ego-{1,2,3}-verified.json`, `tests-final.log`, network/deployment observations. Source archive hash remains `88ccb9a604504b4c6dcd42598324f3940e13d23dfec510e9d156f607368c9e43`.

Operational reconciliation: initial bootstrap SSH wait timed out while oneshot install units continued; observed existing units and successful completion, **never resubmitted**. Stage used `--no-block` and completed successfully. Initial read-only verifier rejected Ubuntu's normal containerd loopback listener; inspected owners, added a precise process/binding allowlist plus regression test, and reverified. Failed logs preserved. Remote `/var/lib/cvent-ego-{bootstrap,stage}.started` and JSON receipts are durable; **do not clear/replay installers**. No background work remains in flight.

**Next bounded work:** per-user access, cross-instance same-event collision prevention, shared quota handling; then secure model/Cvent configuration and activation/three-session isolation checks. User will call out model credential readiness at the end; provider/model remain unspecified. No credentials moved, paid prompts, browser launches or Cvent mutations in this leaf. Local latest trial remains `42437509-fce2-4b0a-8ea9-4bfd97da0e76` **INCOMPLETE**, report/cleanup/spending not yet reviewed. Full RR independently verified unpublished Draft still unproven. Do not auto-retry it or interrupt any subsequent local build. See `deploy/azure/README.md`.

## Azure deployment preparation — prior preflight (2026-09-21)

User authorized starting Azure deployment immediately, targeting three independent users/builds within an hour. **Prepared, not deployed; no one-hour readiness guarantee.** New files only under `deploy/azure/` plus this handoff; live runtime source/prompt/vendor and original private data were not edited. Azure CLI is authenticated. Existing `cvent-pi-dev-vm` hosts a different running Cvent service/Caddy; read-only guest preflight only, no service changes. Do not reuse or interrupt it.

Selected preparation: three ARM64 `Standard_D4ps_v6` VMs in westus3 to preserve the exact ARM64 Steel image. Public retail compute is $0.42/hour combined, excluding disks/network/model costs; quota preflight passed, not a capacity reservation. `deploy/azure/plan.py` generates infrastructure with SSH limited to one public administrator `/32`, password authentication disabled, and all other inbound traffic (including VNet peers) denied. Dashboard/CDP stay loopback-only behind private SSH access. Azure template **validation**, not resource creation, succeeded. No new Azure resources, secrets transferred, browser launches or paid prompts.

`deploy/azure/bundle.py` creates a source-only snapshot preserving intentional edits and the vendor pin/patch, including four required external API-client source files. No jobs, workbooks, profiles, credentials, Pi global state or accounting copied. Snapshot/manifest and sanitized receipts under `logs/azure-preflight/`; source archive SHA256 `88ccb9a604504b4c6dcd42598324f3940e13d23dfec510e9d156f607368c9e43`. **8 deployment tests pass**, whitespace check passes, archive/source hashes match. Existing 324-test runtime evidence reused; no runtime code changes. This is not a content/secret audit, installed runtime, concurrency test or full RR acceptance.

**Human decision requested:** local app uses personal `openai-codex` OAuth with `gpt-6-astra`, no model API-key environment variable. Do not clone that refresh state across team VMs. Choose individual human Pi logins or a team-funded provider API credential via a secure channel, never chat. Three user identities/SSH keys and access networks remain unassigned. Cross-instance same-event exclusion, shared quota coordination, installer/service setup and three-session acceptance still unimplemented. `deploy/azure/README.md` records precise prerequisites and remaining work. Do not provision merely to spend money while these decisions are unresolved, expose a public dashboard, or call templates a working deployment.

The user's local trial `42437509-fce2-4b0a-8ea9-4bfd97da0e76` changed from RUNNING to **INCOMPLETE** during preparation, without intervention. Its report/cleanup and spending have not yet been inspected; no automatic restart/retry/resume is authorized. The prior no-live-runtime-change rule was observed throughout.

## Save observation and API acknowledgment fixes — prior activation (2026-09-21)

**New rollout requirement:** user needs **three users with three independent builds running concurrently**. Current app does not support this: `mountRR` has one global active/busy controller, `data/current`/viewer/ownership are shared, workbook access is not user-scoped, and access control is loopback-only rather than multi-user authentication. Individual jobs already receive separate Steel containers/profiles and random loopback ports. Proposed smallest MVP approach: three fully isolated single-user deployments (separate data, browser/session, process control and access), rather than removing the global busy guard. Requires per-user authenticated access, same-Cvent-event collision prevention across instances, shared API/model quota handling and a three-session isolation/capacity test. Historical status at this inspection: not implemented or deployment-approved. The subsequent Azure preparation authorization and unresolved deployment decisions are recorded in CURRENT above. No broad rollout or automatic additional paid runs.

At this requirement's read-only inspection, new job `42437509-fce2-4b0a-8ea9-4bfd97da0e76` was RUNNING. User explicitly says it is their current trial. No runtime/source changes, restart, browser actions or new sessions were made during this inspection. Leave that run alone; documentation note only.

User authorized implementing the smallest save/verification fixes in the existing single-Pi workflow. **Implemented, offline-tested and activated by the explicitly authorized app-only restart below; live end-to-end completion remains unproven.** No new agent, automatic retry, bulk dependency or prompt expansion. `app/runner-prompt.md` remains 448 words.

- New `ego-bridge/save-observer.mjs`, exposed as project-global `observeSave(page, options)` by `ego-bridge/cli.mjs`. Agent supplies CSS selectors grounded in the observed UI before Save. Read-only polling distinguishes visible pending, completion and explicit rejection; default 60-second polling budget, maximum 120 seconds, existing protocol timeouts apply. Completion/rejection results are explicitly **not saved verification**. No click/retry/navigation/refresh from the observer; spinner disappearance/validation count alone is not an outcome. Visible top-document DOM only, not arbitrary iframe/canvas coverage.
- `ego-bridge/host.mjs` blocks input/navigation while visible `Saving...`/`Saving…` is detected on the selected event, even without calling the helper. Read-only inspection remains available. Observer timeout/conflict/error/lost control emits `BrowserSaveUncertainError` and retains a private current-run `browser-save-uncertain.json`; never overwrites/clears a prior marker. Browser/API write paths reject that marker. Server classifier stops on the first uncertainty error and completion parsing refuses DONE with the marker. This is not permission to reopen/replay historical stopped saves.
- Scoped API dispatcher retains bounded credential-redacted JSON acknowledgment bodies/shape/request IDs before resource validation; malformed JSON is classified without retaining raw non-JSON. A normal resource object takes precedence over incidental `data`. Feature enablement alone may resolve a missing identity on an otherwise nonconflicting successful acknowledgment through the existing full-catalog preserved-state readback plus selected-event Draft verification. Foreign/unsupported identities, conflicting acknowledgments, HTTP failures and stale/conflicting saved state still stop without replay. Other mutation identity checks remain strict. The original feature-response body is still unavailable, so its exact failure cause remains unproven.
- Guidance: `.pi/skills/ego-browser/references/steel-bridge.md` and `CVENT-API.md`. Original pinned skill/vendor patch untouched. No new high-level Page API or browser replacement.

**324 full offline tests pass**, `/tmp/cvent-save-fixes-full.log`; earlier focused run 71 passing, `/tmp/cvent-save-fixes-focused.log`. Syntax, whitespace and indexed-artifact checks pass (`/tmp/cvent-save-fixes-repository.log`); no claim of secret/history audit. Added DOM/polling/pending guard/marker/Stop/CLI/DONE/acknowledgment regressions. Existing dirty work preserved, no commit/push.

**Activation completed after user Stop and explicit restart request:** PID 81774 → **44417**, loopback port **8788**. The stopped run `163ecc86-92ef-4a57-9334-a987fddfb312` was STOPPED with no owned process, operation locks, cleanup failures or unreconciled spending. New app is healthy, USER-owned, browser stopped, with matching served assets. Exact launch environment preserved only in memory; source hashes recorded. All **937 checked non-profile data files** and all budget fields are unchanged across restart (charged $59.793814; historical $110.706098; automatic spending limit disabled). No paid prompt, browser launch, Cvent mutation or historical-marker change. Receipt: `logs/save-fixes-activation-_xxo0msc/receipt.json`; one-time script `logs/activate-save-fixes.py`, **do not automatically replay**. Server-side uncertainty classifier/DONE gate now load in the fresh app; CLI/bridge load per invocation. Refresh the outer dashboard; no job was started or resumed. Do not automatically restart or launch a paid trial. No live Cvent requests, browser actions/launches, account changes, cost reset, historical marker edits or paid sessions during implementation. App preflight remained USER-owned with browser stopped. A fresh approved RR trial is still needed to prove behavior; remaining pricing/assets/frame/workbook blockers were not repaired by this leaf.

**Historical implementation-time observation, superseded by Stop/restart above:** job `163ecc86-92ef-4a57-9334-a987fddfb312` was RUNNING; `/api/runtime` shows USER ownership and `browserStopped: false`, spending still $59.453916. This was not launched, steered, stopped or restarted by this implementation turn. No further source changes or restart occurred while it was active. At that observation the server-side changes were not yet activated; that interrupted run is not proof of the complete new fix. reviewr toggle was invoked as a human review surface; its context was the parent home workspace, not a verified repository-scoped approval.

## Earlier API access review (2026-09-21)

**Latest recheck after user said “Okay done”:** one fresh OAuth request through the unchanged app configuration still returned the same **75 documented scopes**, with **zero added/removed scopes and neither Bulk scope**. Receipt: `logs/api-access-review/scope-recheck.json`; OAuth-only maintenance script alongside it. No resource API calls, bulk retry, credential/permission edits, restart or paid session; budget unchanged and app remained idle USER-owned. The user's reported change is not reflected in this connection's token metadata. Next reconcile the credential-bearing application's saved permissions/identity with the configured client; do not assume success, clear prior receipts, or automatically retry.

User requested a current API availability/update check after supplying a screenshot with both Bulk permissions selected. See [docs/API-ACCESS-REVIEW.md](docs/API-ACCESS-REVIEW.md). Re-fetched Cvent's published SDK OpenAPI: **unchanged SHA-256**, 469 operations / 356 paths / 238 scopes; npm `@cvent/sdk` latest is 1.6.2. No package upgrade or runtime code change.

Authorized read-only maintenance used the existing app configuration/adapter and a **fresh token**. Its `scp` metadata contains **75 documented scopes, neither bulk read nor bulk write**. Only allowlisted public scope names retained; no credentials/token/raw claims logged. JWT metadata decoded locally, not independently signature-verified. Credentials file is configured, with **no client-ID/secret environment override**; North America `/ea` endpoint. This mismatch with the screenshot requires checking the exact configured application and saved/applied grants, not assuming a missing checkbox or stale SDK. Exact cause still unproven; no permission/credential changes or bulk retry performed.

**All 15 supported read operations passed** (event identity/unpublished verification + 14 catalog operations; choices sampled for one event-owned question). Write scopes for event details, registration types, discounts and features appear in token metadata; no write tests. Public route coverage gaps/protected operations are unchanged. Full inventory and scope comparison: `logs/api-access-review/all-api-inventory.md`; sanitized live receipt: `logs/api-access-review/live-access.json`. Scope presence is not proof of destination write behavior or authorization to exceed SOW.

App stayed USER-owned, browser stopped, no paid session/restart/mutations; budget unchanged. Historical uncertainty and prior bulk receipt preserved. No full test rerun for this docs/maintenance-only inspection; preceding 305-test evidence remains. Do not automatically repeat the one-shot audit or bulk create. The next action is to reconcile the UI application/saved grants with the runner's configured connection; full RR completion remains unproven.

Later PDF evidence clarifies the UI: the six-page Downloads capture dated `2026-09-21-00_34_24` is **Edit workspace**, not the application settings. Page 2 visibly selects both Bulk permissions. Cvent's current Developer Quickstart separately requires assigning scopes to the Machine to Machine application and saving it. Next inspect that existing application's scope selection/workspace association; no permission changes, credential rotation or renewed API probe performed from the PDF. Source PDF preserved; only local redacted/OCR derivatives under `/tmp`.

## Bulk access probe (earlier on 2026-09-21)

User authorized starting Bulk API implementation/exposure. The first access-check slice is implemented; **live access is blocked**, so record submission was not exposed. `app/cvent-bulk-probe.mjs`, `app/cvent-api-cli.mjs` and `test/bulk-probe.test.mjs` add `probeBulkAccess --confirm-empty-job` as a separate human-only maintenance operation, not a native RR executor operation. It requires idle USER ownership, the fixed selected event, no active/unsettled resources, durable intent and explicit empty-job confirmation. It never uploads data, runs/cancels jobs, changes event objects or accepts arbitrary URLs/IDs. A saved receipt blocks repeat probes; never remove it to retry. Actual bulk lifecycle/results/saved verification remain unimplemented.

Authorized live check used the existing app's credentials opaquely in memory through `bin/cvent-api`, without restart or credential changes. Selected `(C+D) Medtrade Testing Clone 2` (`e712e34c-6117-4d13-bf4c-8ed54cf2b495`) verified Pending. `POST /bulk-jobs` (no data) returned **403 “Access is forbidden”**, request ID `76f730d3-d192-4733-aedf-a7fbbfd7da34`. Receipt: `logs/bulk-access/receipt.json`. No records or run request submitted. Bulk read/destination write permissions are untested; an administrator must check `bulk/bulk-jobs:read`, `bulk/bulk-jobs:write` and destination permissions. The generic 403 does not prove the exact missing grant. No UI bypass, account administration or automatic retry.

**305 offline tests pass**, `/tmp/cvent-bulk-full-tests.log`; 29 focused probe/discount CLI tests pass, `/tmp/cvent-bulk-probe-tests.log`. Syntax, `git diff --check` and repository indexed-artifact gate pass. These do not establish live bulk execution or full RR completion. No paid session/browser launch, budget reset, commit or push; existing dirty work/vendor patch preserved. CLI loads on invocation; no app restart required for this command. Full bulk implementation is paused at the permission blocker.

### Runtime and prior trial corrections

At the access-check preflight the app at port 8788 had USER ownership, stopped browser and no active jobs. Spending remained $59.453916 (historical $110.3662), automatic spending limit disabled. Recheck before future work; older activation figures below are historical.

Latest actual RR was `BDNY 2026 (BDE261) FINAL RR Doc_NEW_2.26.26 (1).xlsx`, not the older CED261 workbook. Trial `4140adec-05ba-4a04-b23b-a30b21c08571` stopped after Website-feature acknowledgment identity failure; uncertainty remains, although the next trial read Website enabled. Trial `49bd14da-78f7-488d-b924-cb4cb4bb159d` settled INCOMPLETE with a safety stop on uncertain Site Designer save: verified $175 Power Players fee and expanded 3% processing-fee applicability, but four social URLs/final saved Draft unverified. Never reopen/replay the uncertain save or clear markers automatically. Reports/receipts remain under each job directory. Full end-to-end completion is unproven.

Earlier authorized guidance changes remain: use the applicable existing editor rather than assuming the separate Website feature is required; Site Designer last; “Await saves; inspect validation.” Prompt is 448 words. The bridge guide distinguishes responsive pending saves from uncertain outcomes, without relaxing safety stops. No additional paid trial is authorized automatically. Remaining UI/pricing/assets/frame/workbook issues are not solved by bulk access.

## Previous scope cleanup and bounded iframe inspection

**Read this section before continuing.** After the scope/guidance cleanup, the user explicitly authorized an app-only restart and one fresh paid RR run. The restart is complete; fresh upload and human login are pending. One native Pi/RPC executor, API-first routing, Ego for documented gaps, human-only login, event scope, ownership, Stop/uncertainty, saved-result verification and cost tracking remain. Complete live RR execution is unproven; not approved for team/public production.

### This change

- The concise prompt now explicitly includes event details (dates/timezone/location/capacity), RR-supplied branding/assets and RR-required widget types. It remains below **450 words**, with no all-widget checklist, extra agent, reviewer, retry stage or prescribed planning sequence. Existing event-only edits, exact values, shared protection, no-delete, Draft and Stop/no-replay rules remain.
- Replaced conflicting historical guidance in `CVENT-API-COVERAGE.md`, `E2E-ACCEPTANCE.md` and `CLEAN-BUILD-PLAN.md` with short current references. Their previous contents are preserved byte-for-byte under `docs/history/`. Historical create-only policies and acceptance checklists are not current instructions.
- Added bounded iframe inspection in `ego-bridge/host.mjs`: an explicit top-document iframe-owner subtree can inspect an attached same-renderer document, validate frame/document identity and return frame-scoped refs accepted by pinned Ego. Unnamed iframe owners are discoverable. No automatic/nested frame expansion, out-of-process support, extra target attachment or new write pathway. Updated the bridge guide; live frame editing/saved verification remain unproven.
- Reviewed the `(C+D)` API restriction; retained it because the selected RR event qualifies and removing it would broaden the safety boundary. No-delete and isolated shared-creation requirements remain unchanged. Catalog authoring for admissions/fees/paths/questions/rules still requires supported live Ego workflows; no invented APIs or claim that all workflow blockers are repaired.
- **288 offline tests pass**, `logs/scope-cleanup-final-tests.log` (baseline 282; six new frame tests). Focused tests, syntax/whitespace and indexed-artifact checks pass. Captured-prompt tests verify the scope additions and ≤450 words. Baseline sources/hashes: `logs/scope-cleanup-baseline/`; incremental review/evidence: `logs/scope-cleanup-incremental.diff`, `logs/scope-cleanup-verification.json`. Tests establish supplied instructions and mocked behavior, not complete RR execution, reduced cost or ~90-minute delivery.
- Existing dirty work/vendor patch, API behavior, cached-evidence strategy, costs, timer and lifecycle are preserved. The cleanup itself performed no commit/push, paid run, browser launch, Cvent action, restart, accounting reset or private runtime-data modification. The subsequently authorized app-only activation is recorded below.

### Agreed deliverable exclusions

Keep workbook uploads. Structured intake/no-chat, operator template cloning, Azure/M365 deployment/review/approval/Outlook/triage, the original $30 ceiling/$9–12 estimates, retry-from-failed-step, and separate code-guardrail/audit-trail/reference-event-acceptance deliverables are excluded. This is **not permission to remove existing safety protections or saved-result verification**. Do not add a formal acceptance/compliance layer.

### Current capabilities and limits

The preceding scoped API implementation is retained:

- `updateEvent` / `updateEventBasics`: selected-event description/note, dates/deadline, timezone, format, capacity, merged single venue/location and visibility. Fixed name, `(C+D)` guard, preserved planners/languages/type/archive schedule; unknown or unsupported baselines fail closed.
- `updateRegistrationType`: event-only availability, dates and capacity, not shared names/codes/definitions.
- `configureDiscount` / `configureVolumeDiscount`: missing-identity creation, exact-match no-op, existing event-level updates and additive admission/quantity links. No link omission/deletion or identity suffix/rename workarounds.
- `enableEventFeature`: existing unlocked Website/Registration enablement, preserving configuration; no disable/launch/payment edits. Registration needs a complete pricing baseline.
- Shared contact-type/Event custom-field definition reads. Sessions reads remain outside the production capability surface.

Each mutation retains fresh baselines, resource/event binding, RR references, durable intent, paced dispatch/ownership checks, acknowledgment, independent saved verification, locks, uncertainty and no replay. Explicit API denial is not UI permission.

See [CVENT-API.md](CVENT-API.md) and [CVENT-API-COVERAGE.md](CVENT-API-COVERAGE.md) for exact fields/routes. No reviewed public catalog-authoring routes were found for admissions, paths, fee schedules, optional items, vouchers, questions/choices, general advanced rules or Site Designer. These are documented Ego gaps, **not certified browser workflows**. No isolated shared-create API is established: contact types are GET-only; custom-field creation has unproven account-wide effects. Custom-field answers remain blocked because integration identifiers cannot safely be distinguished.

Authorization permits all applicable SOW event-only edits and supported isolated new shared build objects, but not existing shared-definition changes, deletion/archive, other-event effects, publishing, communications, attendee access, sessions/speakers, account/payment provisioning, credentials or integration-identifier changes. Names/codes/values stay exact; no invented defaults. [README.md](README.md#scope-and-safety) records the current scope. Site Designer last; Continue, never Restore.

### Activation and accounting

Automatic dollar thresholds were removed in the preceding source change. Per-run costs, historical totals/reset credits, two-second sampling, final settlement and lost-metering stops remain. Legacy allowance/reserve fields are metadata only.

**Authorized app-only restart completed and verified:** PID 45094 → 81774 on loopback port 8788. Exact launch environment was preserved in memory, never logged. Preflight found no unsettled jobs, live owned native PIDs, operation locks or unreconciled spending. Receipt: `logs/scope-cleanup-activation-urktnztg/receipt.json`; script: `logs/activate-scope-cleanup.py` (one-time maintenance, do not replay automatically).

Post-restart `/api/runtime` confirms `spendingLimitEnabled: false`, login-first enabled, USER ownership and browser stopped. Recorded charge remains **$43.735748**; historical total **$94.648032** and reset ID are unchanged. All **832 checked non-profile data files** are byte-identical; served UI assets match source. No accounting reset, browser launch, Cvent action or paid prompt occurred. Refresh the outer dashboard. Current guidance is captured at fresh Start; the bridge/API CLI load per invocation. Historical captured SOWs remain unchanged.

These are activation-time observations, not permanent live-state proof. Fresh runs ignore historical write uncertainty as a task/prerequisite, but current-run uncertainty and unsettled resources/accounting still block.

### Evidence and remaining risks

- Scoped API baseline: **276 tests**, `logs/scoped-api-final-tests.log`; syntax/whitespace/indexed-artifact checks in `logs/scoped-api-repository-check.log`; capability output in `logs/scoped-api-capabilities.json`. The repository gate is not a secret/content/history audit.
- The first scoped-API fixture run accidentally attempted OAuth with hard-coded dummy credentials and received HTTP 401. No real credentials/authenticated mutation; fixture fixed and external-fetch denial added to `npm test`. Failed evidence remains `logs/scoped-api-first-tests.log`; do not call that first run fully offline.
- Vendor pin: `dca7003349c5f7132189ba00547cbbd7ff8e597e`. Intentional page-ledger modification matches `ego-bridge/patches/steel-session-ledger.patch`; upstream skill remains unchanged.
- Latest historical incomplete job: `c94422ef-2b4c-4ede-ac3a-43f0c868e6cb`, workbook `CED261 RR Doc FINAL 100325 (1).xlsx`, target `(C+D) Medtrade Testing Clone 2`, event `e712e34c-6117-4d13-bf4c-8ed54cf2b495`. Four admissions were created (Staff, Vendor, Show Management Guest, EAC); website/registration/dependencies remain incomplete. Private report: `data/jobs/<job-id>/reports/final-report.md`. Never resume it automatically.
- Unproven: complete saved/connected Draft SOW acceptance, browser/shared-object isolation, tenant scopes/feature semantics, normalization/eventual consistency, concurrency without server ETags/atomic uniqueness, fresh-host deployment and cost/latency targets. Completion parsing validates report structure/safety, not semantic RR completeness.

### Protected resources

- Never execute, modify or delete `/Users/bailey/Downloads/Medtrade_Testing_Clone_2_MOCK_ONLY_New_RR.xlsx`; SHA-256 `bb381b810379e26b3accf27d93234197f4550c5bacbe4fff0dfc0384128b98e7`.
- Preserve original container `cvent-ego-runner-steel`, all profiles/build artifacts/workbooks/costs/receipts, and `/Users/bailey/Downloads/screencapture-127-0-0-1-28877-2026-09-19-17_02_54.png`.
- The separate Steel service at port 4310 is not this app's runtime; do not use/stop it to repair Cvent.
- Job `0cd488a1-0209-461d-a556-69328bfe0334` has exposed authentication material in historical traces. Never dump/publish it. Human incident acknowledgment exists historically; it is not independent revocation proof and must not be fabricated or cleared.

### Next decision

User has authorized one fresh paid trial, not automatic retries or further RRs. App is ready at `http://127.0.0.1:8788`; user must refresh, freshly upload the intended real RR, confirm the selected event, Start Build, complete human login and explicitly Return to Agent. No job was chosen/started during activation; never resume a historical job or use the prohibited mock workbook. Full SOW capability remains unproven. Preserve all dirty work after `af77208`; no reset or Git action authorized.
