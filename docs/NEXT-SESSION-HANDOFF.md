# Copy-ready /new session handoff

Continue in `/Users/bailey/cvent-ego-runner`. Read `MVP-HANDOFF.md` CURRENT first, then this handoff completely. Historical sections describe earlier states; CURRENT supersedes them. Recheck local runtime/jobs and Git before acting. Preserve the extensive intentional dirty work on main; no reset, commit or push without permission.

## Latest checkpoint update (2026-09-21; supersedes the older next-run state below)

User authorized a source-only Git checkpoint/push **before** trying the simpler workflow. Keep the SOW at `/Users/bailey/Downloads/Finalized - EmeraldX_Timeline_Roadmap_v3.docx` as standing event-build scope, with the later agreed delivery exclusions; RR values supply each run's requirements. Ego/Steel remains required, with one native Pi choosing the plan and independently verifying all applicable in-SOW work. No-delete/no-publish and selected-event/login/Stop/uncertain-write protections remain. No simplification or runtime prompt/source change was implemented during checkpoint preparation.

The user then explicitly selected and authorized the converted workbook below. Fresh run `e4d4b2bb-3489-4cb9-ac8d-aed000c1fb02` finished **INCOMPLETE/SETTLED**, **$7.956288**; aggregate charge **$80.299996**. Its 14-sheet upload has no embedded OOXML media/drawings, hash `d22ef277d1c93a9d89ca1b81d2f041fdad8d731330e341e42867d2acc277c31d`. The original workbook's 13 PNGs do not describe this source. A newly opened dashboard had no selected run, disabling Return; selecting this active run enabled it, and human Return started the fresh Pi. No launcher repair yet.

The final report records no verified creations/saved modifications. EMPP checkbox editing reached `saveOnce` preflight error `Save observation lost assigned event/page`; Save dispatch/completion, persistence and final Draft were not confirmed. No current-run browser/API uncertainty marker was found. This was the native agent's safety stop/normal INCOMPLETE settlement, not evidence of controller-marker stopping. Diagnose this exact preflight path before proposing blanket recovery changes; do not replay the edited operation or infer that no persistence occurred. Reports/receipts remain private under this job. Unconfirmed conversion defaults, missing price/date/asset inputs and unresolved mappings/FT42 remain genuine source blockers.

Checkpoint read-only state: USER-owned, browser stopped, no active jobs or unreconciled spending. **344 runtime + 12 deployment offline tests pass**, private logs `logs/git-checkpoint/`. Private Git destination verified; vendor change matches the tracked patch; no runtime artifacts/credentials/SOW binary belong in the commit. Azure untouched. Next implement/test only a bounded simplification after the checkpoint, preserving the 448-word prompt unless specifically revised with approval. No automatic restart, paid rerun or historical reconciliation as part of checkpointing.

## Product direction — keep it simple

We want an Emerald-team MVP: upload a real RR workbook, select the existing Cvent event, complete the human login/Return to Agent handoff, then let **one fresh native Pi session** implement and independently verify the full RR, ending unpublished Draft. Keep the app a thin launcher/lifecycle shell. Pi interprets the workbook and chooses the plan/order/tools; Python workbook utilities, supported API adapter and Ego/Steel are tools, not additional agents. Do not add a planner, reviewer, rigid model-generated checklist, deterministic importer, extra prompt layers or broad redesign. The runtime prompt `app/runner-prompt.md` is **448 words**, at most 450; implementation details belong in linked guidance. Three users means three isolated single-Pi builds, not three cooperating agents.

The desired experience is upload-and-run, but full saved/connected/Draft acceptance is **not yet proven**. Essential safety and evidence are still required; do not promise that ambiguous source inputs, login expiry or external failures can never require a stop.

## Just completed and activated

The user authorized narrow Save/controller fixes, then explicitly authorized one idle local app restart and this handoff. Restart succeeded: PID **44417 → 81487**, `http://127.0.0.1:8788`. USER ownership, browser stopped, no active/unsettled jobs or owned native worker at activation. Environment preserved in memory only. All **1,003 checked non-profile data files**, accounting and target identity unchanged; charged total **$72.343708**, automatic spending limit disabled. No paid prompt, browser launch, Cvent call/write or Azure change.

Receipt: `logs/save-transaction-activation-ym1exljz/receipt.json`. Do not replay any activation script or clear `logs/save-transaction-activation.started`. This restart authorization has been used; no need to restart again unless separately justified and authorized.

Changes:
- `ego-bridge/save-observer.mjs` / `cli.mjs`: project-global `saveOnce(page, saveRef, options)` prevalidates grounded selectors/edit state, rejects pending/prior/conflicting outcomes, checks continuity, clicks exactly once, then observes. `observeSave` remains for a Save already clicked; never call `saveOnce` after that click.
- Only fixed read-only DOM observation retries destroyed/missing execution contexts or not-ready documents, within the original deadline and with ownership/session/target/event checks before and after reads. Control-check failures, crashes/disconnects, operation timeouts/possible late effects, selector and unknown errors stay terminal after Save. No refresh, navigation or Save retry.
- Uncertainty preserves phase, attempts/polls and sanitized category/type/fingerprint, not raw exception messages. Exclusive private historical markers stay immutable.
- `app/browser-failure-guard.mjs` / `rr-connection.mjs`: authoritative current-job marker checks at tool boundaries, two-second monitoring, settlement and cleanup, independent of shell `isError`. A successful later command cannot mask uncertainty; arbitrary quoted stdout cannot trigger marker authority.
- Guidance: `.pi/skills/ego-browser/references/steel-bridge.md`. Helper completion/rejection is **verified:false**. The same Pi must independently verify exact saved values, relationships and relevant defaults through supported reads/reopening after settled writes. The helper is not an automatic API verifier and does not schedule a 10-second persistence read.

**82 focused / 344 full offline tests pass**: `logs/save-transaction-fix/{focused-final,full-final}.log`; syntax/whitespace checks pass. These are mocked DOM/Page and controller fixtures, not real Steel/CDP navigation or live Cvent acceptance. Reuse this evidence while source is unchanged.

## Why these changes were needed

Latest trial `42437509-fce2-4b0a-8ea9-4bfd97da0e76` ended INCOMPLETE after **19m15s**, **$12.549894**. A FULL admission fee Save was dispatched once. Observation failed before its first completed poll (**33 ms, zero polls**), not at its 60-second deadline. The old catch lost the original exception; navigation/context destruction remains a hypothesis. A successful later `listFees` command made the actual tool-end event `isError:false`, masking the browser failure from the old controller. Marker write guards protected against further mutations, but immediate controller stopping failed.

Readback found a new FULL fee `24d8a88e-ca41-41d4-96de-0d121e293114`, product `e8c257b5-5f3a-4041-bbcc-a3c16b0de51b`: USD530, early255 through 2026-08-31 and330 through 2026-10-14, ATT applicability. One fee added, zero existing changed/removed. This is observed persistence, not complete default/relationship/Draft acceptance. **Never duplicate it, replay Save, clear its marker or resume that stopped run.** Most remaining sections were simply never reached, including Site Designer correctly left last.

Read `docs/RUN-42437509-STOP-REVIEW.md` and narrow final reports/receipts if needed. Do not dump whole traces. Earlier uncertain Website enablement and Site Designer social URLs also remain historical evidence, not instructions to retry. Existing API acknowledgment fixes are already present; Bulk is optional, unconfirmed and not a prerequisite.

## Scope, sources and guardrails

- Prior selected event: `(C+D) Medtrade Testing Clone 2`, UUID `e712e34c-6117-4d13-bf4c-8ed54cf2b495`, code `NLNMYJD28PH`. Preserve identity/name and unpublished status; verify the actual selected target before any future run.
- User-mentioned candidate workbook: `/Users/bailey/Downloads/BDNY_2026_BDE261_FINAL_RR_Doc_NEW_2.26.26_Converted_New_RR (2).xlsx`. Mentioning that path was an execution-model comparison, **not execution authorization**. Confirm the next upload/source; do not assume it is byte-identical to the prior original BDNY workbook.
- Source gaps: original XLSX ZIP has **13 PNGs/two drawing parts** despite earlier library reporting zero images. Map raw OOXML media/anchors to sheets; resolve workbook-wide instructions, old/new pricing, shared-object labels/mappings, invalid `#FF8F2`, unmatched `FT42` and blank discount settings from source before inventing defaults. Do not turn every blank into a separate human question if a workbook-wide rule resolves it.
- Asset transfer into remote Steel and frame coverage are not fully proven. Do not assume local file paths exist in Chromium's container. Follow existing supported interfaces; no alternate browser/CDP/API bypass.
- API-first through supported `CVENT_API_BIN`; assigned project `EGO_BROWSER_BIN` and Steel for documented UI gaps. Do not use the global desktop Ego integration as an apples-to-apples replacement. Preserve pinned Pi 0.85.1, Ego `dca7003349c5f7132189ba00547cbbd7ff8e597e`, reviewed session-ledger patch and ARM64 Steel image `sha256:21cf2a5785aa9478d0f7933c04bce96ca79f3d7a93d9824ea184800d29d3cd02`.
- Event-only effects and exact reuse; create genuinely missing objects only within demonstrated isolation. Never alter existing shared definitions, delete/archive, publish, send communications, access attendees/registrants, sessions/speakers, payments/account administration or credentials/integration IDs. No invented suffixes/defaults.
- Login/SSO/MFA/security attestations are human-only; explicit Return to Agent. Stop on identity/ownership loss, expired login, secrets, crash/disconnect or uncertain execution/save. No blind refresh after ten seconds, uncertain write replay or historical marker clearance. Site Designer last: **Continue, never Restore**.
- Preserve all evidence, profiles, accounting and the original `cvent-ego-runner-steel`. Do not touch unrelated Steel on port 4310. Do not execute/modify/delete `/Users/bailey/Downloads/Medtrade_Testing_Clone_2_MOCK_ONLY_New_RR.xlsx`; preserve `/Users/bailey/Downloads/screencapture-127-0-0-1-28877-2026-09-19-17_02_54.png`. Never dump job `0cd488a1-0209-461d-a556-69328bfe0334` traces (historical auth exposure), or auto-resume `c94422ef-2b4c-4ede-ac3a-43f0c868e6cb`.
- Existing Cvent secrets come from `CVENT_CREDENTIALS_FILE`. Never print credentials/raw environment/claims, paste keys into chat, rotate credentials or add redundant `.env`. No automatic cost reset, paid run or retry. Leave any new user-started build untouched.

## Azure — parked, not the immediate local task

Three ARM64 `Standard_D4ps_v6` VMs `cvent-ego-1/2/3` are running in `rg-chartdarts-stg`, westus3, deployment `cvent-ego-pilot-20260921`; compute list estimate **$0.42/hour combined**, plus extras. Apps are staged but **disabled/inactive**, no model/Cvent credentials or activation gates. They have the older source snapshot, not this newly activated local repair. Do not activate/deploy/replay bootstrap/stage scripts automatically. Leave existing `cvent-pi-dev-vm`/Caddy untouched.

Still needed for team rollout: per-user access, cross-instance same-event exclusion (including local execution), shared quotas, secure credentials and three-session isolation/capacity acceptance. User chose team-funded model API credentials and will explicitly provide them **at the end**; do not repeatedly ask or copy personal OAuth. Provider/model unspecified. See `deploy/azure/README.md`, CURRENT handoff and `logs/azure-deploy/receipt.json`. Local functional proof comes before treating infrastructure as end-to-end readiness.

## What to do next

1. Read the current handoff and relevant code/guidance; check app health, job status/ownership and Git read-only. Report the actual checkpoint briefly. Do not restart again or start a workbook just because a handoff exists.
2. Keep focus on the smallest remaining path to upload-and-run with one Pi. No architectural rework or extra agents. Distinguish a tool defect from missing source inputs and from an untested behavior. Inspect only the source/evidence needed for the next bounded step.
3. Propose a bounded real Save/independent-readback acceptance check and source/asset preflight for the user's selected next workbook. Ask before any paid build, Cvent mutation or historical reconciliation; choose a safe next target only with authorization, not the already-uncertain fee. Real browser/CDP transition coverage is still needed; mocked tests do not prove it.
4. When explicitly authorized, run one fresh native Pi build, preserve the selected event and verify exact saved values, relationships and final unpublished Draft. Observe without injecting a second planner/reviewer. Stop honestly on genuine uncertainty; capture the first concrete blocker instead of blindly repeating full paid runs.
5. After one bounded integrated/verified step, return control to the user. Report what is actually live-proven, any precise remaining blocker and costs. Only then continue separately authorized Azure rollout work.

The user wants a dependable simple Pi-driven product, not more orchestration. Do not claim the upload-to-finished-event goal is complete until an actual RR passes independently verified saved/connected/Draft acceptance.
