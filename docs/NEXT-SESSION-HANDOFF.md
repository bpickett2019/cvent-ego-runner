# Copy-ready /new session handoff

Continue in `/Users/bailey/cvent-ego-runner`. Read `MVP-HANDOFF.md` CURRENT first, then this handoff completely. Historical sections describe earlier states; CURRENT supersedes them. Recheck local runtime/jobs and Git before acting. Preserve the extensive intentional dirty work on main; no reset, commit or push without permission.

## Staging publication completed; AI remains disabled — latest (2026-09-21)

**Live:** `https://staging.app-chartsdarts-dashboard.com/` → `/workspaces/1/`, with working 1/2/3 workspace routing to separate ARM64 VMs. User authorized settling existing jobs, renewing restricted access for 24 hours and completing cutover. Existing HTTPS/Basic login preserved; expiry **2026-09-22T16:18:42.211242+00:00**. This shared login is not per-user authorization. Do not bypass or silently renew expiry.

Three app services are enabled/running with separate synthetic preview jobs/browser profiles, USER ownership and $0 model spend. `CVENT_EXECUTION_ENABLED=false` rejects execution/Return/answer endpoints; no credentials copied, native sessions or Cvent authoring. Team Anthropic Key Vault secret comes in the morning; exact **Sonnet 5** ID/availability, event-collision exclusion and shared quota controls still require verification before paid activation. No automatic trial.

Old six unleased waiting jobs settled via canonical store lifecycle, preserving uncertainty and **2,053 artifact files**, database and Caddy backups: five `failed_prewrite`, one `failed_recoverable`. Old `cvent-one-shot` stopped/disabled, not deleted. New gateway on loopback 8890 uses pinned, local-forward-only SSH accounts to each VM's loopback 8788; forward ports 18781/18782/18783. Only new NSG rule permits gateway public `/32` TCP22; no public backend/viewer/CDP/Docker, DNS or IAM changes. `/pi` offline route preserved, retired old viewer is 410.

Application archive **`4e1a410dfa79bf497e741a5ddc055eb367276511a171dac6c62eceb19604ade0`**. Evidence `logs/staging-cutover/`: `root-published-receipt.json`, `live-verification-final.log`, per-host refresh/activation/security receipts. **353 offline tests**, 21 deployment tests at activation; fresh real local three-browser/Stop/cleanup PASS `logs/local-three-PyO8VW/receipt.json`. Live private checks pass for assets/API/config, identity/storage routes, foreign-job read/Stop rejection, disabled AI endpoints and viewer WebSocket **101 upgrades**. No video-frame/rendered cloud acceptance claimed: existing human Basic login is still required. Anonymous public TLS checks return 401. Initial verification-script expectations were corrected; retained failures were not production retries.

Never replay `logs/staging-cutover/publish-approved-once.py` or clear intents: `/var/lib/cvent-ego-gateway-prepare.started`, `/var/lib/cvent-ego-preview-activation.started` on each backend, `/var/lib/cvent-ego-source-refresh/<sha>/`, `/var/lib/cvent-ego-root-cutover/`. Gateway environment `/etc/cvent-ego-gateway.env`; backend activation `/etc/cvent-ego/{runner.env,activation-approved}`. Do not dump auth hashes, keys, environment or legacy DB. Original local PID4846 and interactive supervisor31501 remain untouched; leave local previews and desktop Ego space53 with user. Git push remains authorized; inspect remote/current commit and private publication receipt rather than assume older `0fa34e3` contains these changes. Next: human cloud login/click-around; pause before any new leaf, credentials or paid work. CURRENT MVP handoff supersedes every older instruction below.

## Source push authorized; staging blocked on existing jobs/access — prior (2026-09-21)

User selected **Anthropic Sonnet 5**; team Key Vault secret comes **in the morning**. They authorized GitHub push and publishing the three-workspace setup in place of the existing root staging dashboard. Verify exact model ID/availability after secure credentials arrive; no personal OAuth, key in chat, or automatic paid trial. No model credentials retrieved/transferred.

Pre-cutover discovery found **six durable `login_required` jobs** in the existing cloud app, **zero worker/event leases**, and no direct app children. Do not claim these are six paid executions, or discard/alter them automatically. Existing runtime `CVENT_STAGING_RESTRICTED_ACCESS=1` has expiration **2026-09-21T04:14:27Z**, already expired at the 11:37 UTC check. Basic auth still exists, but bypassing the application expiry during replacement is not allowed. **Publication not completed:** ask approval to stop/settle the waiting jobs preserving all data/evidence and renew restricted access (proposed 24 hours). Then implement/test protected gateway/root cutover without weakening auth or exposing backend/viewer/CDP ports. No cloud service, DNS, NSG, IAM, auth or source/activation changes made in this preflight.

Evidence `logs/azure-staging-ui/{idle-auth-policy,job-lease-preflight}.log`. Private Git repo verified; explicit source-only checkpoint/push authorized, receipt under that log directory. **347 offline runtime tests reused**, **21 deployment tests pass** again. No private data/profile/workbook/credential/cost artifacts in Git; vendor patch retained. Current local previews remain running/handed to user, original dashboard idle. CURRENT MVP handoff supersedes earlier status; check Git remote and receipt to confirm checkpoint push rather than assuming it.

## Existing Azure staging domain — prior (2026-09-21)

User asks to deploy the three-workspace setup using `https://staging.app-chartsdarts-dashboard.com/`. Read-only Azure/domain discovery: hostname points to existing `cvent-pi-dev-vm` / `57.154.50.217`; Caddy HTTPS/one HTTP Basic account; upstream `127.0.0.1:8877`, `cvent-one-shot.service`, `/opt/cvent-one-shot/current`, OS user `cvent`. Also existing `/pi` and other matcher/proxy routes; preserve them and never dump auth hashes/environment/secrets. Sanitized evidence `logs/azure-staging-ui/existing-routing.json.log`. Browser navigation hit `ERR_INVALID_AUTH_CREDENTIALS`, not an app defect; human web authentication still needed for rendered acceptance. Do not bypass the challenge or automatically create replacement browser spaces.

No Azure source/activation/proxy/DNS/network/auth changes or service restarts yet. **User explicitly authorized replacing the existing `/` dashboard, then asked to enable paid AI too.** Asked for team provider/model and optional Key Vault/secret names (not secret values). Do not start paid trials automatically or copy personal Pi OAuth. Cutover/paid activation have not happened. Current shared staging login is not per-user authorization; the local loopback switcher cannot simply be published as a remote multi-user gateway. Preserve authenticated restricted access and backend/browser loopback isolation; implement/test appropriate routing only after cutover choice. Dedicated `cvent-ego-1/2/3` remain staged/disabled; latest preview UI not yet refreshed there. Paid activation is requested but pending team credentials/provider/model and collision/quota controls; local previews still have AI disabled. Existing vault `kvcventstg729` metadata verified, no secret values read and no IAM changes. Existing host is AMD64; preserve exact ARM64 Steel on the three dedicated VMs. Existing Python app exposes status/job routes and uses a Key Vault launcher; PID had no direct children but full idle check is still needed. Shape evidence `logs/azure-staging-ui/existing-app-shape.log`. No gateway code/deployment mutation implemented yet.

Rechecked local original/three previews: USER-owned, original stopped/idle at $103.476558; three interactive preview browsers running at $0. Leave supervisor 31501, ports 50825/50828/50831 and user-owned Ego space 53 alone. No Git commit/push or paid trial. User intentionally stopped the last RR; no failure-diagnosis/replay task. CURRENT MVP handoff supersedes earlier instructions below.

## Interactive local workspaces — prior (2026-09-21)

User corrected the prior framing: **they intentionally stopped the last RR**, so do not diagnose its terminal report as an unsolicited failure or replay it. They need to click around each User 1/2/3 workspace.

Now **running and handed to the user**: User 1 `http://127.0.0.1:50825`, User 2 `http://127.0.0.1:50828`, User 3 `http://127.0.0.1:50831`. Sidebar USER buttons switch actual separate app/data/HOME/browser instances; proper selected-user labels and active-job controls. Original dashboard PID 4846/8788 remains idle and is not one of these preview URLs. Global desktop Ego space **53**, p1, handed back on User 1's viewer; do not operate it or the USER-owned Steel pages without authorization. Model execution disabled, no credentials copied, no paid sessions. Each preview currently holds a synthetic lifecycle workbook/job waiting for human setup, not a real RR build.

Owner supervisor **PID 31501**, `node scripts/validate-three-local.mjs --interactive`, live receipt `logs/local-three-KamoWi/receipt.json`; dispatch/UI handoff/screenshots/tests `logs/local-workspace-ui/`. Keep apps/browsers running for user interaction. Do not launch duplicates, replay the dispatch, recycle profiles or auto-clean up. If user later asks to stop, verify the exact supervisor identity then SIGTERM it; its graceful cleanup stops only these source-copy apps/current browsers and preserves evidence/profiles. Not the original dashboard or unrelated Docker services.

Changes: opt-in `--interactive` mode in local validator; new `public/workspace-config.js` and strict loopback config tests; `public/app.js`/`styles.css` add actual configured workspace switching/labels/mobile visibility and select the own active preview job on a new tab. Generated `public/local-workspaces.json` exists only in private local copies; no public tenant selector, credentials, server/backend/guard change or original restart. Main single-user UI remains single-user without that config. Azure untouched and still staged/disabled; this frontend addition is not on those VMs yet.

**347 offline tests pass**, plus fresh real three-browser Stop/cleanup PASS `logs/local-three-I4wex5/receipt.json`. Click-through **1 → 2 → 3 → 1** and live Cvent login viewer rendering verified in desktop Ego; screenshots User 1/3 retained, User 2 canvas/viewer observed. No automated login/Return, Cvent writes or native execution. Not full authenticated RR/Draft, shared event/quota, distinct OS-user security or Azure acceptance. Next let the user click around and respond to concrete UI feedback; do not start a new paid trial or diagnose the user-stopped RR automatically. CURRENT in MVP-HANDOFF.md supersedes older notes.

## Local validation first — prior (2026-09-21)

User asked to stop the running trial, validate all functionality locally including three users before further Azure work, and confirm roughly $60 cost. The trial had already settled **INCOMPLETE**, actual **$23.176562**, spending reconciled and browser/Pi stopped. Job `4a0f4127-9741-4f88-82c8-82b59163cd2d` final report says saved Internal-path verification found ATTGUE associated but SPONCOMP unassociated. No replay or live reconciliation. Aggregate charge **$103.476558**. $60 remains an unverified target, not an enforced limit.

Completed bounded local validation: new opt-in `scripts/validate-three-local.mjs` creates three separate source-only app/HOME/data copies, shares installed dependencies, launches three real Steel browsers through the unchanged production login-first route, and stops only its own resources. **PASS** `logs/local-three-JtGQC6/receipt.json`: distinct jobs/profiles/sessions/targets/ports/Ego ledgers, separate upload lists, foreign job Stop/read and foreign Origin rejection, viewer HTML proxy routing; Stop of user 1 leaves users 2/3 running. All test apps/browsers stopped; zero native sessions/model cost; no credentials copied, Return, login or Cvent authoring. Original PID 4846 runtime/jobs/lock/accounting unchanged. Retained test profiles/evidence are private, not reusable sessions.

**337 offline tests pass**, including added mocked three-native-controller sessions/Stop/accounting isolation in `test/login-first.test.mjs`; log `logs/local-three-JtGQC6/offline-tests.log`. No runtime source change/restart, Azure action, paid trial or Git commit/push. Azure apps still staged/disabled; existing VMs continue charges.

This does NOT prove distinct OS-user security, human login, interactive viewer stream/input, three paid concurrent native builds, same-event exclusion/shared quota, or complete saved/connected Draft RR acceptance. User explicitly wants local proof before more Azure work. The proposed failure diagnosis is superseded: user clarified that they deliberately stopped the run and instead wants clickable workspaces. Any later paid trial still requires approval and human login. Keep model credentials deferred as requested. CURRENT in MVP-HANDOFF.md supersedes older notes below.

## Three-user deployment — prior (2026-09-21)

User stopped the monitoring task and asked to make **this exact setup run for 3 users**. All three existing dedicated Azure VMs were reverified unused/disabled and refreshed with current native-Pi launcher, 318-word task, standing SOW, Ego/Steel/API code and missing `bin/rr-evidence`. **15 source files updated per VM**, all hashes/dependencies/data/vendor/image verified; policy/SOW import and RR-helper availability checked without agents/browser/API calls. **21 deployment tests pass**, 18 dependency checks per VM. Runtime 334-test and focused 74-test evidence reused; no local runtime code edits.

**Still staged, not active/team-ready.** No credentials copied, personal OAuth, service activation, new containers, paid prompts, network/IAM changes or existing-service interference. Source snapshot SHA256 `c4085c0457ab458bc8f324f09139cafd6c996682ffe84159afe05e269bc8b302`; private evidence `logs/azure-native-pi/`. New `deploy/azure/refresh-stage.py` is source-only for unused/disabled/unconfigured stages with unchanged dependencies, backups and exclusive intent. Never replay private dispatcher `refresh-approved-three.py`, old bootstrap/stage or remove remote `/var/lib/cvent-ego-source-refresh/<sha>/` intents. Read `deploy/azure/README.md` completely before the next deployment step.

Local job `4a0f4127-9741-4f88-82c8-82b59163cd2d` (CGJWS 2026 RR) remained RUNNING at deployment preflight; no intervention. Latest preceding sample $16.892072, not final. It had verified API event-details update and saved-page snapshots for VIP/EXONLY/PRESS/countdown, but no final RR/Draft acceptance. Recheck read-only before future local edits; do not assume settled or auto-retry. Local server PID 4846; Ego dashboard space 52 remains USER-owned.

Next: restricted operator access (three identities/public keys/source networks still needed), event-collision exclusion including local builds, shared quotas; then secure team credentials and explicitly authorized activation/three-session tests. The user will provide the model credential **at the end**; provider/model still unspecified. Do not ask early or copy personal OAuth. Existing three VMs continue prior compute charges. This completed leaf is source alignment, not three-user availability. CURRENT in MVP-HANDOFF.md supersedes the older activation/handoff notes below.

## Native-Pi simplification — prior local activation (2026-09-21)

User said **“Let pi be pi”** and authorized implementation. Working-tree changes after pushed checkpoint **34ee7cc** now replace prescribed workflow with a **318-word `app/native-task.md`**, pointing to a per-job standing SOW derived from `app/standing-sow.md` and the uploaded RR. Tool/editor order is Pi's choice; no API-first routing, Site Designer-last sequence, preferred Save helper, model deadline target or category completion checklist. One native Pi, existing Ego skill/Steel bridge and supported API tools remain; no extra planner/reviewer/importer or native system/tool replacement.

Launcher captures scope and task separately. New result flags are requirements/draft; event binding, blockers/untested, uncertainty and honest saved verification remain. Legacy results retain their interpretation. Explicit no-delete/no-publish, shared-object/event boundaries, human authentication, Stop, accounting and actual uncertainty guards remain unchanged. **334 full offline tests pass**; redundant old prompt-string contracts were consolidated, not skipped. Logs: `logs/native-pi-simplification/`.

**Activated after explicit approval: PID 97787 → 4846**, port 8788, live policy `native-pi`. No active jobs, USER ownership, stopped browser; **1,066 non-profile data files** and charge **$80.299996** unchanged. Receipt `logs/native-pi-activation-ddjmhzzs/receipt.json`; never replay `logs/activate-native-pi.py` or clear its `.started` marker. No additional restart authorization remains. `app/runner-prompt.md` is a frozen legacy task; active code reads **native-task.md**. Ego dashboard space **52** remains handed to the user.

The user then clarified that prohibited/blocked requirements should be skipped and reported, not halt all work. The 318-word task explicitly says leave them untouched, skip dependent work and continue independent requirements. Confirmed pre-dispatch denials are local; actual Stop, identity/login/control loss, crash/disconnect, exposed secrets or uncertain executed writes remain global stops. Prompt-only change loads for the next fresh job without restart; **74 focused tests pass**, including controller survival of prohibited-action denials and retained uncertainty stopping, `logs/native-pi-simplification/local-blockers.log`. The activation source receipt predates this wording change.

No paid run, live Cvent mutation, source commit/push or Azure change. The prior Save identity error is not fixed by making its helper optional. Next separately authorize one fresh RR trial/human login; no historical replay. CURRENT in MVP-HANDOFF.md supersedes all older instructions below.

## Previous checkpoint update (2026-09-21; supersedes the older next-run state below)

User authorized a source-only Git checkpoint/push **before** trying the simpler workflow. Keep the SOW at `/Users/bailey/Downloads/Finalized - EmeraldX_Timeline_Roadmap_v3.docx` as standing event-build scope, with the later agreed delivery exclusions; RR values supply each run's requirements. Ego/Steel remains required, with one native Pi choosing the plan and independently verifying all applicable in-SOW work. No-delete/no-publish and selected-event/login/Stop/uncertain-write protections remain. No simplification or runtime prompt/source change was implemented during checkpoint preparation.

The user then explicitly selected and authorized the converted workbook below. Fresh run `e4d4b2bb-3489-4cb9-ac8d-aed000c1fb02` finished **INCOMPLETE/SETTLED**, **$7.956288**; aggregate charge **$80.299996**. Its 14-sheet upload has no embedded OOXML media/drawings, hash `d22ef277d1c93a9d89ca1b81d2f041fdad8d731330e341e42867d2acc277c31d`. The original workbook's 13 PNGs do not describe this source. A newly opened dashboard had no selected run, disabling Return; selecting this active run enabled it, and human Return started the fresh Pi. No launcher repair yet.

The final report records no verified creations/saved modifications. EMPP checkbox editing reached `saveOnce` preflight error `Save observation lost assigned event/page`; Save dispatch/completion, persistence and final Draft were not confirmed. No current-run browser/API uncertainty marker was found. This was the native agent's safety stop/normal INCOMPLETE settlement, not evidence of controller-marker stopping. Diagnose this exact preflight path before proposing blanket recovery changes; do not replay the edited operation or infer that no persistence occurred. Reports/receipts remain private under this job. Unconfirmed conversion defaults, missing price/date/asset inputs and unresolved mappings/FT42 remain genuine source blockers.

Checkpoint read-only state: USER-owned, browser stopped, no active jobs or unreconciled spending. **344 runtime + 12 deployment offline tests pass**, private logs `logs/git-checkpoint/`. Private Git destination verified; vendor change matches the tracked patch; no runtime artifacts/credentials/SOW binary belong in the commit. Azure untouched. Next implement/test only a bounded simplification after the checkpoint, preserving the 448-word prompt unless specifically revised with approval. No automatic restart, paid rerun or historical reconciliation as part of checkpointing.

## Product direction — keep it simple

We want an Emerald-team MVP: upload a real RR workbook, select the existing Cvent event, complete the human login/Return to Agent handoff, then let **one fresh native Pi session** implement and independently verify the full RR, ending unpublished Draft. Keep the app a thin launcher/lifecycle shell. Pi interprets the workbook and chooses the plan/order/tools; Python workbook utilities, supported API adapter and Ego/Steel are tools, not additional agents. Do not add a planner, reviewer, rigid model-generated checklist, deterministic importer, extra prompt layers or broad redesign. The runtime prompt `app/runner-prompt.md` is **448 words**, at most 450; implementation details belong in linked guidance. Three users means three isolated single-Pi builds, not three cooperating agents.

The desired experience is upload-and-run, but full saved/connected/Draft acceptance is **not yet proven**. Essential safety and evidence are still required; do not promise that ambiguous source inputs, login expiry or external failures can never require a stop.

## Just completed and activated

The user authorized narrow Save/controller fixes, then explicitly authorized one idle local app restart and this handoff. Restart succeeded: PID **44417 → 81487**, `http://127.0.0.1:8788`. USER ownership, browser stopped, no active/unsettled jobs or owned native worker at activation. Environment preserved in memory only. All **1,003 checked non-profile data files**, accounting and target identity unchanged; charged total **$72.343708**, automatic spending limit disabled. No paid prompt, browser launch, Cvent call/write or Azure change.

Receipt: `logs/save-transaction-activation-ym1exljz/receipt.json`. Do not replay any activation script or clear `logs/save-transaction-activation.started`. This restart authorization has been used; no need to restart again unless separately justified and authorized.

Changes:
- `ego-bridge/save-observer.mjs` / `cli.mjs`: project-global `saveOnce(page, saveRef, options)` prevalidates grounded selectors/edit state, rejects pending/prior/conflicting outcomes, checks continuity, clicks exactly once, then observes. `observeSave` remains for a Save already clicked; never call `saveOnce` after that click.
- Only fixed read-only DOM observation retries destroyed/missing execution contexts or not-ready documents, within the original deadline and with ownership/session/target/event checks before and after reads. Control-check failures, crashes/disconnects, operation timeouts/possible late effects, selector and unknown errors stay terminal after Save. No refresh, navigation or Save retry.
- Uncertainty preserves phase, attempts/polls and sanitized category/type/fingerprint, not raw exception messages. Exclusive private historical markers stay immutable.
- `app/browser-failure-guard.mjs` / `rr-connection.mjs`: authoritative current-job marker checks at tool boundaries, two-second monitoring, settlement and cleanup, independent of shell `isError`. A successful later command cannot mask uncertainty; arbitrary quoted stdout cannot trigger marker authority.
- Guidance: `.pi/skills/ego-browser/references/steel-bridge.md`. Helper completion/rejection is **verified:false**. The same Pi must independently verify exact saved values, relationships and relevant defaults through supported reads/reopening after settled writes. The helper is not an automatic API verifier and does not schedule a 10-second persistence read.

**82 focused / 344 full offline tests pass**: `logs/save-transaction-fix/{focused-final,full-final}.log`; syntax/whitespace checks pass. These are mocked DOM/Page and controller fixtures, not real Steel/CDP navigation or live Cvent acceptance. Reuse this evidence while source is unchanged.

## Why these changes were needed

Latest trial `42437509-fce2-4b0a-8ea9-4bfd97da0e76` ended INCOMPLETE after **19m15s**, **$12.549894**. A FULL admission fee Save was dispatched once. Observation failed before its first completed poll (**33 ms, zero polls**), not at its 60-second deadline. The old catch lost the original exception; navigation/context destruction remains a hypothesis. A successful later `listFees` command made the actual tool-end event `isError:false`, masking the browser failure from the old controller. Marker write guards protected against further mutations, but immediate controller stopping failed.

Readback found a new FULL fee `24d8a88e-ca41-41d4-96de-0d121e293114`, product `e8c257b5-5f3a-4041-bbcc-a3c16b0de51b`: USD530, early255 through 2026-08-31 and330 through 2026-10-14, ATT applicability. One fee added, zero existing changed/removed. This is observed persistence, not complete default/relationship/Draft acceptance. **Never duplicate it, replay Save, clear its marker or resume that stopped run.** Most remaining sections were simply never reached, including Site Designer correctly left last.

Read `docs/RUN-42437509-STOP-REVIEW.md` and narrow final reports/receipts if needed. Do not dump whole traces. Earlier uncertain Website enablement and Site Designer social URLs also remain historical evidence, not instructions to retry. Existing API acknowledgment fixes are already present; Bulk is optional, unconfirmed and not a prerequisite.

## Scope, sources and guardrails

- Prior selected event: `(C+D) Medtrade Testing Clone 2`, UUID `e712e34c-6117-4d13-bf4c-8ed54cf2b495`, code `NLNMYJD28PH`. Preserve identity/name and unpublished status; verify the actual selected target before any future run.
- User-mentioned candidate workbook: `/Users/bailey/Downloads/BDNY_2026_BDE261_FINAL_RR_Doc_NEW_2.26.26_Converted_New_RR (2).xlsx`. Mentioning that path was an execution-model comparison, **not execution authorization**. Confirm the next upload/source; do not assume it is byte-identical to the prior original BDNY workbook.
- Source gaps: original XLSX ZIP has **13 PNGs/two drawing parts** despite earlier library reporting zero images. Map raw OOXML media/anchors to sheets; resolve workbook-wide instructions, old/new pricing, shared-object labels/mappings, invalid `#FF8F2`, unmatched `FT42` and blank discount settings from source before inventing defaults. Do not turn every blank into a separate human question if a workbook-wide rule resolves it.
- Asset transfer into remote Steel and frame coverage are not fully proven. Do not assume local file paths exist in Chromium's container. Follow existing supported interfaces; no alternate browser/CDP/API bypass.
- API-first through supported `CVENT_API_BIN`; assigned project `EGO_BROWSER_BIN` and Steel for documented UI gaps. Do not use the global desktop Ego integration as an apples-to-apples replacement. Preserve pinned Pi 0.85.1, Ego `dca7003349c5f7132189ba00547cbbd7ff8e597e`, reviewed session-ledger patch and ARM64 Steel image `sha256:21cf2a5785aa9478d0f7933c04bce96ca79f3d7a93d9824ea184800d29d3cd02`.
- Event-only effects and exact reuse; create genuinely missing objects only within demonstrated isolation. Never alter existing shared definitions, delete/archive, publish, send communications, access attendees/registrants, sessions/speakers, payments/account administration or credentials/integration IDs. No invented suffixes/defaults.
- Login/SSO/MFA/security attestations are human-only; explicit Return to Agent. Stop on identity/ownership loss, expired login, secrets, crash/disconnect or uncertain execution/save. No blind refresh after ten seconds, uncertain write replay or historical marker clearance. Site Designer last: **Continue, never Restore**.
- Preserve all evidence, profiles, accounting and the original `cvent-ego-runner-steel`. Do not touch unrelated Steel on port 4310. Do not execute/modify/delete `/Users/bailey/Downloads/Medtrade_Testing_Clone_2_MOCK_ONLY_New_RR.xlsx`; preserve `/Users/bailey/Downloads/screencapture-127-0-0-1-28877-2026-09-19-17_02_54.png`. Never dump job `0cd488a1-0209-461d-a556-69328bfe0334` traces (historical auth exposure), or auto-resume `c94422ef-2b4c-4ede-ac3a-43f0c868e6cb`.
- Existing Cvent secrets come from `CVENT_CREDENTIALS_FILE`. Never print credentials/raw environment/claims, paste keys into chat, rotate credentials or add redundant `.env`. No automatic cost reset, paid run or retry. Leave any new user-started build untouched.

## Azure — parked, not the immediate local task

Three ARM64 `Standard_D4ps_v6` VMs `cvent-ego-1/2/3` are running in `rg-chartdarts-stg`, westus3, deployment `cvent-ego-pilot-20260921`; compute list estimate **$0.42/hour combined**, plus extras. Apps are staged but **disabled/inactive**, no model/Cvent credentials or activation gates. They have the older source snapshot, not this newly activated local repair. Do not activate/deploy/replay bootstrap/stage scripts automatically. Leave existing `cvent-pi-dev-vm`/Caddy untouched.

Still needed for team rollout: per-user access, cross-instance same-event exclusion (including local execution), shared quotas, secure credentials and three-session isolation/capacity acceptance. User chose team-funded model API credentials and will explicitly provide them **at the end**; do not repeatedly ask or copy personal OAuth. Provider/model unspecified. See `deploy/azure/README.md`, CURRENT handoff and `logs/azure-deploy/receipt.json`. Local functional proof comes before treating infrastructure as end-to-end readiness.

## What to do next

1. Read the current handoff and relevant code/guidance; check app health, job status/ownership and Git read-only. Report the actual checkpoint briefly. Do not restart again or start a workbook just because a handoff exists.
2. Keep focus on the smallest remaining path to upload-and-run with one Pi. No architectural rework or extra agents. Distinguish a tool defect from missing source inputs and from an untested behavior. Inspect only the source/evidence needed for the next bounded step.
3. Propose a bounded real Save/independent-readback acceptance check and source/asset preflight for the user's selected next workbook. Ask before any paid build, Cvent mutation or historical reconciliation; choose a safe next target only with authorization, not the already-uncertain fee. Real browser/CDP transition coverage is still needed; mocked tests do not prove it.
4. When explicitly authorized, run one fresh native Pi build, preserve the selected event and verify exact saved values, relationships and final unpublished Draft. Observe without injecting a second planner/reviewer. Stop honestly on genuine uncertainty; capture the first concrete blocker instead of blindly repeating full paid runs.
5. After one bounded integrated/verified step, return control to the user. Report what is actually live-proven, any precise remaining blocker and costs. Only then continue separately authorized Azure rollout work.

The user wants a dependable simple Pi-driven product, not more orchestration. Do not claim the upload-to-finished-event goal is complete until an actual RR passes independently verified saved/connected/Draft acceptance.
