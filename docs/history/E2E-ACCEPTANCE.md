# RR end-to-end acceptance checklist

Status: **not accepted for end-to-end or general team deployment**.

## Current acceptance boundary

`app/runner-prompt.md` and `CVENT-API.md` supersede the old create-only permission assumptions and fixed category/checklist language below. DONE concerns only RR-required event-build work and dependencies, not broader project deployment. Across that SOW, existing event-only objects, settings and relationships may be modified; exact matches can be reused. Existing shared/account-wide definitions must stay unchanged. If no exact shared match exists, a separate RR-required shared build object may be created without changing existing shared objects, global defaults or other events. Prove scope before writing; unknown scope blocks that change. Never delete/archive anything, including new objects, widgets, rules or links. Keep Draft, event identity, safety and uncertainty protections.

The CLI now implements scoped existing event/registration/discount updates, additive discount links and build-feature enablement; shared/integration edits and shared creation without isolation proof remain blocked. Explicit denials cannot be probed or bypassed. The user removed automatic dollar stops while retaining per-run/historical costs and lost-metering safety; activation requires an authorized app restart. Live acceptance must verify event-only saved modifications/relationships, exact reuse, isolated shared creation where needed, preservation of existing shared objects, no deletion and honest reporting of remaining tool/input blockers. Original workbook/history/costs remain intact. No new mandatory ledger or per-run audit is introduced. Full completion, shared-object isolation and completed-RR cost remain unproven. No paid run is authorized by this document update.

## Historical acceptance notes and prior-policy checklist (not current execution instructions)

Current correction: historical write uncertainty is removed from all fresh-run gates, not merely redirected to an operator. Successful Return starts directly from the new RR/live saved event state. No historical task pointers, operator-decision prerequisite, recovery prompt, request fence or promotion command. Current-run uncertain writes still stop that run; no replay or false DONE. Historical records/costs remain intact. Resource cleanup, spending, security and identity gates remain. See CURRENT in MVP-HANDOFF.md for tests/activation. Full saved Draft completion and the $60 target remain unproven. Historical notes below are superseded evidence, not execution prerequisites.

Superseded/rejected recovery fix: **257 offline tests pass**, `logs/reconciliation-tests.log`. Login-first Return can launch a fresh, fenced read-only reconciliation session for prior browser changes, validate saved-state receipts and continue the same task without restoring old conversations or adding a prompt. Hard safety blockers remain; changed original baselines require explicit restoration approval. No uncertainty was cleared during maintenance. App-only activation PID 10547 retained 557 protected files after safely stopping the $0 login-wait job; `logs/reconciliation-activation-k71gp32r/receipt.json`. Actual Cvent recovery/fence compatibility and full RR completion remain untested. POST-based reads can be blocked; no bypass or automatic repair is permitted.

Latest maintenance: pre-change checkpoint 63a8c64 pushed; user-authorized accounting reset activated with app PID 80905, $0 current usage, unchanged $60 allowance/$10 reserve, all historical costs/evidence preserved. **242 offline tests pass** (`logs/budget-simplification-tests.log`). Python/catalog/browser instructions favor focused output without a new framework or weakened saved verification. No paid benchmark or Cvent operation; savings and completion remain unproven. At that checkpoint, BDNY footer uncertainty blocked execution; that historical prerequisite has now been removed. Resetting accounting never clears that uncertainty. Activation receipt: `logs/budget-simplification-activation-_mcca7fe/receipt.json`.

Latest activated Steel lifecycle: **235 offline tests pass** (`logs/steel-lifecycle-tests.log`), including terminal cleanup, native failure, login shutdown, provisioning cancellation/races, immutable-ID provenance, failed Docker cleanup and evidence preservation. User-authorized one-time cleanup stopped 13 Steel containers after confirmed OOM kills. App-only activation PID 58367 preserved 2,067 non-profile data files; zero Steel containers running and stopped-viewer response verified (`logs/steel-lifecycle-activation-osemb0th/receipt.json`). Full live RR acceptance is still outstanding.

Prior activated update: **211 offline tests pass** (`logs/rr-done-tests.log`). The 362-word native task now explicitly defines DONE as all RR-required website, registration and connected dependencies saved and verified in Draft. Exact matches count; originals stay unchanged. The existing final-report.json supplies eventId, DONE/INCOMPLETE, four completion booleans and blockers/untested arrays (README.md). No normal exit, missing report, false/missing verification, exceptions or unresolved execution can become DONE. Native saved verification remains the semantic authority—not an independent audit or mandatory review phase. There is no added model turn. Historical generic FINISHED/review outcomes display INCOMPLETE without rewriting evidence. User-authorized app-only restart 71626 → 45724 activated it on port 8788. Receipt `logs/rr-done-activation-631hnvkl/receipt.json` confirms 2,005 protected non-profile files unchanged, USER ownership, no active jobs or new paid run; browser/profile untouched. Same-identity creation remains a conditional Ego capability gap, not universal platform support. Full live/product/deployment acceptance remains unproven.
Owner: the active coding session. One native Pi executor, existing API adapter and pinned Ego skill; no new agent framework or browser replay scripts.

The active instructions are consolidated into one captured task/SOW plus job inputs, with native Pi defaults and the pinned Ego skill/bridge. Ordinary selector errors are recoverable; recognized unconfirmed execution and repeated browser unresponsiveness retain global Stop safeguards. The former seven-section checklist/API map (524 words; 195 offline tests) has been replaced by the plain task described above. Dollar targets and financial fields are omitted from the task/envelope; app-side cumulative spending/Stop safeguards remain unchanged. This does not establish a cost-per-completed-RR result. Retained Steel logs confirm `Page crashed!` for the exact assigned target at 2026-09-20T04:15:38.906Z before subsequent timeouts; the trigger inside the Settings-click/snapshot sequence remains unisolated. This change has no live acceptance evidence and does not establish that the browser hang is repaired. Upload/login/Return and create-only scope are unchanged.

Create-only API coverage now includes named volume rules and their initial admission/quantity links, plus event-bound question-choice and feature-status reads. Mock adapter/production-CLI tests do not certify actual volume financial behavior, concurrency or full saved RR coverage. Existing-item updates remain prohibited; a fresh explicitly authorized acceptance is still required.

Latest CED261 run (`280f6e34-1a96-4316-9825-da087e812e26`) settled in 6m51s at $3.199532, with zero API write intents/nine successful Ego executions and substantial untested work. It used the previous policy, not the new variant-creation task. The earlier CED261 test settled after about 10 minutes at $6.065026 without API write intents. Its report identifies existing differences, incomplete creation values and untested behavior; it is not full acceptance. A new compact evidence helper reproduces 655 identity comparisons in 1,392 output characters while retaining full mappings, but does not determine semantic completeness or permit writes. Prompt now requires source-referenced requirement dispositions and specific blockers for untested work. See `RR-EVIDENCE.md`; browser reinspection remains gated by USER ownership. No new paid retry was launched.

## Fixed outcome and constraints

Per-run DONE covers RR-required **website** (theme/branding, header, footer, all six body-widget types), **registration** (admission items, pricing, registration paths, optional items, vouchers, advanced rules), and **dependencies**, saved, connected and verified in Cvent with the event still Draft. Missing, blocked or unverified requirements mean INCOMPLETE. This is separate from the roadmap's broader Azure/M365/notification/triage/CI engagement deliverables. No automatic review stage is introduced.

Complete every permitted requirement from the actual approved RR. Never rename the event. Reuse existing objects only when all RR-specified values and relationships match exactly. Any specified difference, including one letter or number, requires a separate RR-compliant object; leave originals unchanged. Missing objects also require creation. Exact new/prior creations must not be duplicated on rerun. Fully configure and verify new objects and dependencies; selecting an existing reference does not authorize editing it. Do not invent missing values, suffixes or different codes to evade Cvent uniqueness/singleton limits. A concrete platform limitation blocks that dependent requirement, not independent executable work. No mandatory review phase.

Original workbooks, prior saves, evidence, uncertain-write records and cumulative costs must remain intact. No publishing, communications, attendee access, other events, account-global resources, merchant configuration, deletion or archival. Authentication is human-only. Stopped jobs require a fresh upload/session. API failures and policy denials never authorize browser bypass.

## Prerequisites for live acceptance

- [ ] Reverify incident state and authenticated handoff for acceptance. Read-only audit found the historical incident in `securityAcknowledgedJobs`, timestamp `2026-09-20T01:37:20.724Z`; do not demand or fabricate a new attestation for that same incident. This is human attestation, not independent remote revocation proof. New incidents still block.
- [ ] Identify an approved test RR and the exact existing unpublished test event. The Downloads `Medtrade_Testing_Clone_2_MOCK_ONLY_New_RR.xlsx` is preserved and must not be applied: it expressly prohibits execution. Do not infer authorization to override its warning.
- [ ] User authorizes a bounded paid acceptance run and any permanent new test items. No cleanup/deletion is permitted; choose useful, approved creations rather than disposable clutter.
- [ ] RR supplies concrete required values, suitable existing/missing-item cases and approved financial values; resolve only genuine ambiguities.
- [ ] No live job, unfinished operation lock, failed resource cleanup or unreconciled spending. Prior-run write reports are not prerequisites for a fresh RR. Record current ownership, exact target, allowance/reserve and cumulative cost before execution.

## SOW coverage, based on current code and evidence

Historical API write tests are not permission to change existing objects under the new policy. Read audits do not prove browser authoring. See `CVENT-API.md` and `CVENT-API-COVERAGE.md` for sources.

| SOW area | Current permitted route | Remaining acceptance/gap |
|---|---|---|
| Event identity/details | API read; event name immutable; production CLI rejects event-setting updates conservatively | Verify identity unchanged. Existing differing settings are exceptions, not automatic edits. |
| Theme/branding, header/footer/body widgets | Ego for documented API gaps, only permitted missing content | Existing branding/widgets stay unchanged. Prove a genuinely missing addition can be saved without changing existing items. |
| Website pages/navigation/content | Ego for documented API gaps | Missing-page/content creation, existing-page reuse, independent reopen/readback; modifying an existing page is not automatically allowed. |
| Registration types | API catalog read; existing types reused | Prove any event-local missing-type setup is possible without account-global definitions or editing existing items. Otherwise block. |
| Admission items | API read, Ego creation for documented API gap | Create missing item, reuse existing type/path references where allowed, verify saved fields and preservation. |
| Fees/pricing | API read, Ego creation for documented API gap | Create missing approved fees/tiers; verify exact amounts, dates, currencies and associations. No guessing financial values. |
| Discount codes | API reuse or complete, confirmed-missing creation | Mocked CLI creation/readback/retry/preservation tested. Live creation and financial fields remain unproven. |
| Volume discounts and discount-item associations | New named volume rules and new-code/new-volume initial admission/quantity links connected | Offline adapter/CLI coverage only; verify actual volume financial behavior and saved links in authorized live acceptance. No existing-discount or standalone link/update grant. |
| Registration paths | API read, Ego for documented API authoring gap | Create missing path/flow, reuse existing items, verify navigation and settings without editing existing paths. |
| Questions/choices | Event-bound question and choice API reads; Ego authoring gap | Prove missing questions/choices/logic and reuse without changing existing questions. |
| Optional quantity/donation items | Scoped catalog reads connected; Ego authoring gap | Verify permitted creation, pricing and selection rules. Donation linking is unsupported. Never use attendee quantity endpoints. |
| Vouchers | API read, Ego for documented API authoring gap | Missing-voucher creation and restrictions, existing-voucher reuse and independent readback. |
| Advanced rules/privacy/policy content | Ego for documented gaps; API-supported discount subset separate | Verify missing-rule creation and dependencies without overwriting existing rules/content or financial-account settings. |
| Event custom-field answers/integration identifiers | Read/reuse only under preservation; CLI updates blocked | Report differing answers; never change integration IDs or synchronization to satisfy a test. |

## Ordered implementation and verification

1. **Production API preservation gate — implemented, local verification.** CLI excludes legacy updates from allowed capabilities and rejects direct requests before network access. A before-dispatch check permits confirmed-missing discount POSTs and narrowly guarded same-command initial linking/finalization of the verified new object. Existing discount values never change; differences are returned and receipted as such. Newly created codes are reused on subsequent calls, not overwritten. Low-level legacy helpers remain outside the production CLI; this is not a general shell sandbox.
2. **Ego existing-object protection — pending.** Current guards enforce identity, ownership, prohibited actions and uncertainty, but do not prove object-level create-only behavior. Establish a concrete mechanism against observed Cvent workflows; do not claim generic DOM/button filtering can guarantee preservation. Keep the pinned skill and real native interaction, not replay automation.
3. **Create-only API expansion — implemented offline, live acceptance pending.** Code/volume creation, initial admission/quantity links, question choices and feature-status reads are connected with scope, preservation and failure tests. Map the approved RR to these exact schemas; unsupported eligibility stays blocked. Do not expand to unrelated APIs or exempt existing items from preservation.
4. **Supervised live acceptance — blocked by prerequisites.** Fresh upload and native session; local workbook reading before agent Cvent access; guarded login/event setup; API-first execution and Ego only for documented gaps. Stop on security, identity, ownership, uncertainty or budget conditions. Do not automatically start a second run.
5. **Human review and limited pilot — pending acceptance.** Inspect all meaningful changes and exceptions. A sequential local pilot is distinct from concurrent team deployment; authentication/access control, user/session isolation and sensitive-output protection remain deployment gates.

## Required acceptance cases and evidence

For each requirement, keep the RR sheet/cell reference, object identity, desired state, before-state/absence evidence, route and reason, action, independent saved-state evidence and final disposition in the existing route plan/receipts/final report. Use explicit dispositions: `VERIFIED_EXISTING`, `VERIFIED_CREATED`, `PRESERVED_DIFFERENCE`, `BLOCKED`, `NOT_APPLICABLE`, `UNTESTED` or `UNCERTAIN`. A preserved mismatch or unsupported dependency is not a verified requirement. Do not declare the whole SOW complete while required work remains blocked/unverified.

- [ ] Target-bound upload and Start Build launch no Pi process/RPC/prompt while waiting for login. One verified Return creates one fresh native session and execution prompt; no old conversation restoration.
- [ ] Agent understands the actual workbook's relevant sheets/dependencies before execution; no prescribed inventory tool or ledger schema. Ambiguous/missing ordinary values skip dependent work while independent requirements continue. Authorization/security/identity/uncertainty still pause.
- [ ] API/browser identity agree before mutations; workbook rename request cannot rename or switch the event.
- [ ] Login is human-only; no sensitive hidden/password/token inspection; takeover shields/ownership work.
- [ ] Existing identical item is reused with no write; existing differing item stays byte-for-byte/business-state unchanged and is reported as different.
- [ ] Missing item is confirmed against complete scoped lookup, created exactly once, independently reopened/read and verified.
- [ ] Duplicate/ambiguous identity or incomplete lookup blocks creation. A retry, stale read or later requirement never creates a duplicate or overwrites a prior creation.
- [ ] All required SOW areas above have real persisted-result evidence or an explicit exception; no untested area silently passes.
- [ ] Before/after inventories show only approved new records and necessary permitted relationships. Existing business values/event name are unchanged; normal audit metadata is distinguished.
- [ ] Stop/takeover before dispatch prevents a write. Interruption after dispatch leaves uncertainty and never causes blind replay or browser fallback.
- [ ] Authentication, permission, rate-limit, server and validation failures are not treated as unsupported API capabilities.
- [ ] Cost display and final ledger include prior spending, current session and external reserve; unknown billing or exhaustion stops safely.
- [ ] Original workbook, prior reports/receipts and uncertainty evidence are preserved; final report honestly distinguishes completion from partial results.

## Current evidence

- Earlier instruction/lifecycle/UI baseline: 98 passing tests, no full live RR acceptance.
- Preservation implementation: local test logs `logs/create-only-narrow-tests.log` and `logs/create-only-full-tests.log`; see the activation receipt referenced in `RPC-CONNECTION.md` for final count and preserved-file hashes.
- Read-only continuation audit reran the 138-test baseline successfully. Current discount-link writes remain mocked; no full RR acceptance exists.
- The privacy slice adds full runtime-tree Git exclusions, an index artifact-path check, payload-free SSE projection and denial of raw progress downloads. It preserves private native evidence on disk. A preservation-verified app-only restart activated it with the Stop/status fixes (151 offline tests; receipt in `RPC-CONNECTION.md`). It does not sanitize all assistant reports or supply authentication. Shared-ownership/cancellation and status regressions pass fixtures; live acceptance is still required.
- Clear/New RR and automatic workbook replacement pass the 164-test offline suite. Idle reset endpoint and updated assets were checked after an app-only restart; 253 private non-profile data files unchanged. Active reset/fresh-session/cumulative-spending checks use fixtures, not live acceptance.
- Upload preservation correction: 165 offline tests pass. Real rendered UI + local workbook backend tested with an isolated synthetic workbook (upload, target retention, reload, failed replacement and explicit Clear). Actual latest user workbook checksum/preview verified read-only. No live RR execution acceptance is implied.
- Browser failure circuit breaker: 173 offline tests pass. Known SDK errors trip after 3 failures/10 minutes; unconfirmed page execution stops immediately and blocks future runs through retained uncertainty. Recorded-error replay and a 288-file-preserved app-only activation passed. This does not prove live renderer repair, saved progress, or detection of successful but unproductive reading. Latest stopped run cost $5.594820 / event total $29.022338; saved-state review is still required.
- No paid prompt or Cvent mutation was used for this implementation. Historical incident acknowledgment is present (see prerequisite above). The mock workbook remains prohibited; no execution approval has been inferred.
