# RR connection to native Pi

## Scoped API writes and cost tracking — current

Executable API expansion is implemented, not prompt-only: selected-event basics/deadline/merged venue, event registration availability/dates/capacity, existing discount/volume values and additive links, Website/Registration enablement, plus shared definition reads. Shared/integration edits and unproven isolated shared creation remain blocked. See CVENT-API.md and CVENT-API-COVERAGE.md for exact fields, scope proofs and gaps. Durable intent, acknowledgment, independent saved verification, ownership checks at paced dispatch/completion, current-run uncertainty and no-replay remain. No second orchestrator or paid continuation.

User removed automatic dollar stops. Per-run costs, historical accounting/reset credits and two-second metering remain; unknown metering still stops. No accounting data/reset was changed. **An authorized app restart is required for the monitor/start-gate change; none was performed.** New CLI invocations load API changes directly. No paid build, browser launch, Cvent mutation, commit or push. CURRENT in MVP-HANDOFF.md records verification and residual live risks.

## Event-only edits and shared-object protection — prior prompt-only milestone

User clarified that all SOW-required event-only objects/settings/relationships may be created or modified, including existing admission items. Existing shared/account-wide objects stay unchanged; reuse an exact shared match or create a separate required shared build object if absent, without altering existing shared objects, global defaults or other events. Prove scope before writing; event location in the UI is not proof of isolation. No deleting/archiving anything. Draft, fixed event identity, human-only authentication, saved verification and uncertainty/Stop protections remain.

Implemented **prompt/guidance only** as requested: canonical task, API/evidence/Steel guides and linked scope documentation. **259 offline tests pass**, `logs/event-only-scope-prompt-tests.log`, including three new instruction-contract regressions and unchanged create-only capabilities/safety/native lifecycle tests. The CLI and browser guards are unchanged; explicit denials remain blockers, not authorization to bypass them through another route. Some authorized event-only edits therefore remain unavailable through the installed tools. Missing helpers and explicit denials are distinguished; no wrapper continuation, additional agent, mandatory ledger or new report schema.

Fresh Start captures the task without restart; historical jobs/scopes are unchanged. No paid run, Cvent mutation, reset, restart, commit or push. Full live execution/shared-object isolation and completed-RR cost are not established by these tests. See CURRENT in MVP-HANDOFF.md for the remaining tool-support decision and current accounting.

## Pricing and capability-stop clarification — prior

The native task now explicitly permits RR event prices, fees and discounts on separate compliant new objects and during initial configuration, while preserving existing objects and excluding merchant/payment-account, payment-processing, tax and currency changes. The Steel reference now treats known unsupported capabilities before possible execution as local requirement blockers; uncertainty after a possible write and all safety/operator stops still stop the entire run. No API operations, guard logic, RPC lifecycle, completion schema or automatic continuation changed. Original-object exceptions still require specific authorization.

**256 offline tests pass**, `logs/pricing-capability-guidance-tests.log`, including pricing-scope and pre-execution-gap versus uncertain-write instruction contracts. This is not live model/capability acceptance. New Start captures the prompt without restart; the current tool guides load when read. No paid run, Cvent action or reset. See CURRENT in MVP-HANDOFF.md.

## Event-build SOW completion — retained behavior

DONE now explicitly concerns the approved event-build scope: website/theme/header/footer/widgets, registration, admission items, pricing, paths, optional items, vouchers and advanced rules, with applicable RR values and necessary dependencies implemented, connected, verified and saved as Draft. Unrelated RR requirements and whole-project acceptance do not block this per-run result. Pi reports exclusions separately, never disguising an in-scope blocker as excluded. Scope/preservation guards and the one-session native lifecycle are unchanged; no semantic filtering of blockers, extra report schema or automatic reprompt was added.

**254 offline tests pass**, `logs/event-build-sow-tests.log`, including SOW guidance and completion-result regressions. These do not prove live scope classification or end-to-end execution. Fresh Start captures the task from disk without restart. No paid run, Cvent action or budget change. See CURRENT in MVP-HANDOFF.md.

## Native Pi execution guidance — retained behavior

After an inspection-only live run ended INCOMPLETE, corrected the existing native task and tool guides rather than changing RPC or settlement. Pi must distinguish executable missing dependencies from observed blockers, continue independent requirements, interpret literal values separately from embedded RR instructions, and assess unfinished work before ending. Capability gaps do not establish platform impossibility; forbidden operations remain forbidden. Specific permission exceptions are reported, not self-authorized. No required ledger, reviewer, new tool, approval endpoint or automatic paid reprompt.

**252 offline tests pass**, `logs/native-execution-guidance-tests.log`, including four new instruction-contract regressions and existing one-session, settlement, preservation and cleanup tests. No runtime/API/browser guard changes. Fresh Start captures the updated prompt; no server restart is needed. These tests do not establish live model compliance or end-to-end completion. No paid run, Cvent mutation or budget reset was started for this correction. See CURRENT in MVP-HANDOFF.md.

## RR-only efficiency — retained behavior

The canonical task now executes only requirements in the fresh RR and their necessary dependencies; category coverage is not permission to add unrequested widgets/options. `rr-evidence` reuses a verified job-local extraction, preserves cell styles and can compare explicitly mapped RR/catalog fields with exceptions-only output. Full source evidence and original-workbook inspection remain available; unsupported visual/derived semantics are not guessed. Human updates amend ongoing execution rather than asking for an acknowledgment-only ending. This is prompt guidance, not a new native continuation loop or a guarantee. The one-session/one-launch-prompt lifecycle and all cleanup/safety/accounting gates are unchanged.

**248 offline tests pass**, `logs/rr-efficiency-tests.log`; synthetic 400-row field comparison prints under 2,500 characters while retaining every row/value. Future fresh Start loads the task and CLI changes without server restart. No paid execution, live Cvent action or budget reset. See CURRENT in MVP-HANDOFF.md for evidence and limitations; complete RR cost remains unproven.

## Thin native-Pi wrapper — retained behavior

One path: upload locally → Start clean Steel → human login → verified Return → fresh Pi via RPC → execute/verify → DONE or honest INCOMPLETE → stop Pi and Steel. Interruptions and failed cleanup remain STOPPED. Removed legacy direct execution, paid RR intake/clarification/configuration transitions, idle prepared-session replacement and the unused session-restoration helper. `/read` and `/answer` keep their dashboard-compatible names but now only start the browser and accept human Return; `/start` and `/continue` only reject. No alternate mode activates when a browser provisioner is missing.

`app/rr-connection.mjs` is reduced from 654 to 445 lines, with exactly one execution-prompt site. Native fresh-session checks, transport, tools, scope, spending, Stop, current-run uncertainty and Steel cleanup remain. Historical artifacts stay unchanged and never become execution tasks. **240 offline tests pass** (`logs/thin-wrapper-tests.log`); legacy-only tests were replaced/migrated to the sole login-first path. Live completion and cost remain unproven. See the CURRENT handoff for activation status.

## Fresh RR correction — retained behavior

Every successful Return launches directly in EXECUTING with one fresh empty Pi session and the new workbook/live saved event state. No prior-evidence pointer or recovery prompt is sent. Prior-run browser/API uncertainty no longer blocks any execution entry path. The historical-uncertainty gate, operator-decision requirement, recovery modules/prompt, request fence, promotion command/route and API/Ego legacy flags are removed. Historical reports and costs remain evidence/accounting, not model task inputs or prerequisites.

Current-run uncertain writes still stop their own run without replay or false DONE. Live processes, unfinished locks, failed cleanup, unknown spending, identity, security, human login, ownership and Steel teardown guards remain. Fresh does not reset/delete/clone saved Cvent configuration. Old UI rejections are labelled retired rather than presented as current prerequisites. Historical notes below describe superseded behavior, not runtime instructions.

Offline verification and activation details: see the CURRENT section of MVP-HANDOFF.md. Full RR acceptance and the $60 completion target remain unproven; no paid test is authorized by maintenance.

## Fresh-session recovery instead of a blocked Return — superseded/rejected

Production login-first Return now distinguishes recoverable prior browser uncertainty from hard process/spending/security/identity/API-lock blockers. A verified fresh session can start in RECONCILING with a persistent app-owned browser request fence and API/Ego authoring guards. Native Pi independently reads saved state and submits source/event-bound receipts via `bin/rr-reconcile`. Original values differing from recorded baselines require explicit restoration approval; no repair, replay, delete or publish is granted. Valid complete evidence is recorded separately from immutable history, the fence is released and the same single prompt/session continues the new RR. No automatic follow-up prompt or old conversation restoration. Unfinished reconciliation cannot pass DONE; fence failures/Stop races fail closed. POST-based browser reads may be blocked; this is not full live Cvent acceptance or a hostile-shell sandbox.

**257 offline tests pass** (`logs/reconciliation-tests.log`), including fresh session/one prompt, write blocking, preserved history, repeat-upload resolution lookup, evidence integrity, incomplete/changed-original rejection and Stop/setup/promotion races. Activated app-only **80905 → 10547** with original environment retained in memory. Login-wait job 04241500-83ca-44dd-9e65-b8ebf8a655fd was safely stopped at $0 before restart. `logs/reconciliation-activation-k71gp32r/receipt.json`: 557 current non-profile files unchanged after Stop, no added data files, USER ownership, stopped browser, unchanged accounting reset/$0 usage. No paid test or Cvent configuration action. Fresh Start/login/Return required; historical footer/Facebook uncertainty remains to be verified through the new path.

## Accounting reset and focused native execution — latest

Pre-change checkpoint **63a8c64** pushed first. User-authorized operator-only credits now exclude captured historical native costs from future spending gates without rewriting jobs or bypassing event uncertainty/process checks. All three entry paths (login-first, legacy start and legacy intake) use the same credit calculation. New charges still accumulate under the unchanged $60 allowance/$10 reserve; Clear/New RR never resets accounting. UI distinguishes post-reset usage from the selected historical run. No model-facing billing coaching, automatic reset or paid prompt was added.

Execution remains one native Pi task: compact source-referenced Python extraction, supported API calls and focused Ego observations; full receipts remain on disk, no truncation/interception layer. Current instructions discourage repeated unchanged snapshots and broad catalog dumps while retaining complete lookup, state-transition checks and saved verification. No new planning/audit framework. **242 offline tests pass**, `logs/budget-simplification-tests.log`; live savings/completion remain unmeasured.

Activated app-only **58367 → 80905**, preserving launch environment in memory. Receipt `logs/budget-simplification-activation-_mcca7fe/receipt.json` verifies 531 current protected data files unchanged, only the two accounting files added, $0 current usage, $50.912284 historical costs retained across 25 jobs, USER ownership and stopped browser. Shared-Facebook/footer uncertainty remains unreconciled; resetting money does not authorize or unblock uncertain execution. No new paid run/Cvent action. Changes after the pushed checkpoint remain local.

## Steel teardown policy — latest

User authorized stopping all running Steel environments after confirmed Docker OOM kills; 13 were stopped without deleting profiles/evidence. Each job now stops its provenance-checked Steel container on native settlement, Stop/Clear, failure, failed provisioning and graceful app shutdown. Pending provisioning settles before cleanup, preventing Stop/start races; cleanup verifies process exit by immutable ID. Failures remain durable STOPPED cleanup errors, not successful completion. Viewer shows Browser stopped after verified cleanup. No retry/model turn is added; uncertainty and spending are preserved. **235 offline tests pass**, `logs/steel-lifecycle-tests.log`; emergency cleanup receipt `logs/steel-lifecycle-emergency-stop.json`. Activated with preserved original environment, app PID **58367**. `logs/steel-lifecycle-activation-osemb0th/receipt.json` confirms 2,067 protected non-profile files unchanged, zero running Steel containers and a verified stopped-viewer response; no paid run or Cvent mutation. Hard process/host kills or Docker failure require manual reconciliation; stopped profiles/container filesystems remain on disk.

## DONE policy activation — prior

User explicitly authorized restart. Idle app-only restart **71626 → 45724** on loopback port **8788** loaded DONE/INCOMPLETE/STOPPED and the earlier exact-match/separate-creation changes. The original launch environment was reused in memory without logging/persisting its values. Browser/profile and USER ownership stayed untouched; no new job, Pi prompt, Cvent operation or security attestation.

`logs/rr-done-activation-631hnvkl/receipt.json` confirms **2,005 protected non-profile data files unchanged**, unchanged global recorded Pi spending **$39.386908**, login-first runtime health, no active jobs and served JS matching source. Existing **211 passing offline tests** remain valid. Historical reports/scopes/costs remain immutable. Refresh the app; full live RR acceptance is not inferred from restart or tests.

## DONE requires full scoped RR configuration — implementation before activation

The user's confirmed roadmap scope is now explicit in the 362-word native task: website/theme/header/footer/all six body-widget types, registration/admissions/pricing/paths/optional items/vouchers/advanced rules, and connected dependencies, saved and verified in Draft. All RR-required elements must be satisfied; exact existing matches count. Missing, blocked or unverified work is INCOMPLETE. Native exit is not completion, and there is no mandatory review phase.

`agent_settled` still waits for native continuation/compaction to finish. Normal cleanup then interprets the existing final-report.json: bound eventId, status DONE, completion.website/registration/dependencies/draft all strictly true, blockers/untested empty, no retained uncertainty/operation locks. Otherwise INCOMPLETE. Interrupted runs and cleanup/spending/API/tool uncertainty remain STOPPED. The connection does not invoke another agent, audit, paid prompt, retry or API/browser call. It checks explicit result consistency, not independent semantic correctness of Pi's saved verification. Missing or malformed results cannot silently pass. README.md documents the small result shape.

Job, state, result and public event expose DONE/INCOMPLETE/STOPPED; old FINISHED/REVIEW_REQUIRED UI labels become INCOMPLETE without rewriting history. Tests cover successful DONE, ordinary exit without a report, malformed/partial/contradictory claims, exact event binding, cleanup failures, ownership and legacy display. **211 offline tests pass** (`logs/rr-done-tests.log`); syntax/whitespace/repository checks pass. No live run or restart. **Controlled app-only activation remains pending; preserve the existing server's credential/model launch configuration.**

## Exact-match execution and native terminal states — prior source-only update

User now authorizes separate RR-compliant objects whenever an existing object differs, even by spelling/number/format, with originals preserved. The 322-word task requires execution, exact reuse/new creation and saved verification, not review. Pi owns the plan; no required ledger, extra validator, reprompt or workflow engine.

`agent_settled` leads to FINISHED after clean Stop/process/ownership cleanup. Interruption, cleanup failure, invalid progress state, unresolved tools/API writes or unreconciled spending yield STOPPED. `result.json`, job/state and public event use the resulting lifecycle status; UI no longer injects a review demand. Historical status labels are mapped for display without modifying old records. FINISHED does not certify all RR requirements; the native final reply retains actual results and blockers. Security incidents and uncertainty still require their existing resolution, not an automatic retry.

206 offline tests pass (`logs/exact-rr-execution-tests.log`), including no second prompt, ownership release, unresolved/failing natural settlement, exact-match preservation and variant creation. API details and same-identity authoring limits are in CVENT-API.md. No live run/API/browser action, server restart, commit or push. **Controlled app-only restart is still required to load the connection change.** Fresh approvals capture the new prompt; old prompts/jobs/costs remain intact.

## One native task, no audit workflow — prior update

At the user's direction, the canonical task is now **296 words**, a direct request to execute the uploaded RR in the authorized Cvent event with Ego/assigned local Steel while preserving existing objects/name and prohibited-action boundaries. No required section checklist, ledger schema, evidence-tool sequence or completion audit. Pi chooses its own plan and working notes; compact evidence remains optional. Supported API operations and all existing app safety/identity/ownership/spending gates remain.

Removed connection-side audit invocation/metadata plus its unused wrapper and tests. The connection is byte-for-byte restored to the saved pre-audit source; native `PiRpc`, tools/system prompt and compaction/retry lifecycle are unchanged. App settlement uses `agent_settled` (not intermediate `agent_end`), saves the final reply as REVIEW_REQUIRED and releases ownership through the existing Stop path. No added prompt/continuation/validator loop.

203 offline tests pass (`logs/native-task-simplification-tests.log`), including one prompt after human handoff, preservation, intermediate native retry/compaction events, and settlement without requirements.json. No restart, live execution, Cvent/API/browser operation or new paid prompt. Fresh approvals capture the current task; prior captured scopes remain unchanged. Earlier source-only audit activation guidance below is superseded. This simplification is not full live RR acceptance.

## Local completion audit — prior source-only update

`rr-evidence audit` checks a small source/event-bound requirements ledger before finishing; number-format distinctions now survive compact grouping/previews. The canonical task is 547 words. `rr-connection.mjs` also invokes the local audit once at natural settlement, bounded to 10 seconds, retaining only fixed summary fields in job/result metadata. Missing/failed audits remain incomplete/unavailable; structural accounting still requires semantic/saved-state review. REVIEW_REQUIRED, Stop/USER ownership release, spending gates and no automatic continuation remain unchanged. No model prompt is added by settlement. See `RR-EVIDENCE.md` for schema and limits.

205 offline tests pass (`logs/simple-fixes-final-tests.log`), including missing-ledger settlement without another prompt, original preservation, format distinction, source/sheet/evidence validation, audit failure and local installation checks. `npm run check:installation` performs only local prerequisites; configurable external-client roots retain existing defaults. No live execution or restart was performed. **CLI/prompt changes load for fresh invocations/approvals; automatic connection-side audit requires a controlled app-only restart.** Historical jobs/prompts are untouched. Fresh-install and full-RR acceptance remain unproven.

## Compact local evidence and completion accountability — latest

Added `./bin/rr-evidence` for bounded all-sheet/cell views and scoped local catalog identity comparisons. Full source-addressed cells and every row/ID match remain on disk, with explicit continuation and no satisfaction inference. No new API calls or authoring grants. The canonical prompt now has **524 words** and directs compact evidence use, a source-referenced requirement ledger and specific blockers for untested work. Fresh approvals pick it up without restart; no historical SOW changes. See [RR-EVIDENCE.md](RR-EVIDENCE.md).

**195 offline tests pass**, `logs/compact-evidence-final-tests.log`. Repeated-row term grouping reduces the 655 discount term rows to 19 patterns with full source membership retained. Copied CED261 evidence replay retained 21 sheets/18,230 cells and 655 discount comparisons; 649 existing/six missing identities returned in 1,392 characters. This proves output reduction, not full execution or 90-minute acceptance. The live run settled review-required at $6.065026 without API write intents; existing conflicts, incomplete inputs and untested behavior remain. Project Ego inspection was attempted but rejected by the USER ownership/Return gate; no bypass or new paid run. App PID remains 71626; no restart.

## Create-only API expansion — prior update

Added `configureVolumeDiscount` for confirmed-missing named volume rules, including initial admission/quantity links through the existing guarded transaction lifecycle. Existing rules and associations remain immutable; all steps preserve intent/uncertainty until independent verification. Added event-bound `listQuestionChoices` and read-only `listEventFeatures`. See `CVENT-API.md` for exact inputs and `CVENT-API-COVERAGE.md` for the public write-family audit and exclusions.

The route map is now **474 words**; no added financial coaching or rigid workflow. **188 offline tests pass** (`logs/api-expansion-full-tests.log`), including production-CLI preservation, durable multi-step failure/no-replay and read-scope tests. No paid execution, live API request, Cvent mutation or browser operation. No app restart: CLI subprocesses load current adapter source and new approvals read the live prompt getter; PID **71626** reverified. Earlier captured SOWs/history remain unchanged. Live volume financial behavior, browser reliability and full RR acceptance remain unproven.

## Concise routes; billing stays app-side — prior update

The canonical prompt is now **473 words** with a compact capability map: supported event/catalog reads via API; `configureDiscount` is the only production API write (confirmed-missing supported final-total/item-scoped codes); assigned Ego handles documented API-unsupported work. Unsupported integrations/financial semantics are gaps, not blanket browser permission. Pi chooses work order and consults only relevant docs as needed. The map is regression-checked against local CLI capability output.

`executionPrompt()` no longer injects allowanceUSD, targetCostUSD, externalCostReserveUSD or priorEventCostUSD. The task contains no dollar target, budget coaching, instruction to initially read job.json, or spending-report requirement. **Server-side accounting, cumulative threshold, reserve and reconciliation remain unchanged**; this is not unlimited-spend authorization. Local historical evidence/job ledgers retain financial data and may still be encountered by the model. Native tools, browser gates, preservation and upload/preview flow are unchanged.

**177 offline tests pass**, `logs/concise-routing-full-tests.log`, including no financial fields in real login-first/legacy prompt fixtures and unchanged cumulative budget Stop. Syntax/whitespace/artifact-path gates pass. Idle app-only restart **17768 → 71626**, port 8788: `logs/concise-routing-activation-2026-09-20T19-09-02-758Z/receipt.json`, **288 non-profile files unchanged**, USER ownership and **$30.122350** recorded global cost unchanged. No new paid run, browser/profile operation or Cvent mutation. Earlier captured scopes/history remain unchanged. No cost-per-completed-RR or live reliability claim follows from these offline tests.

## Section-based task and isolated page crash — prior update

The canonical prompt is now **526 words** (previously 651), with seven explicit configuration sections and section-level verification/exception reporting. Section values and applicability come from the actual RR; the list is not a fixed work order, permission to modify existing items, or a requirement to force writes. The UI/upload/target/preview flow, native Pi launch and all app guards are unchanged. `RUN_POLICY.approvedSow` reads the prompt at fresh approval, so this prompt-only edit requires no process restart and does not alter historical captured SOWs. **176 offline tests pass** (`logs/section-prompt-full-tests.log`).

The stopped run's Steel logs provide a new concrete fact: **CDPService/Puppeteer emitted `Page crashed!` at 04:15:38.906Z on 2026-09-20, for the exact assigned page target**. The tool beginning at 04:15:38.243 clicked observed Settings ref 30100 then requested a snapshot. Accessibility.enable timed out at 04:15:58.835, then URL evaluation and reload failed. This establishes a page crash before the later timeout/recovery loop, not the underlying crash trigger: click-related application work versus accessibility/snapshot instrumentation still needs controlled isolation. No claim that simpler prompting fixes Chromium.

Private evidence: `logs/hang-isolation/diagnosis.json`, container log window and sanitized timeline. No container restart/OOM-kill flag was recorded, but current resources and retained Docker events cannot rule out a renderer-only OOM or identify crash-time pressure. No browser/ownership/profile change, live page probe, Cvent mutation or model prompt was performed. Browser remains USER-owned; a controlled reproduction must not bypass that gate or replay uncertain actions.

## Simplified native execution — prior activation

`app/runner-prompt.md` is the single canonical task/SOW. `RUN_POLICY.approvedSow` reads it for each newly approved job; `executionPrompt()` sends that captured scope once plus current workbook, verified target and cumulative budget inputs. No duplicate JSON SOW or login-first workflow appendix. Retained legacy execution entry points use the same builder; their same-session intake lifecycle remains fixture-covered, not re-enabled in production.

Pi retains its built-in system prompt, tools, extensions and compaction. `--no-context-files` and the explicit pinned Ego skill remain; `--append-system-prompt` is removed and `app/runtime-policy.md` is now a historical pointer. Removed fixed route-plan schemas, forced API-write ordering, operation-specific recipes already in API docs and the one-observation/one-retry rule. Source coverage, saved verification, reports and authorization boundaries remain. Ordinary selector/stale-ref failures no longer count toward the global breaker; **3 recognized unresponsive-browser failures in 10 minutes** still Stop, and recognized unconfirmed execution still stops immediately with durable uncertainty. This does not fix renderer/CDP hangs or guarantee productive model behavior.

Upload/preview/target retention, Start/login/Return, Clear, Stop, API/ownership/identity, uncertainty and cumulative spending gates are unchanged. No new paid run or live Cvent mutation is authorized by this maintenance. Create-only remains both explicit policy and guarded API behavior; browser-object/shell enforcement remains incomplete.

**175 offline tests pass** (`logs/prompt-simplification-full-tests.log`), including single captured prompt, unchanged upload/login/Return lifecycle, recoverable selectors, renderer failure Stop, immediate uncertainty retention and legacy continuation. Syntax, whitespace and repository artifact-path gates pass. Idle app-only restart **27024 → 17768** on 8788 activated the changes; receipt `logs/prompt-simplification-activation-2026-09-20T16-53-46-667Z/receipt.json`: **288 non-profile files unchanged**, USER ownership, global recorded cost **$30.122350** unchanged. No paid prompt, new RR job, browser/profile operation or Cvent mutation. Changes remain local/uncommitted; no full RR acceptance claimed.

## Bounded browser-failure recovery — historical activation (selector quota superseded above)

On user approval, safely stopped run `ca3dfacc-a81a-47c0-8b8c-53152c1d32db`: **$5.594820** session / **$29.022338** event cumulative, no native Stop failures or unreconciled spending. Diagnosis found ambiguous/missing selectors followed by unresponsive accessibility/page-info/reload/CDP operations, plus an overly repetitive dependency ledger. Nine successful API reads are evidence of inventory, not full execution. Do not infer absent browser changes merely from absent API-write receipts; this stopped run still needs saved-state review.

`app/browser-failure-guard.mjs` observes failed native bash results locally and projects only fixed category/count/reason metadata to the job ledger. **3 recognized failures in a rolling 10-minute window** trigger the existing Stop path; intervening successful snapshots do not reset the count. Recognized unconfirmed page execution triggers immediate Stop and retains its tool identity in unresolved changes, blocking fresh event execution through the existing history gate. Guards are per live job; no prompts, retry, successor or browser reset are dispatched. Signatures are specific to known SDK errors, not arbitrary-error detection, a browser sandbox, a verified-progress meter or a detector of all wasteful reads.

Runner/runtime and bridge instructions now require observed unique selectors (no guessed IDs), one corrected read-only recovery attempt per affected step, no automated reload/replacement after unconfirmed execution, python3, and grouped dependency reporting without dropping real populated RR identities. These are instructions, not proof the model will always comply. Underlying Cvent renderer reliability remains unproven; no live repair was attempted.

**173 offline tests pass**, `logs/browser-failure-full-tests.log`. Tests cover rolling windows, nonconsecutive failures, duplicate events, private-payload exclusion, immediate uncertainty Stop, native ownership/cost cleanup and blocking a fresh run with uncertainty. Read-only replay of recorded failures would first trip at **04:15:58.835Z**; `logs/browser-failure-diagnosis.json`. This replay is not a new execution or saved-state verification.

Idle app-only restart **11071 → 27024** on 8788 activated the guard. Receipt `logs/browser-failure-activation-20260920T042415Z/receipt.json`: **288 non-profile data files unchanged**, USER ownership, global recorded Pi cost **$30.122350** unchanged after Stop. No new paid prompt/build, browser/profile operation or Cvent mutation during implementation. Historical records were not rewritten. The stopped conversation cannot resume; any later acceptance needs reviewed saved state and a fresh explicitly authorized run.

## Upload preservation correction — prior activation

The user reported uploads apparently losing all input. The latest actual workbook was saved (checksum verified; 20-sheet preview HTTP 200), but automatic reset erased the target/file picker before confirming its replacement. Corrected `public/app.js` and `public/index.html`: another Excel retains the entered target and only replaces the selected workbook after successful save/preview; failures retain the prior workbook, edits and selection. The filename stays visible with a persistent `Saved: filename` label even after reload. Only explicit CLEAR / NEW RR blanks the form. Completed Stop is never undone by a failed upload.

**165 offline tests pass**, `logs/upload-preservation-full-tests.log`. A real terminal-browser test against an isolated temporary server with synthetic workbooks verified upload/target preservation, saved-file display, reload restoration, failed replacement retention and explicit Clear. No Cvent browser was connected to that test; its browser and server were closed. `logs/upload-preservation-receipt.json` records served updated assets, unchanged app PID **11071**, USER ownership and no active jobs. Static assets required no app restart. No paid prompt, build, live Cvent mutation or original-workbook change; full live RR acceptance remains unproven.

## Clear / New RR — prior activation (upload clearing corrected above)

**CLEAR / NEW RR** sits directly below the Excel upload. Selecting another `.xlsx` uses the same reset automatically before local preview. Active-run/unsaved-edit replacement requires confirmation. `POST /api/new-rr` checks the selected owner, serializes against Start/Return/upload, stops active native execution or releases a legacy prepared session, refuses unsettled durable jobs/cleanup failures/unreconciled spending, and never creates a replacement Pi process. Unstarted jobs explicitly cleared become `CLEARED`, not retryable. Originals, saved versions, reports, receipts, browser profiles, uncertainty and cumulative costs remain intact.

Only confirmed reset clears the form/preview/target/answers/security checkbox and sessionStorage selection. Stale polling or startup workbook restoration cannot repopulate it. Clear does not reset browser login or revoke any security incident acknowledgment. The next explicit Start + verified Return creates a new empty Pi conversation; prior evidence/cost accounting remain, but old conversations are not restored. Reloading the page alone is not a context reset.

**164 offline tests pass**, `logs/new-rr-full-tests.log`, including reset exclusivity, foreign-job protection, cleanup failure, legacy prepared-session release, zero-prompt reset, distinct next session, retained event cost, confirmation and stale-restoration regressions. App-only restart **6009 → 11071** on 8788; receipt `logs/new-rr-activation-20260920T034958Z/receipt.json`. All **253 non-profile data files unchanged**, ownership USER, global recorded Pi cost **$24.527530** unchanged. New HTML/JS and an empty idle reset endpoint were checked live. No browser/profile operation, new build, paid prompt or Cvent mutation; real active-run acceptance is still pending.

## Stop/shared ownership, truthful status and privacy — prior activation

The user requested fixes before manually uploading/naming and starting a monitored run. Stop now revokes job and matching shared ownership before and after native cleanup, without touching a replacement browser. Atomic identity/ownership transitions and cancellation checks prevent delayed Return from restoring control. Stop stays actionable during pending Start/Return; preparation failure cannot overwrite a stopped status. Cleanup failure preserves warnings/uncertainty rather than claiming success.

Execution clears stale setup/security messages and exposes bounded timestamped, payload-free tool activity (100 entries). Tool completion and agent-reported checkpoints are explicitly not independent saved-result verification. Settled jobs show review status instead of stale running actions. This is metadata activity, not complete per-requirement coverage reporting.

**151 offline tests pass**, `logs/stop-status-full-tests.log`; 34 focused tests in `logs/stop-status-focused-tests.log`. Identity, provisioning, native preparation, Stop, cleanup failures and UI races use fixtures; full live RR acceptance remains unproven.

Verified idle app-only restart **66266 → 6009** on port 8788 activated these changes and the prior privacy slice. Receipt: `logs/stop-status-activation-20260920T033755Z/receipt.json`. All **253 non-profile private data files** matched preservation hashes; browser/profile not restarted or inspected, ownership remains USER, recorded global Pi cost remains **$24.527530**. Updated JS is served and raw progress download returns HTTP 409. No paid prompt, live Cvent mutation, login, incident attestation or new build was initiated. User will supply RR/target and explicitly start; do not execute the prohibited mock or infer bounded live authorization from this maintenance.

## MVP privacy audit — historical source-only checkpoint (now activated above)

Read-only baseline reverified 138 passing tests, no active jobs/operation locks/live recorded Pi processes, and USER ownership on existing app PID 66266. Historical incident acknowledgment is present; older unresolved-incident assertions below are superseded by the explicit acknowledgment, not by an automated revocation claim.

The bounded privacy slice passes **140 offline tests** (`logs/mvp-privacy-full-tests.log`). Raw `progress.jsonl` remains private durable evidence but is no longer an HTTP-downloadable result in the updated source. SSE exposes only allowlisted lifecycle/tool metadata, never native payloads, arguments, results, reasoning or extension dialogs. Git excludes all `data/` and `logs/` artifacts; `npm run check:repository` checks indexed artifact paths, not content/history secrets. At that audit, source, docs and tests were unstaged except the pre-existing submodule entries. The subsequent user-approved private source checkpoint is documented in `README.md`; it does not activate the runtime changes or constitute release acceptance.

**No server restart, paid prompt, browser operation or Cvent mutation occurred.** The running server still has the old HTTP behavior until an ownership-verified restart. Assistant replies/reports and job state remain private trusted-local data, not generally sanitized content. Stop/shared-ownership, stage accuracy, portable installation, browser preservation and full RR acceptance remain open. There is no multi-user readiness claim.

## API writes before browser work — ACTIVE, 138 offline tests passed

At the user's request, stopped `88758a4b-46bf-4bf8-a80a-b422ddbf0b22`: STOPPED_REQUIRES_REVIEW, $5.682544 run cost / $23.427518 target cumulative, no Stop failures or unreconciled spending. Its API reads passed, but item-scoped discounts were blocked by a genuine missing association-write integration. Do not describe the earlier read-only inventory as full API-write coverage.

`configureDiscount` now supports one command for confirmed-missing item-scoped code creation: complete scoped admission/quantity lookup, inactive POST, verified new-code-only item links, finalizing PUT, and complete readback. All steps retain one durable uncertainty record. No standalone update/link grant was added; existing codes and associations remain unchanged. Added quantity/donation catalog reads and explicit machine-readable write capabilities. The runner prompt requires per-requirement read/write routing and eligible independent API writes before unrelated browser work. Volume discount writes and arbitrary eligibility semantics remain gaps, not browser fallback permission.

Verified **138 passing offline tests**, `logs/api-item-writes-full-tests.log`; mocked tests include the production CLI and multi-step durability, preservation, Stop/takeover and no replay. **Current app PID 66266 on 8788.** Activation receipt: `logs/api-item-writes-activation-20260920T015938Z/receipt.json`, 135 preserved file hashes. First restart 55122 → 65715 exposed a stale AGENT ownership flag after Stop; no active jobs/processes remained. Explicit Take Control restored USER, then the preservation-hashed restart 65715 → 66266 passed. No browser restart/logout, live Cvent mutation or paid build occurred. API capability readback and login-first mode verified; no active job remains. Refresh and upload freshly for another user-authorized run; do not resume the stopped job. Real item-discount creation remains unvalidated against Cvent; no live acceptance claim.

## Clean browser / login before AI — ACTIVATED

See **CLEAN-BUILD-PLAN.md** for implementation, evidence and activation steps. On approval, stopped `b8ba480a-ffab-47fb-bf2a-016f5c234cb6` through the app: STOPPED_REQUIRES_REVIEW, no cleanup failures/unreconciled spending, $0.857928 final run cost / $17.979042 cumulative at that point. No browser reset or logout occurred.

Production source now provisions a genuinely empty per-job Steel browser on Start Build, performs zero Pi launches/RPC/prompts during human login, and starts one fresh Pi session only after explicit verified Return to Agent. It retains API/event/incident/budget/uncertainty checks and source-referenced incremental RR reads. Existing Ego handles narrowly guarded fresh-browser bootstrap. **129 tests pass** (`logs/login-first-full-tests.log`); isolated real Docker/Ego bootstrap probes passed without paid prompts or authentication actions. Their specifically owned containers were stopped, profiles/evidence retained; the assigned Cvent browser was untouched.

**Activated on explicit user restart authorization**, app PID **55122** on 8788 (previously 36470). The newer waiting run `8e34cbc8-a107-4e84-adc0-b6a78ca22816` was stopped through the app first: $0.865944 final run cost, no stop failures or unreconciled spending. Preflight confirmed no remaining active job, live recorded Pi process or operation lock. Receipt `logs/login-first-activation-20260920T013425Z/receipt.json` records 129 passing tests and 50 preservation hashes. `/api/runtime` now reports `loginFirst: true`, static assets match tested source, and no active jobs remain. Browser/profile and USER ownership were preserved; no browser reset/logout, paid prompt or Cvent mutation was performed. Recorded cumulative Pi cost is $18.844986. Refresh and upload freshly to use the new flow. The historical security incident still needs human acknowledgment; a fresh browser is not revocation. No live human login or paid full-RR acceptance of the new lifecycle is claimed.

## Single ownership toggle — current UI contract

The user requested one state-dependent button. The separate Take Back Control button is removed. When the browser is AGENT-owned, **TAKE CONTROL** invokes the existing operator Stop/handoff. When USER-owned, that same button reads **RETURN TO AGENT** and uses the guarded same-live-job setup answer. Successful ownership changes update the label in both directions. Pending states disable duplicate actions; security confirmation, target verification and stopped-job non-resumption remain enforced. A stopped build still needs a fresh upload before Return to Agent becomes available; this is not a pause/resume change.

**121 tests pass** (`logs/control-toggle-full-tests.log`), including both label transitions and endpoint directions, setup-only return, explicit human attestation and failure shielding. This is a static UI update, not a server restart or live handoff. Refresh the app to load it. The earlier control-direction iterations below are historical and superseded.

## Control direction corrected — prior two-button iteration

The user clarified that **TAKE CONTROL means the agent takes the browser**, not the operator. It now invokes the same guarded `answer(true)` action as I've Signed In / Return to Agent, scoped only to the live job waiting for setup. It never calls the operator takeover endpoint. The separate **TAKE BACK CONTROL** button retains operator Stop-and-handoff behavior (or simply focuses an already USER-owned browser). The viewer shield stays in place during agent handoff; failed or still-blocked setup is shown beside the viewer rather than falsely claiming agent ownership. An outstanding security incident requires the existing explicit, unchecked-by-default human attestation; neither button supplies it automatically.

**121 tests pass** (`logs/agent-control-full-tests.log`), including both directions, duplicate-click protection, no takeover for non-setup/stopped jobs, and security denial/result reporting. Static asset HTTP hashes match the changed `public/{app.js,index.html,styles.css}`. Desktop TaskSpace 48 visually confirmed both buttons after an outer app refresh and was handed back to the user; neither handoff button nor any Cvent control was clicked. The active job `b8ba480a-ffab-47fb-bf2a-016f5c234cb6` remains RUNNING / AWAITING_INPUT / setup with the unresolved human security-attestation gate. No server/browser restart, incident clearance, paid prompt or execution resumption was performed by this UI change. Live successful agent handoff remains pending human security confirmation. Historical control-button sections below are superseded where their direction differs.

## Browser control button clarity — served UI update

**Label correction:** On the user's request, the button now consistently reads **TAKE CONTROL** (or TAKING CONTROL… during a pending handoff), never FOCUS BROWSER. USER-owned clicks still focus the existing viewer without stopping/resuming a job; non-USER clicks retain the guarded takeover. The 10 focused UI tests pass (`logs/take-control-label-tests.log`). Authorized inspection of desktop TaskSpace 48 confirmed the corrected label in the rendered app after an outer-page refresh; monitoring control was handed back afterward. No Cvent controls or build buttons were clicked. Job `b8ba480a-ffab-47fb-bf2a-016f5c234cb6` was active during this label-only change and was not interrupted. The paragraph below records the preceding UI revision.

The user reported Take Control doing nothing. Runtime was already USER-owned with no active job; the UI disabled Take Control in that state without explaining why. `public/app.js` now shows an enabled **FOCUS BROWSER** button for USER ownership, scrolling/focusing the existing viewer without Stop, resume, browser reload or other POST. For non-USER ownership, **TAKE CONTROL** retains the existing Stop-and-handoff endpoint, prevents duplicate requests, keeps the interaction shield in place until settlement and displays status/errors next to the browser (`public/index.html`). Existing submitting/human-login gates remain unchanged.

**118 tests pass** (`logs/browser-control-full-tests.log`), including USER focus without POST, double-submit/pending-handoff shielding and failed-handoff feedback. HTTP readback hashes confirm both updated static files are served by the existing app PID `36470`; no restart, paid prompt, browser reset or Cvent mutation occurred. Refresh the app to load the new UI. These checks verify UI wiring; they do not prove live Steel keyboard/mouse transport or a real AGENT-to-USER handoff. Browser ownership remained USER.

## Multiline Events-list login detection — ACTIVATED

After explicit permission for read-only inspection, the existing desktop monitoring TaskSpace 48 showed the assigned Cvent viewer at `https://app.cvent.com/Subscribers/Events2/EventSelection`, with the authenticated Events table and Create Event control—not a login form. Only the outer monitoring page was scrolled; no Cvent controls, Return to Agent, navigation, session reset or build execution were invoked. The monitoring TaskSpace was handed back to the user afterward.

A representative multiline accessibility snapshot reproduces rejection by `authenticatedCventPage`: the `events.*(?:create event|search)` expression cannot span newlines. `app/session-security.mjs` now normalizes snapshot whitespace before matching, retaining the existing allowed-host, credential-URL, login-page and login/MFA/expiry denials. New regression coverage checks multiline Events-list acceptance, all auth denials across whitespace, explicit human incident attestation and continued USER ownership. **115 tests pass**, log `logs/multiline-login-full-tests.log`.

On explicit user approval, stopped waiting job `5f9f82cb-b89c-425a-87a8-cc500cedf56c` through its app Stop endpoint: STOPPED_REQUIRES_REVIEW, no stop failures or unreconciled spending, final run cost $0.794842. Verified no active job, live recorded Pi process or operation lock; gracefully restarted only the app, PID `31957` → **`36470`** on 8788. Receipt: `logs/multiline-login-activation-20260920T005122Z/receipt.json`. The fix is now loaded; API health and zero active jobs were checked. All 46 preserved files (including originals, job ledgers/reports, workbook versions and browser runtime) matched their post-Stop hashes. Browser ownership remains USER; no browser reset/logout, incident acknowledgment, paid prompt or Cvent mutation occurred. Recorded cumulative Pi spending remains $17.121114. A fresh upload and explicitly named target are required for another run; the stopped conversation is never restored. The observed UI confirms login visually, not API/event authorization or session-revocation attestation. Live acceptance of the corrected checker remains pending the next human handoff.

## Security acknowledgment fix — ACTIVATED

The user reported repeated login/security prompts. Root cause: `prepareTarget` persisted incident acknowledgment only after event lookup, unpublished-state validation and event identity verification all succeeded. An unrelated target error therefore discarded the acknowledgment and demanded it again on the next attempt. USER ownership was also described as if it necessarily meant logged out; it does not.

Source changes: `app/session-security.mjs` now separately verifies the explicit human invalidation attestation plus a read-only observation of an authenticated assigned Cvent page, then durably acknowledges the specific incident **before** event lookup/setup. It never navigates, reads cookies/tokens, logs out, replaces the browser, or grants authoring ownership. Failed login, browser replacement or takeover retains the unresolved incident; subsequent event failures do not erase a successfully verified acknowledgment. A new incident still needs a new attestation. Actual Cvent/SSO expiry remains human-login-only. `app/server.mjs` invokes this check under the existing handoff lock and clarifies that USER control is not necessarily logout.

**114 tests pass** (`logs/session-security-full-tests.log`), including acknowledgment persistence after target failure, no repeated prompts for resolved incidents, missing human attestation, failed login, takeover/replacement and query-data exclusion. During verification the user started another live job, `489f4d89-22b6-45c5-bed6-4339edbd63aa`, so initial activation was deferred rather than interrupting it.

On explicit user restart authorization, preflight found all runs already settled, no live recorded Pi process and no operation locks. Gracefully restarted only the app, PID `25446` → **`31957`**, on 8788. The fix is now loaded. Receipt: `logs/session-security-activation-20260920T004006Z/receipt.json`. Reused the still-valid 114-test evidence; 44 originals, job ledgers/reports, saved workbook versions, browser runtime and user reference files were hash-verified unchanged. Browser ownership remains USER; Steel/browser were not restarted, cookies/login were not cleared, no incident was acknowledged on the user's behalf, and no paid prompt or Cvent mutation occurred. Recorded total Pi spending across jobs remains **$16.326272**. API health and zero active jobs were verified after restart. A new run requires a fresh upload with its explicitly named target. Human incident invalidation/attestation remains required if unresolved; the fix prevents a verified acknowledgment being lost to a later event error, not bypassing that requirement.

## Named target + Forge UI — current

The user corrected the RR-source-event assumption after the CGJWS run selected Coconut Grove. Every **new upload now requires an explicit `eventName`**, persisted as `requestedEventName` before a fresh native session is prepared. This immutable upload-bound target—not `intake.json.eventName`, workbook text, browser state or historical jobs—is used for API/browser setup. Model output naming Coconut Grove (or null) cannot redirect the job; the regression suite exercises both. Legacy `/start` rejects a name different from the bound upload, and `/read` rejects old unbound uploads. Changing the target requires another upload/fresh session. No event was renamed or switched during implementation.

Flow: choose RR → preview/edit locally if desired → enter the exact existing target → **START BUILD** → human login/security handoff when necessary → API-first execution. Fixed policy now skips/report missing or ambiguous requirement values and dependent work without routine clarification questions; authorization, identity, safety, ownership and spending blockers still pause. The prior CGJWS run remains stopped, unchanged, at $1.100012; no conversation was restored.

`public/index.html`, `public/styles.css` and `public/app.js` reproduce the supplied Forge screenshot's layout: staging banner, sidebar/profile controls, progress strip, event and safeguards cards, intake/status columns, workbook editor and lower Cvent browser. Start/Stop, open browser, takeover, viewer reload, job-history selection, sheet selection/paging, revert and draft saves are connected. User 2/3 remain explicitly unavailable (one shared browser, not fictitious isolated profiles). RESET CVENT LOGIN remains disabled/operator-managed; it never silently clears credentials or replaces the browser. No instruction field was reintroduced.

The local workbook editor (`app/workbooks.mjs`, `app/workbook-editor.py`) stages uploads without Pi prompts, preserves the immutable original, and saves text edits as separate immutable versions. Prior versions are backups; edits never overwrite the original. Formula cells are read-only; formulas are preserved, not recalculated. First 64 columns and 80 rows/page are displayed. Invalid/stale saves, active-build writes, unsupported workbook structures and XML entity/DTD declarations are denied. ZIP parts outside the edited sheet are preserved. The selected version becomes a fresh job's immutable RR; source-workbook/version provenance is recorded. No new browser framework or execution agent was added.

**108 tests pass** (`logs/forge-target-full-tests.log`), including target binding, no model redirection, lifecycle/ownership gates and workbook preview/version preservation. Activated as PID `25446` on 8788; receipt `logs/forge-target-activation-20260920T002403Z/receipt.json` verifies 30 preserved files, including previous originals/ledgers, runtime, the Downloads mock workbook and the supplied screenshot. No paid prompt or Cvent mutation occurred.

**Rendered visual verification is pending.** Desktop Ego TaskSpace 48 refused takeover: the user had taken control and ended the task. That hard stop was respected; no alternate browser/space or ownership bypass was attempted. User may refresh the app directly or explicitly say continue before the same monitoring space is reclaimed for visual inspection. Do not claim a pixel-perfect verified match or full live end-to-end acceptance from local tests.

## End-to-end readiness work — prior checkpoint

`E2E-ACCEPTANCE.md` now tracks each SOW area, permitted routes, remaining integration/browser gaps, live prerequisites and acceptance evidence. Full RR completion and general deployment are **not accepted**. No prohibited mock workbook was applied, paid run launched or browser session replaced.

The first bounded prerequisite is implemented: production `cvent-api` capabilities omit legacy updates and expose their explicit policy denial; direct update calls produce BLOCKED receipts before any network access. A before-dispatch guard allows only confirmed-missing discount POSTs, never existing-item/event-setting edits. `configureDiscount` now always reuses existing codes unchanged, including same-job creations; mismatches return `requirementsSatisfied: false`/`action: preserved` and `PRESERVED_DIFFERENCE` receipts instead of implying RR success. Exact matches remain verified no-ops. Durable creation intent, acknowledgment, bounded readback, deduplication, Stop/ownership checks and uncertainty blocking remain.

This closes the normal production API-tool update path, not arbitrary shell execution or object-level Ego mutation. Underlying legacy adapter helpers remain for historical regression tests; production CLI rejection is independently exercised with intercepted network access. The remaining object-level browser protection and required API integration work are explicit pending gates in the checklist. Live acceptance still requires human session invalidation/login, an approved executable RR/target and bounded paid-run authorization.

Verification/activation: **102 tests pass** (`logs/create-only-full-tests.log`). Active server PID `12812`; receipt `logs/create-only-activation-20260919T235330Z/receipt.json` records the production capability probe, implementation hashes, and 27 preserved originals/ledgers/runtime files including the Downloads mock workbook. Browser ownership remains USER; no active job or operation lock was present before activation. No paid prompt, browser restart or Cvent mutation occurred. Recorded cumulative RR cost remains $13.31. This completed only the API-preservation prerequisite; stop new dispatch and obtain user direction before the next work item.

## Preservation instruction revision — history

The fixed job instruction, approved scope, local intake, execution prompt and appended runtime policy now require: **never change the event name; reuse existing items unchanged; create/configure only RR-required items confirmed missing within scope**. This includes defaults and prior-run items. Check identity, code/name and purpose before creation; ambiguous/incomplete searches do not establish absence. Do not overwrite, disable, replace or duplicate an existing item to satisfy the RR. Keep it and report differences as preserved/unresolved rather than RR-compliant, while continuing independent work. Clarifications, workbook text and API update capabilities do not override this policy. All prior safety/budget/ownership protections remain.

This revision adds native-agent instructions, not a new object-level API/browser sandbox. The underlying adapter still exposes update capabilities for its existing integration tests; their availability is expressly not permission to use them on existing items. No live paid run or Cvent mutation was performed. **98 tests pass** (`logs/preserve-existing-full-tests.log`), including checks that both intake and execution receive preservation instructions and the durable approved scope retains them. Behavioral compliance in a live RR remains untested. Activated as PID `7678`; receipt `logs/preserve-existing-activation-20260919T234032Z/receipt.json` verifies 27 preserved originals/ledgers/runtime files and the Downloads mock workbook. Browser ownership remains USER. No paid prompt, browser restart or Cvent write occurred; recorded RR spend remains $13.31 cumulative.

## Upload-only flow — current

The primary UI is now **choose RR → Upload & run RR → sign in when prompted → Pi executes**. There is no instruction field or manual event-name/Find Event form. `RUN_POLICY.instruction` supplies the fixed task: read this RR, identify its existing event, execute applicable requirements through API first/Ego for documented gaps, verify saved results, preserve originals/prior saves, and never delete or publish. New uploads accept only the workbook; instruction overrides are rejected before preparing a session. Genuine ambiguities, ownership, uncertain writes, login and budget boundaries still pause execution.

The button uploads immutable bytes into a new job/empty native Pi session, then explicitly starts paid local RR reading via `/api/jobs/:id/read`. The raw upload endpoint alone still sends no model prompt. Pi—not a fixed workbook parser—reads this actual workbook, identifies the existing event, explains its understanding and raises genuine questions in `intake.json`. During intake the job has no authorized browser runtime; API authoring and Ego access remain blocked. `app/intake-prompt.md` defines that small local handoff, not a prescribed execution plan. Human login ends with the explicit I've signed in ownership handoff; then execution continues automatically.

This remains a loopback-only, single-user/shared-browser app with one active job, **not concurrent team sessions or a multi-tenant deployment**. Simplifying upload does not add user isolation or authentication. Upload-only revision: **97 tests pass** (`logs/upload-only-full-tests.log`), including file-only instructions and rejection of overrides. No paid run or Cvent mutation was used to test this change. Activated on port 8788 as PID `4938`; receipt `logs/upload-only-activation-20260919T233334Z/receipt.json` verifies 27 preserved files, including the browser runtime, originals, ledgers and Downloads mock workbook. Desktop Ego inspection confirms Upload & run RR with no instruction or manual event field. Recorded RR costs remain $13.31 cumulative; the prior security incident remains unresolved.

A clear intake triggers the existing exact-name API lookup, event-history/budget guard **before browser navigation**, and Ego identity/login/location verification. The application then supplies verified execution context to the **same live Pi session**. Pi uses the existing Cvent adapter first and unchanged pinned Ego skill for documented gaps. No second agent, browser framework or workbook-specific replay sequence was added. The legacy `/start` route remains for compatibility, but the UI never calls it.

Genuine intake questions and login/setup failures pause the live job. `/api/jobs/:id/answer` accepts a bounded plain-text answer only for that actively waiting job; it verifies original bytes, the exact live session, Stop/ownership and cumulative spending. Clarification answers continue local reading; the explicit **I've signed in** action retries guarded setup. There is no `switch_session`, old-job restoration, new paid run, or automatic successor. While waiting, the process is idle and the job still blocks another upload; Stop/server shutdown settles it. Stopped/completed jobs still require fresh uploads.

Reading cost counts even if an event is never identified. Before identification, the gate conservatively considers all prior job costs; after identification it uses same-event history plus paid unassigned intake jobs. Costs display in the UI. Existing uncertainty markers, originals, prior saves and ledgers remain authoritative. Prior security-review reports block browser setup until the human explicitly confirms invalidating the affected Cvent session and fresh login verifies successfully. The checkbox records human attestation; it is not programmatic proof of Cvent token revocation.

The runtime policy now requests compact workbook reads, fewer redundant observations, and visible/non-sensitive UI inspection excluding hidden/password/authentication/CSRF fields. These are native-agent instructions, **not a new sandbox or a guarantee against all secret disclosure**. The server sets owner-only umask for itself and child tools. The character-by-character activity rendering bug is fixed. Browser-session reset/replacement is not automatic; a missing assigned target remains a genuine operator blocker.

Verification: **96 tests pass**, including new fake-native lifecycle and UI checks for read-before-setup, mock-warning clarification, same-session replies, automatic target verification, login consent, Stop races, malformed intake, stale-session rejection, prior uncertainty before navigation, and retained pre-target spending (`logs/simple-flow-full-tests.log`). No paid model prompt or Cvent configuration write accompanied implementation. Live native intake/clarification/authoring and speed improvement are not yet acceptance-tested. Existing security incident and mock workbook questions remain unresolved; do not infer live completion from these tests. Historical sections below describe earlier UI/launch behavior where they differ.

Activated on 8788 as PID `3253`, receipt `logs/simple-flow-activation-20260919T232910Z/receipt.json`. All 27 checked originals/ledgers/uncertainty records, browser runtime, global instructions and the user's Downloads mock workbook remained unchanged. Desktop Ego TaskSpace 48/p1 reloaded the live UI and confirmed the new Read & execute button, instruction field, absence of manual event controls, USER browser ownership and unchanged $13.31 cumulative spending. No upload or execution click occurred during that UI check.

## Production instruction isolation — current

The native RPC launch now uses `--no-context-files --append-system-prompt <project>/app/runtime-policy.md`. Fresh sessions alone do not prevent inherited global development policies. This removes AGENTS.md/CLAUDE.md discovery for the RR process, without modifying the operator's global policy, authentication, extensions, native tools, explicit Ego skill or application guards. The explicit runtime policy disallows importing development policies or historical checkpoint instructions, and uses native compaction rather than an arbitrary tool-round/50%-context stop. Completion, genuinely blocked remaining work, operator Stop, ownership/identity, uncertain writes and cumulative-budget boundaries remain valid stopping conditions. No automatic successor prompts or new RR consumption were added.

86 tests passed (`logs/runtime-policy-tests.log`). The installed native CLI/parser/resource-loader probe confirmed two fixture development contexts became zero with the actual launch flag, the exact runtime policy loaded, and a fresh native session had zero messages/cost: `logs/runtime-policy-probe-1789856699534/receipt.json`. End-to-end completion is not established by this probe; the authorized live run must provide that evidence.

Final activation: PID `78818`, receipt `logs/runtime-policy-final-activation-2026-09-19T22-28-45.614Z.json`, 87 passing tests (`logs/runtime-policy-final-tests.log`). Preflight exposed Cvent's separate `event-insights-ui.app.cvent.com/events/registrationInsights/registrationOverview?evtstub=<approved UUID>` route. That exact authenticated same-event overview is now accepted solely as a return source to approved Details/Overview; it is not a general authoring host or destination grant. Foreign/duplicate IDs, extra parameters, credentials, wrong hosts/paths and mutations during LOCATING remain denied. Live Return to Agent successfully navigated back and confirmed API/browser identity.

User-authorized fresh job `5ece14f0-51e1-45aa-88f5-1dc4db4b89aa` started through one Ego UI Execute click. Start receipt: `logs/runtime-policy-e2e/start.json`. Same immutable RR bytes and approved event; fresh session `01a0bbc9-b5d0-7401-bc2e-8cded7871678`, prior cumulative cost $5.236992. The zero-prompt prepared session was cleanly closed for the final idle reload and replaced with another empty session on Execute, never restored. All earlier originals/ledgers and the global development policy remain intact. Do not start a second run while it is active.

Live outcome: **REVIEW_REQUIRED / incomplete**, not an end-to-end pass. It read the workbook before agent Cvent commands and continued through 36 turns / 41 tool calls, beyond the old checkpoint. Existing dates/timezone/venue were verified, not replayed. It attempted one event-local palette Save, then incorrectly reloaded while the UI still showed `Saving...`; reload and subsequent CDP observation timed out. Palette persistence remains unknown. No second Save or new paid run was sent. Final native cost: $3.521674 for this run, $8.758666 cumulative. Evidence: `logs/runtime-policy-e2e/outcome.json` and the job's `unresolved-changes.json` / final reports.

Review also found that native Pi emitted its uncertainty report as a top-level array. The event-history guard now accepts nonempty arrays and `changes` as well as `uncertainWrites`, failing closed on malformed supported lists. 23 focused lifecycle/navigation/RPC checks pass (`logs/runtime-policy-post-run-safety-tests.log`), and a read-only check against the real saved records confirms this event is blocked from fresh execution until reconciliation. Activated after settlement as PID `83142`, preserving all eight originals/ledgers, uncertainty reports, browser runtime and global instructions. No browser reset or uncertain-save replay occurred. Next required step is assigned-browser connection recovery and read-only persisted-palette reconciliation, not another Execute.

## Post-restart palette reconciliation and fresh test — 2026-09-19

After human login, Return to Agent passed with the exact approved event name/UUID. A fresh Site Designer document loaded from Cvent showed **Color 1 `#141413` and Color 3 `#10069F`**, with top-level Save disabled. Only navigation and Theme/Colors inspection occurred; no field edit or repeat Save. The old job's `receipts/palette-readback-after-restart.{json,txt}` preserve this saved-result evidence. Its original uncertainty report was archived byte-for-byte as `receipts/unresolved-changes-before-palette-reconciliation.json`; the active report now records no outstanding uncertain writes and the verified resolution. Historical final reports and costs are unchanged. The event-history gate passes at $8.758666 cumulative; this does not mean the full RR is complete.

The operator-requested fresh test is job `0cd488a1-0209-461d-a556-69328bfe0334`, session `01a0bbe3-0440-7793-af4e-7618ad89b4a4`. One UI upload used the same immutable RR bytes and original filename; its session was verified empty/zero-cost, not restored. API/browser target confirmation passed, followed by one UI Execute click. Job entered RUNNING at stage `Read RR first`, using `api-first-ego-fallback` and carrying forward $8.758666. Native trace confirms workbook reading preceded its API and Ego work.

**Fresh-test outcome: REVIEW_REQUIRED / incomplete**, report status `PARTIAL_SECURITY_REVIEW_REQUIRED`. Broad `page.evaluate` enumeration of input/textarea/contenteditable values unexpectedly included hidden authentication material in tool output. No sensitive values are reproduced in this document. Pi stopped; its recorded PID is gone and no job operation locks remain. It reported no new saves or publishing, and verified existing venue/dates/timezone and persisted palette. Final native cost: **$4.553690 this run / $13.312356 cumulative**. All files under this job are now owner-only (0600; original 0400), directories 0700; this does not retract already-emitted tool output. App Take Control returned USER ownership. The affected Cvent session needs human invalidation, and future inspection must exclude hidden/password/authentication inputs before another explicitly authorized fresh run. No successor was started. Sanitized outcome: `logs/operator-restart-20260919T224933Z/fresh-test-outcome.json`.

## User-authorized restart — 2026-09-19 22:49 UTC

On explicit operator request, gracefully stopped app PID `83142` and restarted only `cvent-ego-runner-steel`, retaining its mounted Chrome profile. No other container was touched. Rebound the app to the unique restored Cvent login tab whose return URL identifies the approved event. New Steel session: `f0d8e04e-7468-4f2e-9715-05863c119d79`; app PID: `87741`. Read-only project Ego observation and local target inventory agree on the restored login page. This verifies transport only, not authenticated event identity or saved palette values.

Ownership is USER and app setup state is LOGIN_REQUIRED. The existing desktop monitoring page (TaskSpace 48, p1) was reloaded and handed to the operator; its embedded viewer now shows the live Log In tab, not Session Offline. The old job remains visible as historical REVIEW_REQUIRED; it was not resumed. Sign in manually, then Return to Agent. Reconcile persisted colors read-only before permitting another execution. A fresh test needs a fresh upload/session. All eight originals/job ledgers and uncertainty reports passed preservation hash checks. No upload, paid prompt, Cvent configuration write or implementation change occurred; recorded RR spending remains $8.758666 cumulative.

Private restart/transport receipts: `logs/operator-restart-20260919T224933Z/`. The initial administrative session parser expected an array instead of the API's `{sessions}` envelope; it failed before changing runtime metadata. After correcting that shape, rebind and Ego observation succeeded. Original uncertainty remains intact.

## Read-only recovery check — 2026-09-19 22:44 UTC

All eight jobs are idle (three REVIEW_REQUIRED, five UPLOADED); recorded prior job PIDs are gone. Connection lock PID `83142` owns the live loopback listener on 8788; no current/job operation locks were found. Runtime and Ego TaskSpace `1246080070` report AGENT ownership. The project Ego CLI resumed that existing space, but `task.tabs()` failed: **Assigned page target is missing; stop rather than select another tab**. Browser operations stopped at that guard. No reset, replacement target, reload, Save, upload or paid prompt followed. Palette persistence is still UNKNOWN and the original uncertainty report remains intact. Human inspection/restoration of the assigned browser through app controls is required before read-only persisted-color verification; do not clear the execution block or replay Save.

Receipt: `data/jobs/5ece14f0-51e1-45aa-88f5-1dc4db4b89aa/receipts/palette-reconciliation-blocked-20260919T224437Z.json`. Recorded RR spending is unchanged: $3.521674 latest / $8.758666 cumulative (development conversation and external costs are not metered here). No implementation change or new RR run occurred.

## Fresh session on upload — current contract

Every accepted upload immediately launches a new native Pi process and creates a verified empty, zero-cost session (`sessionMode: fresh-on-upload`). No model prompt or Cvent operation occurs during upload. Execute verifies that same session is still empty and idle, then sends its first prompt. Replaced unexecuted upload sessions are cleanly closed; active execution blocks new uploads. After restart, an unexecuted older upload gets a new empty session, never a restored conversation.

Each completed/stopped run releases its Pi process. The Continue Same RR/reply controls are removed and `/continue` rejects old-session continuation. Upload again for another run, even for the same workbook/event. Prior originals, ledgers and evidence remain intact.

A settled prior attempt no longer blanket-blocks the event. New jobs receive factual saved-result/report references (not prior conversation or workbook context), reconcile current saved state after reading their own RR, and skip already-satisfied requirements. The event budget sums each job's native cost once, retains the strictest prior allowance/reserve, and blocks genuine uncertain writes, unfinished operations, live processes or unreconciled spending. Prelaunch errors remain visible in the durable ledger and UI. These are lifecycle/budget safeguards, not a hardcoded RR execution sequence.

Verification: 85 project tests passed (`logs/fresh-on-upload-tests.log`). Two real HTTP uploads in an isolated native-Pi probe created different empty sessions, zero messages/cost, two `new_session` calls, no `switch_session` or model prompts, and clean shutdown: `logs/upload-native-probe-1789855572968/receipt.json`. No paid RR run or Cvent mutation was performed; workbook-first behavior and full execution still need live acceptance.

Activated on port 8788 with backend PID `70387`; receipt `logs/fresh-on-upload-activation-2026-09-19T22-08-10.299Z.json`. All six prior originals/ledgers and the browser runtime were byte-for-byte preserved. Older uploads remain unexecuted and receive a new empty session on Execute if needed. Prior activation details below are historical.

## Workbook-first native execution

Pi's first substantive task is now local reading and understanding of this upload's actual RR and approval/spending record, before its own Cvent API calls, TaskSpace acquisition, browser inspection or navigation. It then derives requirements, dependencies and a dynamic configuration/verification plan. Removed the prescribed per-requirement Ego click-step plan and repeated browser setup instructions; a concise living route plan is agent working memory, not an approval gate. No new workflow state machine or hard tool-order guard was added.

App-managed API lookup, login checks and event-opening setup still precede Execute; these are explicitly labeled as setup, not workbook processing or configuration. Upload stores the original and prepares an empty native session; paid model reading/execution starts only on explicit Execute. Initial execution progress says `Read RR first`, and the UI displays Pi's real job-local stage instead of masking it with RUNNING.

No-delete/no-publish, credential, ownership/Stop, uncertainty and spending boundaries remain. Existing bridge/API guards were not removed. This change improves agent instructions and removes rigid workflow prescriptions; it does not certify that a model will follow every instruction on a live run.

83 tests pass (`logs/workbook-first-tests.log`). The owned listener is PID `61051`; activation receipt `logs/workbook-first-activation-2026-09-19T21-44-38-780Z.json`. All six upload originals/ledgers and browser runtime were preserved. No paid prompt or Cvent mutation was performed for this change. The latest uploaded job `bf9c96a5-7bb8-457a-86b2-02ecc2f07d42` remains unexecuted; verify workbook-before-Cvent tool order in its eventual live trace.

## Historical new-upload isolation — superseded by fresh-on-upload above

Every accepted upload, including identical filename/bytes, creates a new job UUID, immutable original, progress state, receipts and dedicated Pi session directory. Upload itself does not start a paid model run. Execute creates a new native Pi process and calls `new_session`; it must confirm a changed idle session with zero messages, no queued work and zero prior session spending. The session file must belong to this job's session directory and its ID/path must not belong to another job. Failure blocks the initial prompt. The ledger records `sessionMode: fresh-on-execute` and verified `contextIsolation` evidence.

Choosing a new file disables Execute/Continue until upload succeeds. Successful upload detaches the old progress stream and clears prior replies, target selection and progress; the UI renders job-local progress instead of shared legacy state. Target selection survives page refresh only when explicitly selected for that job. Duplicate pending upload clicks are suppressed. Only the separate Continue Same RR action restores an existing session; upload never invokes it. Active work must still be stopped explicitly, and new contexts cannot bypass prior-event uncertainty/reconciliation or spending guards. Historical jobs and receipts are retained, not copied into the new conversation. This is single-user conversation/job isolation, not a multi-tenant filesystem sandbox.

Verification: 81 passing full project tests (`logs/fresh-upload-full-tests.log`) and 4 passing final UI checks (`logs/fresh-upload-ui-tests.log`). Two real native Pi probes created distinct empty sessions at zero cost (`logs/fresh-context-probe-1789853402379/receipt.json`); the hardened launch guard also passed a real zero-prompt probe (`logs/fresh-session-guard-probe-1789853749159/receipt.json`). No paid RR execution or Cvent mutation occurred during this change.

Fresh-upload activation used backend PID `57944`; receipt `logs/fresh-upload-activation-2026-09-19T21-36-32-502Z.json`. All five existing job ledgers/originals and browser runtime were unchanged by activation. Browser ownership and authentication were not reset.

## Historical native autonomy and continuation — continuation now disabled

The runner now leaves native Pi idle on `agent_settled` instead of cancelling its process. `CONTINUE SAME RR` / `POST /api/jobs/:id/continue` accepts an optional plain-text reply and continues the existing session, event and cumulative budget. After a clean Stop or backend restart it uses native `switch_session`, verifies the exact session ID/path and idle state, and reconciles native cumulative cost against the ledger before sending any prompt. It never calls `new_session` for continuation. Independent uploads still get fresh sessions.

Continuation rechecks API/browser ownership and identity, preserves previous reports/result/state/ledger in `receipts/continuation-*`, and rejects uncertainty markers, unfinished tools/operation locks, unreconciled costs, live prior processes, changed originals/events and exhausted budgets. Original policy remains in `executionPolicy`; the continuation's policy is recorded in `activeExecutionPolicy` and its history. No automatic continuation prompts, budget reset or replay of satisfied work.

Pi owns planning, bounded read-only recovery, investigation of actual widget sharing and independent requirement execution. Same-event subscreens may omit the event name after exact name/UUID verification; explicit conflicting names, wrong UUIDs and login still fail. Generic page headings are no longer treated as event names. Uncertain writes, human ownership, prohibited actions and financial ambiguity remain boundaries. The upstream Ego skill/runtime and bridge guards were not replaced or weakened.

Verification: 78 full regression tests (`logs/autonomy-full-tests.log`), then 16 focused lifecycle/UI checks after final race fixes (`logs/autonomy-final-focused.log`). Actual native `switch_session` restored session `01a0bb60-2e5a-700e-ad6a-8ef1e79fa9a9`, matching cumulative cost $2.549498 with zero model prompts; receipt `logs/native-continuation-probe-1789851931727/receipt.json`.

Activation receipt: `logs/autonomy-activation-2026-09-19T21-07-28-980Z.json`, backend PID `46384`. All five originals/ledgers and assigned browser runtime were preserved. The one authorized live continuation attempt was blocked before any prompt or ledger change because Cvent redirected to `/subscribers/login.aspx`; receipt `logs/autonomous-continuation-1789852078383.json`. Full live autonomous configuration remains unproven. This historical continuation workflow has since been replaced by fresh sessions on upload; its prior receipts remain evidence, not instructions to resume.

## Current operator policy: API first, Ego for documented gaps

The operator restored production-style API-first routing. `app/run-policy.mjs` now defines `executionPolicy: api-first-ego-fallback`; the job ledger, initial Pi prompt and runtime API use that single source. Pi reads `CVENT-API.md` and `CVENT-API-COVERAGE.md`, uses the existing `$CVENT_API_BIN` for supported authoring/readback, and uses the exact v2 Ego skill for documented API-unsupported UI work. API failures, policy denials and integration gaps do not authorize browser bypass. No custom HTTP/SDK authoring, standalone browser automation or replacement agent is permitted. Browser identity, login, spending, Stop and uncertainty gates remain unchanged.

The previous Ego-only job `32229dee-b74e-45b1-a75d-f47ae70e4f35` settled as REVIEW_REQUIRED/PARTIAL before the policy change, with final recorded Pi cost $2.549498 and no unresolved tool calls or uncertain writes reported. Its report records saved/reloaded event dates/timezone, then Site Designer identity/shared-content and workbook-conflict blockers. Original policy, receipts, report, budget and workbook remain immutable historical evidence. It was not replayed or retroactively relabeled API-first. A fresh upload cannot erase that event's prior execution; a new session reconciles its saved-result evidence and cumulative spending.

API-first activation: owned listener PID `36637`, 71 passing project tests; receipt `logs/api-first-activation-2026-09-19T20-43-22-083Z.json`. All five originals/ledgers and browser runtime were preserved. No new paid run or Cvent write was performed during policy restoration. This is routing activation, not complete production certification: retained `(C+D)` client restrictions, unconnected API operations, workbook conflicts and browser scope blockers still apply.

The earlier navigation experiment remains archived in `test/event-navigation.test.mjs.reverted`. Following explicit approval, a narrower, tested bridge fix now permits the Ego skill to open only the API-confirmed event's overview/details link from the neutral Events list. List input and authoring remain blocked there; direct navigation between different events is denied, including attempts to silently navigate back from a wrong event. Ordinary navigation inside the approved event remains supported. Live Ego navigation from Events → approved clone → Event Information passed with exact name/UUID verification and no business-data writes.

### Same skill in the operator and backend

The skill is now the exact upstream **v2.0.0**, matching `/Users/bailey/.pi/skills/ego-browser/SKILL.md` byte-for-byte (SHA-256 `9402bf03db895209a755d5e2af9b436dbf98eed0911907110a4ea482c30632de`). The vendor pin is `dca7003349c5f7132189ba00547cbbd7ff8e597e`; the actual upstream TaskSpace/Page runtime is built and used, not a fabricated compatibility facade. Its one-line session-identity patch is preserved in `ego-bridge/patches/steel-session-ledger.patch`: Steel identity, not changing shell parent PIDs, namespaces durable Page/ref state. The obsolete helper facade is archived, not loaded. See `.pi/skills/ego-browser/references/steel-bridge.md` for setup restrictions and unsupported desktop/iframe capabilities.

`app/pi-rpc.mjs` now launches native Pi with `--no-skills --skill <absolute project SKILL.md>` to prevent same-name/global skill collisions, and prepends this project's `bin` to PATH so the verbatim skill CLI resolves to the assigned bridge. Native tools and extensions remain unchanged. A real, idle RPC startup returned exactly one skill command, `skill:ego-browser`, without a model prompt. That installed Pi version omits paths from `get_commands`; launch-argument tests and skill-byte equality provide the separate path/content evidence. This is discovery validation, not proof of a model-driven RR.

71 project regression tests pass after activation, event-location, Execute-button polling and API-first routing fixes. The existing 226 focused upstream tests, upstream typecheck and API-reference checks also passed for the unchanged vendor runtime. Live v2 read-only checks passed on the existing approved event: TaskSpace lookup, assigned-tab adoption, evaluate, viewport/full-page snapshots, cross-process Page/ref reuse, URL/selector waits, and finish retaining the Page. The receipt is `data/jobs/389a1f49-47a9-4666-bebc-e5ce8a224462/receipts/ego-v2-upgrade.json`. One initial resumption script had a compile-time name collision with the injected `snapshot` helper; renaming the local variable fixed it before any browser operation occurred. Creation/deletion and foreign-target access fail closed; claim/takeover cannot bypass app ownership gates.

At the earlier workbook-first activation, the owned listener was PID `61051` on port `8788`. The existing Steel session, target, all five upload originals and job ledgers were preserved. The updated policy/launch settings are active. The operator explicitly chose dynamic, full-RR execution rather than an automatic acceptance slice: each uploaded workbook and confirmed event are supplied per job, and no one-setting limit is imposed. The prompt must not substitute a prior workbook, cached plan or test event. Input-isolation tests exercise different uploaded bytes/filenames and different selected events through the Pi prompt. Existing scope prohibitions, per-job event identity, spending, ownership and uncertainty gates still apply.

Activation caught and corrected two v2 preflight regressions: RETURNING allows assigned-tab read-only inventory but still forbids task acquisition/takeover and mutations; `eventObservationProgram()` explicitly JSON-serializes observations because v2 `cliLog(object)` uses inspect-format output. App preflight uses upstream compatibility observation helpers without acquiring a TaskSpace; model-driven authoring continues to use the v2 skill/Page API.

Live `POST /api/return-agent` returned HTTP 200 with authenticated AGENT ownership and matching API/browser event identity. `/api/state` is READY, the updated dynamic-input UI and viewer respond, and a fresh native Pi startup discovered exactly one Ego skill with configured model/pricing. The idle Pi probe was stopped cleanly without a model prompt. Receipts are under `logs/reload-2026-09-19T20-12-29-032Z.json`, `logs/return-agent-*.json`, and `logs/rpc-readiness-*/receipt.json`. An earlier failed preflight is retained as evidence, not mistaken for success.

The later automatic-location test reached a real Cvent login redirect when attempting same-event navigation: the session had expired despite the still-rendered Event Information screen. The return endpoint correctly responded HTTP 409, preserved the session and changed ownership to USER / status LOGIN_REQUIRED. Evidence: `logs/event-location-1789849654437.json`. Live LOCATING navigation remains pending fresh human login; neutral-list navigation, dynamic routes, v2 program sequencing and restricted ownership have unit-test coverage. The app must not report this blocked live attempt as a navigation pass.

No paid prompt, Cvent configuration write or full RR accompanied activation. Live browser saved-result validation and Stop during actual work remain unproven. Skill/CLI, readiness and mocked lifecycle checks are not proof of model-driven RR completion.

This is a transport/lifecycle adapter, not another agent. `app/pi-rpc.mjs` launches the installed `pi --mode rpc`; native Pi performs all reasoning, tool use and session management. There is at most one executing job. No SDK agent or replacement browser implementation was added.

## Activation

The previous server may still own port 8787. Do not replace or kill an active or unowned process. Confirm its owner has stopped it before switching to this entrypoint. Editing files does not convert a running interactive Pi or old JSON-mode process into RPC mode.

From the working Pi environment, retain `PI_PROVIDER` and `PI_MODEL` (both required), existing Pi authentication/configuration, workbook tooling and Steel runtime. No local app username, password or connection token is required. Run:

```sh
cd /Users/bailey/cvent-ego-runner
export CVENT_CREDENTIALS_FILE=/Users/bailey/cvent-agent/.env
PORT=8788 npm start
```

The server binds **127.0.0.1 only**. The running local instance uses http://127.0.0.1:8788 (`PORT=8788`); the CLI default remains 8787. Open it directly—no username or password. HTTP and viewer WebSocket requests must come from loopback with a loopback Host; cross-origin and cross-site browser requests are rejected. This is not authentication: other local processes/users can access the app. Do not expose or reverse-proxy it publicly. Cvent login and the execution approval gates are unchanged. Direct Steel debug ports remain a separately secured local operator surface.

A private exclusive `data/rr-connection.lock` prevents two connection processes from owning this checkout. A stale lock is deliberately not stolen. Before an operator removes a stale lock, verify its process is gone, reconcile any owned process group/browser work, inspect jobs, and settle spending and uncertain writes. A persisted STARTING/RUNNING/STOPPING job blocks new execution after a restart; there is no automatic crash recovery/replay.

## Workflow

1. Upload the original `.xlsx`; its bytes, original filename and SHA-256 are saved under `data/jobs/<uuid>/`. The original is read-only. Nothing calls a model during upload.
2. Enter the exact **Cvent event name** and click **Find event via API**. Existing local OAuth credentials authenticate read-only lookup; zero or duplicate matches block selection. If the app already owns the browser, the UI automatically requests event location. Otherwise, sign in through Take Control and click **Return to Agent**. The user does not navigate to the event. Return observes the current location and validates API access, then uses Ego v2 `taskSpace` / Page navigation to open the selected event's Details page from the neutral Events list or another page of that same event. Restricted `LOCATING` ownership permits only confirmed overview/details navigation, never authoring or takeover. Exact displayed name/UUID and login checks must pass before AGENT ownership and Execute. Wrong-event, login, ambiguous-location and uncertainty failures stop; there is no silent recovery from another event. Selection changes invalidate the previous target lock.
3. Click **Execute RR**. There are no scope, allowance or reserve fields. The user-approved defaults in `app/run-policy.mjs` apply the workbook's website + registration requirements to the confirmed event, excluding sessions/speakers and unrelated operations. The goal is **under $60 total per completed RR**.
4. Upload already created a native RPC process using the working provider/model and low reasoning, with confirmed changed idle session ID, zero messages and zero cost. Execute checks that session is still empty and sends its first `prompt` containing the workbook path, SOW, event and cumulative budget. It never resets active work or restores an old conversation.
5. Pi reads the workbook, API guide/coverage and project-pinned v2 Ego skill, saves a per-requirement route plan, and uses the existing API adapter for supported operations/readback. Only documented API-unsupported work uses Ego/Steel. Local workbook tools remain available. App-managed read-only API identity preflight stays mandatory; API failures and uncertain writes block execution rather than triggering UI bypass. Missing adapter integration is not API absence; report it as a blocker.
6. Review Pi's reply/results. `agent_settled` is REVIEW_REQUIRED, not proof of completion; settlement and Stop release the owned process. Upload again for a fresh run. New uploads reconcile saved work and cumulative event spending without importing old conversations. No successor prompt or uncertain write is automatically replayed.

## Endpoints

All are local-only and require no app credentials. Upload uses multipart field `rr`; other POST bodies are JSON.

- `POST /api/jobs` — save an original RR, prepare a fresh empty native session, return job ID/workspace/hash/session ID. No model prompt.
- `POST /api/jobs/:id/continue` — disabled (409); upload for a fresh run.
- `GET /api/jobs/:id` — durable job ledger.
- `POST /api/target` — `{eventName}`; invalidates the old target lock and performs exact-name Cvent API search. Returns one unique `apiEvent`. Human login and `POST /api/return-agent` confirm the same event in Steel.
- `POST /api/jobs/:id/start` — `{authorizedEventName}` only; scope and spending overrides are rejected. The runner snapshots its fixed scope and internal spending policy into the durable job before prompting Pi; the name must match the previously confirmed target. API and browser preflight must both pass before any paid prompt; the API event UUID and browser identity are persisted, not guessed from the name.
- `POST /api/jobs/:id/rpc` — native `get_state`, `get_session_stats`, `get_last_assistant_text`, `get_commands`, `steer` or `follow_up`. Queue messages require bounded plain text `message`, not slash commands. Reset, model changes, arbitrary shell RPC and ungated prompts are not exposed. Extension UI dialogs appear in the event stream but interactive extension-dialog responses are not yet exposed; stop and resolve such blockers rather than leaving paid execution unattended.
- `GET /api/jobs/:id/events` — live SSE with sequence IDs. Reconnection is live-only; durable full history is available as `progress.jsonl`, so disconnects do not lose evidence.
- `GET /api/jobs/:id/results/:file` — allowlisted `job.json`, `state.json`, `progress.jsonl`, `result.json`, `final-report.json`, `final-report.md`. Missing artifacts return 404, not fabricated success. Section receipts and native sessions also remain in the private workspace.
- `POST /api/jobs/:id/stop` — revoke job browser control, acknowledge `clear_queue`, then native `abort` and `abort_bash`, snapshot costs, terminate only the owned Pi process group (including its Ego programs). If queue clearing fails, terminate owned execution without unsafe native abort. The shared Steel service and human browser are not destroyed. Take Control and graceful server shutdown use this same Stop path.

## Safety and spending limits

- Stop does not undo API/UI requests already submitted. Keep receipts, progress and unresolved changes; inspect authoritative Cvent state before further work. `STOPPED_REQUIRES_REVIEW` is not rollback or completion.
- Job ledgers, sessions, reported cumulative cost (including Pi-reported tool/compaction costs), originals and evidence survive fresh context. Every new run uses a fresh upload/session; prior conversation context is never resumed. New uploads cannot hide prior execution for the same event.
- Cost controls are internal, not operator form fields. The fixed policy uses a $60 allowance and $10 external/in-flight reserve, pausing at $50 of cumulative reported Pi usage across the event's jobs to leave headroom for the under-$60 total goal. Costs are sampled every two seconds and after cancellation. This is **not an exact billing cap**: provider usage may arrive late, one request can overshoot, and external Steel/API charges are not metered here. Missing model pricing blocks prompts; lost metering pauses execution and flags reconciliation. A reported zero does not prove zero spend.
- Cvent prohibitions are execution instructions plus the existing UI guards, **not a new security sandbox**. Native Pi retains the requested shell/workbook/API capabilities. Use appropriately scoped Cvent credentials. Never delete/archive, publish events, send communications, change other events, or blindly replay uncertain writes.
- Server restart does not automatically terminate orphaned resources or reconcile provider billing. Native Pi exits on stdin EOF, but durable in-flight records still require operator review.

## Verification

`npm test` runs transport/lifecycle fakes and existing workbook/bridge tests, without paid model calls or Cvent mutations. A real RPC launch with the installed extensions, live login, Cvent API availability, browser cancellation and one approved RR still require an operator acceptance run. No under-$60 completion claim has been measured yet.
