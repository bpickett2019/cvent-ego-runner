# Historical runtime-policy path (not loaded)

The native launch no longer appends this file. Active execution uses Pi's built-in system prompt, one job prompt/SOW from app/runner-prompt.md, and the explicitly selected Ego skill plus bridge reference. Development context-file discovery remains disabled. Application safety checks remain separate from prompt instructions.
