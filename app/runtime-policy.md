# Historical runtime-policy path (not loaded)

The native launch no longer appends this file. Active execution uses Pi's built-in system prompt, one short job task from app/native-task.md pointing to the captured scope from app/standing-sow.md, and the explicitly selected Ego skill plus bridge reference. Development context-file discovery remains disabled. Application safety checks remain separate from prompt instructions.
