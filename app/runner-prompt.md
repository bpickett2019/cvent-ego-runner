Execute—not review—the uploaded RR in authorizedEvent. Read this job's original.xlsx across all sheets and dependencies; configure only applicable event-build requirements and dependencies: event details (dates/timezone/location/capacity), RR-supplied branding/assets, website/theme/header/footer/RR-required widget types, registration, admission items, pricing, paths, optional items, vouchers and advanced rules. Choose your own plan. Use this workbook and current live saved Cvent state, not historical tasks/transcripts/workbooks. Keep the upload-bound event identity/name fixed; never reset/clone it.

Reuse exact matches; create missing required objects and edit existing event-only differences, including admissions/prices/relationships—not duplicate workarounds or inspection-only reports. Preserve RR spelling, values and formatting; never invent defaults or suffixes. Prove scope before writing; an event URL/new template is insufficient. Never modify existing shared/account-wide objects, definitions or defaults. Reuse exact shared matches; create separate required shared objects only through supported, proven-isolated workflows. Unknown scope/inputs or unsupported operations block dependent requirements only; execute independent work and report precise blockers without self-authorizing exceptions.

Use "$CVENT_API_BIN" first for supported operations; consult CVENT-API.md as needed. For documented gaps, read .pi/skills/ego-browser/SKILL.md and references/steel-bridge.md; use only "$EGO_BROWSER_BIN" nodejs with assigned local Steel. Never bypass API failures/tool denials or use direct HTTP/CDP/substitute browsers. Use RR-EVIDENCE.md and ./bin/rr-evidence for cached workbook views/comparisons; inspect uncaptured original details. Complete scoped lookups and fresh saved verification remain required; identity matches alone prove nothing. Never begin a write you cannot verify. Await saves; inspect validation. Avoid repeated unchanged reads. Configure RR-required pages in the applicable existing editor; do not assume website requirements require enabling Cvent's separate Website feature. Site Designer last after other executable work, including setup and dependencies; Continue, never Restore. Aim for ~90 minutes, not a cutoff or verification shortcut.

Never delete/archive anything, including new objects/links. Never publish, send communications, access attendee/registrant records, configure sessions/speakers or affect other events. No account administration, merchant/payment-account provisioning, banking/settlement changes, shared payment/tax/currency or credential/integration-identifier changes. Authentication/security attestations are human-only; never inspect secrets, cookies/storage, page source or hidden/password/token inputs. Do not modify runner/guards or spawn agents.

Recover ordinary selector/navigation errors. Stop immediately on operator Stop, identity drift, lost AGENT ownership, expired login, exposed secrets, crash/disconnect or uncertain execution/save. Never replay an uncertain save, clear uncertainty or reload crashed pages to continue.

Verify applicable values and saved connections, not clicks/toasts. Human updates amend execution unless requesting pause/Stop. Finish only when complete or no permitted executable work remains, unless a safety stop intervenes. DONE requires everything applicable implemented, connected, verified and saved as Draft; otherwise INCOMPLETE. Never disguise blocked/untested work as N/A/excluded; explain genuine exclusions/non-applicability separately.

Keep state.json current, receipts and unresolved-changes.json when needed. Save source-referenced creations, modifications, exact matches and blockers in reports/final-report.md. reports/final-report.json requires eventId, status (DONE/INCOMPLETE), completion booleans website/registration/dependencies/draft, and blockers/untested arrays. DONE requires all four true and both arrays empty. Do not consume another RR.
