Execute—not review—the uploaded RR in authorizedEvent using the Ego skill and assigned local Steel browser. Read this job's original.xlsx across relevant sheets and dependencies, choose your own plan, and execute every applicable requirement. The upload-bound target wins over workbook names. Never change the event name.

DONE means the RR-required website (theme/branding, header, footer, all six body-widget types), registration (admission items, pricing, registration paths, optional items, vouchers, advanced rules), and dependencies are saved, connected and verified in Cvent, still Draft. Missing, blocked or unverified requirements mean INCOMPLETE. Reading, reporting or ending a session is not completion.

Inspect the complete event-scoped catalog, including prior creations. Reuse only exact RR matches in values, relationships, spelling, numbers and formatting. For any difference or missing object, create a separate RR-compliant object; preserve originals. Fully configure and verify new objects and their dependencies. Do not invent missing values or alter RR names/codes to evade uniqueness. Record concrete platform blockers and continue independent work.

Use "$CVENT_API_BIN" first for supported operations; consult CVENT-API.md as needed. For browser work, read .pi/skills/ego-browser/SKILL.md and references/steel-bridge.md; use only "$EGO_BROWSER_BIN" nodejs. API failures do not authorize browser bypass. No substitute browser or direct HTTP/CDP authoring. Optional compact workbook/catalog views: `./bin/rr-evidence help`.

Never delete/archive, publish, send communications, access attendee/registrant records, configure sessions/speakers, or change other events, account-global resources, merchant/financial settings or integration identifiers. Authentication and security attestations are human-only; never inspect credentials, cookies, storage, page source, network secrets or hidden/password/token inputs. Do not modify the runner or guards, spawn agents, or load prior transcripts or workbooks.

Recover ordinary navigation/selector errors yourself. Stop on event drift, lost AGENT ownership, disconnect, operator Stop, expired login, exposed secrets, page crash or uncertain execution/save. Never replay an uncertain save, reload a crashed page to continue, or clear uncertainty evidence.

Verify saved results, not just clicks/toasts. Keep state.json current; save receipts, unresolved-changes.json when needed, and reports/final-report.md plus reports/final-report.json. Report creations, exact matches and specific blockers with source references—not a review request. Final JSON must include eventId, status (DONE/INCOMPLETE), completion booleans website/registration/dependencies/draft, and blockers/untested arrays. DONE requires all four true and both arrays empty after saved verification. Finish only when complete or no permitted executable work remains; do not consume another RR.
